---
phase: 01-profile-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260512_teammate_profiles.sql
  - package.json
  - package-lock.json
  - src/lib/recgon/skillVocabulary.ts
  - src/lib/recgon/skillTagger.ts
  - src/lib/prompts.ts
  - src/lib/recgon/types.ts
  - src/__tests__/skillVocabulary.test.ts
autonomous: false
requirements: [PROFILE-02, PROFILE-03]
tags: [supabase, migration, canonical-vocab, cmdk, walking-skeleton]
must_haves:
  truths:
    - "The canonical skill vocabulary lives in exactly one place; both the skill picker and skillTagger import from it (per D-14, PROFILE-02)."
    - "A new additive teammate_profiles table exists in Supabase with `(team_id, user_id)` uniqueness, raw + canonical text[] columns, and a numeric capacity (per D-04, D-14, PROFILE-03)."
    - "The teams table has a new profile_visibility column defaulting to 'team_visible' (per D-17, D-18)."
    - "cmdk@^1.1.1 is installed and lockfile committed; `npm run build` resolves it (Pitfall 4)."
    - "Existing teammates table schema is unchanged (per D-05)."
  artifacts:
    - path: "supabase/migrations/20260512_teammate_profiles.sql"
      provides: "additive table teammate_profiles + profile_visibility column on teams + updated_at trigger"
    - path: "src/lib/recgon/skillVocabulary.ts"
      provides: "CANONICAL_ROLES, CANONICAL_MODIFIERS, CANONICAL_VOCAB, CANONICAL_SET, CanonicalTag type, isCanonical()"
    - path: "src/lib/recgon/types.ts"
      provides: "TeammateProfile type, ProfileVisibility type"
    - path: "package.json"
      provides: "cmdk@^1.1.1 dependency"
  key_links:
    - from: "src/lib/prompts.ts"
      to: "src/lib/recgon/skillVocabulary.ts"
      via: "import { CANONICAL_ROLES, CANONICAL_MODIFIERS }"
      pattern: "import.*skillVocabulary"
    - from: "src/lib/recgon/skillTagger.ts"
      to: "src/lib/recgon/skillVocabulary.ts"
      via: "import (replacing inline list)"
      pattern: "from ['\"]\\./skillVocabulary['\"]"
---

<objective>
Plan 01 establishes the Walking Skeleton substrate for Phase 1: extract the canonical skill vocabulary into a single source-of-truth module, run an additive migration that creates `teammate_profiles` and adds `profile_visibility` to `teams`, and install the `cmdk` dependency required by Plans 02-04.

Purpose: PROFILE-02 requires a single canonical vocab shared between the picker and the existing `skillTagger`. PROFILE-03 requires a separate additive table. Without both landing first, Plans 02-04 cannot import vocab nor write profile rows.

Output: A migration file, a vocab module, a refactored `skillTagger.ts` that imports the vocab, a TypeScript type for `TeammateProfile`, and `cmdk` in `package.json` — pushed to Supabase and verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-profile-foundation/01-CONTEXT.md
@.planning/phases/01-profile-foundation/01-RESEARCH.md
@.planning/phases/01-profile-foundation/01-PATTERNS.md
@.planning/phases/01-profile-foundation/SKELETON.md
@supabase/migrations/20260428_project_integrations.sql
@src/lib/prompts.ts
@src/lib/recgon/skillTagger.ts
@src/lib/recgon/types.ts

<interfaces>
<!-- Existing source-of-truth: canonical vocab inlined in prompts.ts -->

From `src/lib/prompts.ts:893-895` (to be extracted verbatim):
```
Roles: engineering, frontend, backend, mobile, devops, design, ux_design, marketing, social_media, content_writing, copywriting, seo, ads, growth, analytics, data, sales, customer_support, product, strategy, research, qa, finance, operations, legal

Modifiers (optional, only if obviously relevant): ai, ml, video, photo, branding, community, partnerships, fundraising, hiring
```

From `supabase/migrations/20260428_project_integrations.sql` (additive-table + updated_at trigger pattern — Pattern Map analog).

From `src/lib/recgon/types.ts` (additive — new types land here, alongside existing `Teammate`, `FitProfile`, `SkillStat`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Extract canonical vocab into `skillVocabulary.ts` and refactor `skillTagger` to import it</name>
  <read_first>
    - src/lib/prompts.ts (lines 880-925, the `TAG_TASK_SKILLS_SYSTEM` block — verbatim source of the canonical lists)
    - src/lib/recgon/skillTagger.ts (current consumer of the inlined vocab via the prompt string)
    - .planning/phases/01-profile-foundation/01-PATTERNS.md (§`src/lib/recgon/skillVocabulary.ts`, const-module structural analog)
    - src/lib/recgon/match.ts lines 31-71 (`TITLE_STOPWORDS` / `TITLE_ALIASES` — the const-module shape pattern to mirror)
  </read_first>
  <behavior>
    - Test 1: `CANONICAL_ROLES` array length equals the number of comma-separated tokens on `prompts.ts` line 893 (parity assertion).
    - Test 2: `CANONICAL_MODIFIERS` matches line 895 token-for-token in order.
    - Test 3: `CANONICAL_VOCAB` equals `[...CANONICAL_ROLES, ...CANONICAL_MODIFIERS]` (no extras, no duplicates).
    - Test 4: `CANONICAL_SET` is a `Set<string>`; `isCanonical('engineering')` is `true`; `isCanonical('nodejs')` is `false`.
    - Test 5: After refactor, `TAG_TASK_SKILLS_SYSTEM` still contains the same Roles + Modifiers comma-joined strings (regex `Roles:\s*engineering.*legal` and `Modifiers.*hiring`).
  </behavior>
  <action>
    Create `src/lib/recgon/skillVocabulary.ts` exporting four const arrays + one set + one type + one type-guard, all derived from `src/lib/prompts.ts:893` (roles) and `src/lib/prompts.ts:895` (modifiers).

    Exports (exact names):
    - `CANONICAL_ROLES`: readonly tuple `as const` containing the 25 role tokens in `prompts.ts:893` left-to-right.
    - `CANONICAL_MODIFIERS`: readonly tuple `as const` containing the 9 modifier tokens in `prompts.ts:895` left-to-right.
    - `CANONICAL_VOCAB`: `[...CANONICAL_ROLES, ...CANONICAL_MODIFIERS] as const`.
    - `CanonicalTag`: `typeof CANONICAL_VOCAB[number]`.
    - `CANONICAL_SET`: `new Set<string>(CANONICAL_VOCAB)`.
    - `isCanonical(tag: string): tag is CanonicalTag` returning `CANONICAL_SET.has(tag)`.

    No external imports — pure module. No Supabase, no Next types, no React. Module-doc comment must state: "Single source of truth for Recgon canonical skill vocabulary. PROFILE-02. Phase 1 (2026-05-12)."

    Then refactor `src/lib/prompts.ts:889-925` (the `TAG_TASK_SKILLS_SYSTEM` template literal):
    - Add `import { CANONICAL_ROLES, CANONICAL_MODIFIERS } from '@/lib/recgon/skillVocabulary'` at the top of `prompts.ts`.
    - Replace the literal `engineering, frontend, backend, ... legal` after `Roles: ` with `${CANONICAL_ROLES.join(', ')}`.
    - Replace the literal `ai, ml, video, ... hiring` after `Modifiers (optional, only if obviously relevant): ` with `${CANONICAL_MODIFIERS.join(', ')}`.
    - Convert the surrounding string from `'...'` to a backtick template literal if not already.

    Then refactor `src/lib/recgon/skillTagger.ts`:
    - Replace any locally hardcoded canonical-tag references (search the file for the literal `'engineering'`, `'frontend'`, `'backend'` lists used during sanitization) with `import { CANONICAL_SET, CANONICAL_VOCAB } from './skillVocabulary'` and reference `CANONICAL_SET.has(tag)` instead of any inlined predicate. If `skillTagger.ts` does NOT currently hold an inlined predicate (only relies on the prompt), import is still required so that downstream sanitization in this phase can use `CANONICAL_SET` — add the import statement and add a top-of-file comment: `// Phase 1: canonical vocab moved to skillVocabulary.ts (PROFILE-02).`

    Create `src/__tests__/skillVocabulary.test.ts` covering all 5 behaviors above. Use the existing vitest pattern from `src/__tests__/recgonMatch.test.ts` (no shared setup, plain `describe` / `it` blocks).

    Then add the new `TeammateProfile` and `ProfileVisibility` types to `src/lib/recgon/types.ts` (alongside existing `Teammate`):
    - `export type ProfileVisibility = 'team_visible' | 'owner_only';`
    - `export interface TeammateProfile { id: string; teamId: string; userId: string; skillsRaw: string[]; strengthsRaw: string[]; interestsRaw: string[]; skillsCanonical: string[]; strengthsCanonical: string[]; interestsCanonical: string[]; weeklyCapacityHours: number | null; createdAt: string; updatedAt: string; }`

    Do not touch any other file. Do not call `chatViaChain`, do not write Supabase code, do not import from `cmdk`.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/skillVocabulary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/recgon/skillVocabulary.ts` exists and exports the 6 names listed above.
    - `grep -c 'CANONICAL_ROLES' src/lib/prompts.ts` returns `1` or higher (import line plus interpolation).
    - `grep -n 'Roles:.*engineering.*legal' src/lib/prompts.ts | grep -v '^#' | wc -l` returns `0` (literal hardcoded list removed).
    - `grep -n 'Modifiers.*hiring' src/lib/prompts.ts | grep -v '^#' | wc -l` returns `0`.
    - `grep -c "from './skillVocabulary'" src/lib/recgon/skillTagger.ts` returns `1` or higher.
    - `grep -c 'export interface TeammateProfile' src/lib/recgon/types.ts` equals `1`.
    - `grep -c 'export type ProfileVisibility' src/lib/recgon/types.ts` equals `1`.
    - `npm run test -- --run src/__tests__/skillVocabulary.test.ts` exits `0`.
    - `npx tsc --noEmit` exits `0`.
  </acceptance_criteria>
  <done>
    Single source of truth established; `skillTagger.ts` and `prompts.ts` both read from it; types for the next plan's storage layer exist.
  </done>
</task>

<task type="auto">
  <name>Task 1.2: Write additive migration for `teammate_profiles` + `teams.profile_visibility`</name>
  <read_first>
    - supabase/migrations/20260428_project_integrations.sql (analog — additive table + unique index + updated_at trigger)
    - supabase/migrations/20260426_recgon_admin.sql (existing `teammates` table — confirms `team_id` and `user_id` are `text`, NOT `uuid`; Pitfall 5)
    - .planning/phases/01-profile-foundation/01-PATTERNS.md (§migration analog, top of file)
    - .planning/phases/01-profile-foundation/01-RESEARCH.md (Pitfall 5 — table-name + column-type confirmation)
  </read_first>
  <action>
    Create `supabase/migrations/20260512_teammate_profiles.sql` following the exact structural pattern of `20260428_project_integrations.sql`.

    Section 1 — header comment:
    Two-line block comment describing the table: "Teammate self-declared profiles. Additive layer over the existing teammates row; field-level fallback handled by profileMerge. Phase 1: PROFILE-03, D-04."

    Section 2 — `create table if not exists teammate_profiles` with columns (in this order):
    - `id uuid primary key default gen_random_uuid()`
    - `team_id text not null references teams(id) on delete cascade`
    - `user_id text not null references users(id) on delete cascade`
    - `skills_raw text[] not null default '{}'::text[]`
    - `strengths_raw text[] not null default '{}'::text[]`
    - `interests_raw text[] not null default '{}'::text[]`
    - `skills_canonical text[] not null default '{}'::text[]`
    - `strengths_canonical text[] not null default '{}'::text[]`
    - `interests_canonical text[] not null default '{}'::text[]`
    - `weekly_capacity_hours numeric` (nullable — D-06: blank means "I haven't said anything")
    - `normalization_pending boolean not null default false` (Pitfall 7 — set true when LLM degraded and canonical arrays are empty)
    - `created_at timestamptz not null default now()`
    - `updated_at timestamptz not null default now()`

    Section 3 — unique key + indexes:
    - `create unique index if not exists uq_teammate_profiles_team_user on teammate_profiles (team_id, user_id);` (D-04)
    - `create index if not exists idx_teammate_profiles_team on teammate_profiles (team_id);`

    Section 4 — updated_at trigger:
    Copy the `*_touch_updated_at` function + trigger pattern from `20260428_project_integrations.sql` lines 36-47 verbatim, renaming function to `teammate_profiles_touch_updated_at` and trigger to `trg_teammate_profiles_touch_updated_at`.

    Section 5 — `teams.profile_visibility` column extension:
    - `alter table teams add column if not exists profile_visibility text not null default 'team_visible' check (profile_visibility in ('team_visible','owner_only'));`
    - Comment line preceding: `-- D-17/D-18: team-level visibility toggle, default collaborative.`

    Do not add RLS policies (the codebase uses service-role-only access; matches existing tables per CLAUDE.md key rules).

    No data migration; no seed; no `drop` statements.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260512_teammate_profiles.sql &amp;&amp; grep -c 'create table if not exists teammate_profiles' supabase/migrations/20260512_teammate_profiles.sql &amp;&amp; grep -c 'profile_visibility' supabase/migrations/20260512_teammate_profiles.sql &amp;&amp; grep -c 'uq_teammate_profiles_team_user' supabase/migrations/20260512_teammate_profiles.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260512_teammate_profiles.sql` exists.
    - `grep -c 'create table if not exists teammate_profiles' supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c 'team_id text not null references teams' supabase/migrations/20260512_teammate_profiles.sql` equals `1` (NOT `uuid`).
    - `grep -c 'user_id text not null references users' supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c 'skills_raw text\[\]' supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c 'skills_canonical text\[\]' supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c 'weekly_capacity_hours numeric' supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c 'normalization_pending boolean' supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c 'uq_teammate_profiles_team_user' supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c 'trg_teammate_profiles_touch_updated_at' supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c "profile_visibility.*default 'team_visible'" supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c "check (profile_visibility in" supabase/migrations/20260512_teammate_profiles.sql` equals `1`.
    - `grep -c 'drop table' supabase/migrations/20260512_teammate_profiles.sql` equals `0` (additive only).
  </acceptance_criteria>
  <done>
    Migration file is committed-ready; ready for `supabase db push` in Task 1.4.
  </done>
</task>

<task type="auto">
  <name>Task 1.3: Install `cmdk@^1.1.1` and commit lockfile</name>
  <read_first>
    - package.json (verify cmdk is NOT present — Pitfall 4)
    - .planning/phases/01-profile-foundation/01-RESEARCH.md (Standard Stack table — exact version)
  </read_first>
  <action>
    Run `npm install cmdk@^1.1.1 --save`. Do not pass `--legacy-peer-deps`. Confirm the install succeeds against the project's React 19.2.4 (cmdk peerDeps: React 18 || 19).

    Do NOT install other packages; do NOT update other dependencies; do NOT run `npm audit fix`; do NOT run `npm update`.

    After install, run `npm run build` to confirm cmdk resolves at build time (catches the Pitfall 4 silent break before any plan imports it).
  </action>
  <verify>
    <automated>grep -c '"cmdk"' package.json &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"cmdk":' package.json` equals `1`.
    - `grep -E '"cmdk":\s*"\^?1\.' package.json` matches (version starts with `1.`).
    - `package-lock.json` lists `node_modules/cmdk` (`grep -c 'node_modules/cmdk' package-lock.json` ≥ `1`).
    - `npm run build` exits `0` (cmdk resolves, no module-not-found).
  </acceptance_criteria>
  <done>
    cmdk available for Plan 03's `ProfileForm`; build passes; lockfile committed.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1.4: [BLOCKING] Push migration to Supabase</name>
  <what-built>
    The additive migration `supabase/migrations/20260512_teammate_profiles.sql` has been written (Task 1.2) and tested in CI. It must be applied to the live Supabase project before any plan tries to read or write `teammate_profiles` rows.
  </what-built>
  <how-to-verify>
    Run the migration push:

    1. Confirm `SUPABASE_ACCESS_TOKEN` is set in the shell environment (`echo ${SUPABASE_ACCESS_TOKEN:+set}` should print `set`). If not, log in via `supabase login` first.
    2. Execute: `supabase db push`
    3. Confirm the migration applies cleanly. Expected output mentions `20260512_teammate_profiles`.
    4. Verify the live table exists by querying:
       - Via Supabase MCP `list_tables` tool, confirm `teammate_profiles` appears.
       - Confirm `teams` table now has a `profile_visibility` column with default `'team_visible'`.
    5. Report any errors before resuming.

    **Why this is `checkpoint:human-action`:** A non-TTY `supabase db push` requires `SUPABASE_ACCESS_TOKEN` and an interactive consent on first-time use; the operator must confirm it executed successfully before downstream plans run. Without this push, Plan 03's `POST /api/teams/[id]/profile` would 500 (table missing) while every local test/build passes — the canonical false-positive verification scenario the orchestrator flagged.
  </how-to-verify>
  <resume-signal>Type "pushed" once the migration is applied AND `list_tables` confirms `teammate_profiles` exists and `teams.profile_visibility` is present. If push fails, paste the error and we will debug before any subsequent plan runs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | DDL touches the production-shared Supabase instance via service-role push. |
| skillVocabulary module → consuming code | Pure const, no input — boundary is type-system only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T (Tampering) | migration file | mitigate | Migration is additive-only; `grep -c 'drop table'` acceptance check enforces no destructive statements. `create table if not exists` + `add column if not exists` make it idempotent. |
| T-01-02 | I (Information Disclosure) | `teammate_profiles` table | mitigate | No RLS added because codebase uses service-role-only access (CLAUDE.md key rules). Future client-side reads MUST go through API routes that call `verifyTeamAccess` first; documented as Plan 03's responsibility. |
| T-01-03 | E (Elevation of Privilege) | `profile_visibility` default | accept | Default `team_visible` is the more-open setting and matches CONTEXT D-18; if an owner wants stricter, they explicitly flip to `owner_only` in team settings (out of Phase 1 scope to build that UI — owner can update via direct API in the interim, documented in SUMMARY). |
| T-01-04 | D (Denial of Service) | `npm install cmdk` | accept | Single direct dep, ~7 KB gzip, no transitive supply-chain bombs flagged by `npm audit`. Verified version 1.1.1 published Nov 2024 (`npm view cmdk version`). |
| T-01-05 | S (Spoofing) | `CANONICAL_SET` consumers | mitigate | `isCanonical()` is the only public membership predicate; Plan 03's normalization filters LLM output through it. No string-comparison shortcuts elsewhere. |
</threat_model>

<verification>
- Migration applied (Task 1.4); `teammate_profiles` queryable via Supabase MCP `list_tables`.
- `npm run build` passes after cmdk install (Task 1.3).
- `npm run test -- --run src/__tests__/skillVocabulary.test.ts` passes (Task 1.1).
- `grep -c "from './skillVocabulary'" src/lib/recgon/skillTagger.ts` ≥ `1` — proves PROFILE-02 single-source-of-truth.
</verification>

<success_criteria>
- `teammate_profiles` exists in live Supabase with the column shape declared in Task 1.2.
- `teams.profile_visibility` column exists with default `'team_visible'`.
- `src/lib/recgon/skillVocabulary.ts` is the single source of canonical vocab; `prompts.ts` and `skillTagger.ts` both import from it.
- `cmdk@^1.1.1` is in `package.json` and `package-lock.json`.
- `npm run build` and `npx tsc --noEmit` both exit `0`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-profile-foundation/01-01-SUMMARY.md` documenting:
- exact final canonical vocab counts (roles, modifiers, total),
- the migration filename + applied-at timestamp,
- the cmdk version installed,
- any deviations from the plan (e.g., if `skillTagger.ts` had no inlined predicate and only the import-comment landed).
</output>
