---
phase: 02-github-skill-inference
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - src/lib/recgon/profileMerge.ts
  - src/lib/recgon/dispatcher.ts
  - src/lib/recgon/inferredSkillsStorage.ts
  - src/lib/recgon/__tests__-helpers/inferredSkillsFixtures.ts
  - src/__tests__/profileMerge.inferred.test.ts
  - src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts
  - src/__tests__/dispatcher.threeSourceBlend.test.ts
autonomous: false
requirements:
  - SKILL-04
  - SKILL-05
  - SKILL-06
tags:
  - profileMerge
  - dispatcher
  - blend-math
  - time-decay
  - integration
  - storage-test

must_haves:
  truths:
    - "profileMerge(teammate, profile, inferred, ema) accepts a non-null InferredSkillMap and produces a weighted blend self=0.5 / inferred=0.3 / ema=0.2"
    - "Rejected inferred rows are EXCLUDED from the blend even if they appear in the map (defense-in-depth — Plan 02 worker already filters at emit time, but profileMerge double-checks per ROADMAP success criterion 3)"
    - "Time decay applies at READ time: each inferred row's score is scaled by exp(-Δt/τ) with τ=90d before blending; never persisted decayed (RESEARCH Pattern 5 contract)"
    - "EMA source (4th profileMerge arg, fitProfile) also passes through applyTimeDecay at read time so a teammate who stopped writing React 6+ months ago sees their React EMA fade — ROADMAP success criterion 4"
    - "Dispatcher's runDispatch and dispatchTask entry points load inferred skills per teammate via listActiveInferredSkillsForTeam and pass to profileMerge — replacing Phase 1's `null` argument"
    - "A teammate who rejects 'python' never has a Python task minted off the inferred signal alone — verified by integration test"
    - "listActiveInferredSkillsForTeam SQL shape verified by a storage-layer test against a real Supabase fixture set (confirmed + inferred + rejected rows) — not just mocked in the dispatcher test"
    - "Phase 1 regression tests stay green: profileMerge with inferred=null still produces identical output (additive widening only)"
  decisions:
    - "D-22: confirmed inferred skills continue to influence the dispatcher after a teammate revokes consent (profileMerge reads them as before); rejected rows stay excluded across re-consent cycles"
    - "D-24: rejected inferred rows are excluded from the 3-source blend (defense-in-depth — worker filters at emit time AND profileMerge double-checks), making the per-skill reject permanent in dispatch decisions"
  artifacts:
    - path: "src/lib/recgon/profileMerge.ts"
      provides: "Three-source blend math (0.5/0.3/0.2) with read-time decay; preserves D-08 owner-view fallback"
      exports: ["profileMerge"]
    - path: "src/lib/recgon/dispatcher.ts"
      provides: "runDispatch + dispatchTask load inferredSkillsByTeammateId once per cron run and pass into profileMerge"
      contains: "listActiveInferredSkillsForTeam"
    - path: "src/__tests__/profileMerge.inferred.test.ts"
      provides: "4 GREEN unit tests covering the blend math + reject filter + decay + null-passthrough regression"
    - path: "src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts"
      provides: "Storage-layer test: inserts confirmed + inferred + rejected rows directly into teammate_inferred_skills and asserts only non-rejected rows are returned by listActiveInferredSkillsForTeam, grouped by teammateId — catches SQL select / join typos that a dispatcher mock cannot"
    - path: "src/__tests__/dispatcher.threeSourceBlend.test.ts"
      provides: "1 integration test: rejected-Python teammate is NOT assigned a Python-tagged task even when self+ema are silent"
    - path: "src/lib/recgon/__tests__-helpers/inferredSkillsFixtures.ts"
      provides: "Shared fixture factories for InferredSkill rows (accepted/rejected/decayed)"
      exports: ["makeAccepted","makeRejected","makeOld"]
  key_links:
    - from: "src/lib/recgon/profileMerge.ts"
      to: "src/lib/recgon/fitLearning.ts"
      via: "import { applyTimeDecay } from './fitLearning'"
      pattern: "applyTimeDecay"
    - from: "src/lib/recgon/profileMerge.ts"
      to: "src/lib/recgon/types.ts"
      via: "import { InferredSkill, InferredSkillMap } from './types'"
      pattern: "InferredSkillMap"
    - from: "src/lib/recgon/dispatcher.ts"
      to: "src/lib/recgon/inferredSkillsStorage.ts"
      via: "listActiveInferredSkillsForTeam(teamId) per cron run"
      pattern: "listActiveInferredSkillsForTeam"
    - from: "src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts"
      to: "src/lib/recgon/inferredSkillsStorage.ts"
      via: "Storage-layer test exercises real SQL shape (rejected filter + teammate grouping) — defense against typos that pass dispatcher unit tests but break in prod"
      pattern: "listActiveInferredSkillsForTeam"
---

<objective>
Close the Phase 2 loop. Plans 01-03 produced and surfaced inferred skills. This plan makes them affect dispatch — the actual ROADMAP success criterion of Phase 2 ("A teammate who rejected Python never sees a Python task minted off the inferred-Python signal alone"). This is the moment a teammate's commit history starts changing assignment outcomes.

Purpose: SKILL-04 (three-source blend), SKILL-05 (rejected exclusion round-trip from UI back to dispatcher math), SKILL-06 (time decay applied to EMA at read time, not just inferred). The change is small (one function signature widening + dispatcher loader injection) but load-bearing — the entire Phase 2 user story collapses if this plan ships wrong.

Output: profileMerge widened to accept InferredSkillMap; dispatcher loads inferred skills + passes through; 3 new test files (profileMerge unit, storage-layer integration, dispatcher 3-source integration). Turns profileMerge.inferred.test.ts from RED to GREEN and adds two regression guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-github-skill-inference/02-CONTEXT.md
@.planning/phases/02-github-skill-inference/02-RESEARCH.md
@.planning/phases/02-github-skill-inference/02-PATTERNS.md
@.planning/phases/02-github-skill-inference/02-01-PLAN.md
@.planning/phases/02-github-skill-inference/02-02-PLAN.md
@.planning/phases/02-github-skill-inference/02-03-PLAN.md
@.planning/phases/01-profile-foundation/01-02-SUMMARY.md
@.planning/phases/01-profile-foundation/01-04-SUMMARY.md

<interfaces>
From Plan 01:
- src/lib/recgon/types.ts → InferredSkill, InferredSkillMap = Map<canonicalTag, InferredSkill>
- src/lib/recgon/inferredSkillsStorage.ts → listActiveInferredSkills(teammateId): Promise<InferredSkill[]>

From Plan 02:
- src/lib/recgon/fitLearning.ts → applyTimeDecay(score, lastSeenAt, now?, tauDays?) → number, DECAY_TAU_DAYS=90

From Phase 1 (existing, MUST NOT break):
- src/lib/recgon/profileMerge.ts → profileMerge(teammate, profile, inferred=null, ema) — Phase 1 P02 already widened the type signature to accept `inferred: unknown | null`; this plan replaces the type with the concrete InferredSkillMap shape and adds the blend math
- src/lib/recgon/dispatcher.ts → existing runDispatch + dispatchTask + backfillLegacySchedules; per Phase 1 D-10 follow-up: dispatchSingleTask receives mergedTeammates (CONTEXT.md note)
- src/__tests__/profileMerge.test.ts → Phase 1 baseline tests; must STAY green
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatcher → DB (storage) | Inferred skills are loaded fresh per cron run; rejected rows filtered at storage layer via listActiveInferredSkills (extra defense-in-depth filter in profileMerge) |
| profileMerge → match.ts | Merged profile output is consumed by ranking math; this plan changes the SHAPE of skills (now blended) but NOT the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-20 | Tampering | profileMerge blend ignores rejection | mitigate | listActiveInferredSkillsForTeam filters at SQL level (where rejected_at is null); profileMerge ALSO checks `if (row.rejectedAt) continue` inside the blend loop — belt + suspenders matches PATTERNS.md ProfileMerge analog defense-in-depth. **SQL shape verified** by Task 2.5 storage test |
| T-02-21 | Information Disclosure | dispatcher loads another teammate's inferred skills | mitigate | listActiveInferredSkillsForTeam scopes by teamId via join; dispatcher iterates over the team's own teammates list (existing Phase 1 path); no cross-team leakage path exists. Storage test inserts two teams' worth of rows and asserts only the queried team's rows return |
| T-02-22 | DoS | dispatcher N+1 query storm | mitigate | Load inferred skills ONCE per cron run in a single batch query (`listActiveInferredSkillsForTeam(teamId)` — new helper in storage), then build the per-teammate map in memory. Mirrors Phase 1 dispatcher loading pattern (single profile query, not per-teammate) per 01-04-SUMMARY.md |
| T-02-23 | Tampering | time-decay sign error (score grows instead of shrinks) | mitigate | applyTimeDecay tests (Plan 02) cover sign; profileMerge.inferred.test.ts adds a regression assertion: Δt=180d → score reduced to ~13.5% of original BEFORE blending |
| T-02-24 | Tampering | SQL typo in listActiveInferredSkillsForTeam (e.g. wrong column in select, missing rejected_at filter) | mitigate | Task 2.5 storage-layer test fixtures three rows (confirmed/inferred/rejected) and asserts the returned shape. A SQL `select` typo or join error would surface here, not in production — dispatcher mock alone cannot catch this |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen profileMerge to consume InferredSkillMap + apply read-time decay + 3-source blend</name>
  <files>
    src/lib/recgon/profileMerge.ts,
    src/lib/recgon/__tests__-helpers/inferredSkillsFixtures.ts,
    src/__tests__/profileMerge.inferred.test.ts
  </files>
  <read_first>
    src/lib/recgon/profileMerge.ts (entire — Phase 1 implementation; widen, do not rewrite),
    src/lib/recgon/fitLearning.ts (applyTimeDecay export from Plan 02),
    src/lib/recgon/types.ts (InferredSkill, InferredSkillMap from Plan 01),
    src/__tests__/profileMerge.test.ts (Phase 1 baseline — read EVERY case to know what regressions to guard against),
    .planning/phases/01-profile-foundation/01-02-SUMMARY.md (D-06..D-08 semantics: field-level fallback, owner-view when profile=null),
    .planning/phases/02-github-skill-inference/02-PATTERNS.md (profileMerge.ts modify section)
  </read_first>
  <behavior>
    profileMerge.ts widened signature: `function profileMerge(teammate: Teammate, profile: TeammateProfile | null, inferred: InferredSkillMap | null, ema: Teammate['fitProfile']): Teammate & { interests: string[] }`. Body changes:

    1. **Imports added (top):** `import { applyTimeDecay, DECAY_TAU_DAYS } from './fitLearning';` and `import type { InferredSkill, InferredSkillMap } from './types';`.

    2. **Constants near top:** `const WEIGHT_SELF = 0.5; const WEIGHT_INFERRED = 0.3; const WEIGHT_EMA = 0.2;` (sum = 1.0; tunable later per ROADMAP "initial weights, tunable").

    3. **D-08 fallback STAYS:** if `profile === null` → existing owner-view branch unchanged. Phase 1 P02 SUMMARY pinned this; do NOT remove.

    4. **Blend logic (added after Phase 1's existing skills assembly, before return):**
       - Build a `Map<canonicalTag, { self: number; inferred: number; ema: number }>`:
         - Seed `self` for every tag in `profile.skills` (or `teammate.skills` if profile=null — Phase 1 fallback) with value 1.0 (self-declared = full weight).
         - For each entry in `inferred` (when non-null):
           - **Defense-in-depth:** `if (row.rejectedAt !== null) continue;`
           - `const decayed = applyTimeDecay(row.score, row.lastSeenAt, now);` (default `now = new Date()`; tests inject deterministic `now`).
           - Add `decayed` to the tag's `inferred` field. (If multiple sources merged to the same tag in storage, the row has one score — Plan 02's runScan already collapsed via max-score on conflict.)
         - For each entry in `ema` (fitProfile.skills if present):
           - `const decayed = applyTimeDecay(emaScore, emaLastSeenAt, now);`
           - Set `ema` field for the tag. (Phase 1 already stores ISO lastSeenAt per skill per 01-02-SUMMARY.)
       - Compute weighted blend per tag: `blended = WEIGHT_SELF * self + WEIGHT_INFERRED * inferred + WEIGHT_EMA * ema`. Skip tags where blended === 0 (no source spoke up).
       - Replace the merged-teammate's `skills` array with the tags where `blended > 0`. Store `blended` in a parallel score map if the consumer (match.ts) reads weighted scores; otherwise the array membership alone is the contract per Phase 1.

    5. **Return shape preserved:** `Teammate & { interests: string[] }` — no consumer needs to change.

    6. **EMA-only fade for old-skill scenario (ROADMAP success #4):** if a teammate has `react` ONLY in `ema.skills` (no self, no inferred) and `ema.lastSeenAt` is 180d old → applyTimeDecay scales the EMA score to ~13.5% of original → `blended ≈ 0.2 × 0.135 × originalEMA` → very small but non-zero. Test asserts this is strictly LESS than the same teammate's recent `typescript` skill (last_seen 7d ago).

    `__tests__-helpers/inferredSkillsFixtures.ts`:
    - `makeAccepted(canonicalTag, opts?: { score?: number; daysAgo?: number; source?: InferredSkillSource })` → InferredSkill with rejectedAt: null, default score 0.8, default daysAgo 30.
    - `makeRejected(canonicalTag, opts?: { score?: number; daysAgo?: number; rejectedDaysAgo?: number })` → InferredSkill with rejectedAt populated.
    - `makeOld(canonicalTag, opts?: { score?: number })` → accepted but lastSeenAt = 180 days ago (for decay tests).

    `profileMerge.inferred.test.ts` (Plan 01 RED test file — flesh out cases listed in must_haves):
    - **Case 1 (regression):** profileMerge(t, p, null, ema) — identical output to Phase 1 baseline. Uses an existing Phase 1 fixture; asserts strict deep-equal.
    - **Case 2 (3-source blend):** self has {typescript}, inferred has {react: 0.8 lastSeen=30d}, ema has {python: 0.5 lastSeen=7d}. Assert merged skills array contains all three. Assert blended scores: typescript ≈ 0.5×1 = 0.5; react ≈ 0.3 × 0.8 × exp(-30/90) ≈ 0.172; python ≈ 0.2 × 0.5 × exp(-7/90) ≈ 0.0925. Use `toBeCloseTo(value, 3)`.
    - **Case 3 (rejected excluded):** inferred map contains BOTH an accepted react row AND a rejected python row (rejectedAt set). Assert merged.skills includes react but NOT python.
    - **Case 4 (EMA fade):** ema has {react: 0.9 lastSeen=180d} and {typescript: 0.9 lastSeen=7d}. Assert blended(react) ≈ 0.2 × 0.9 × exp(-2) ≈ 0.0243 < blended(typescript) ≈ 0.2 × 0.9 × exp(-7/90) ≈ 0.167.
    - All 4 cases pass with `expect(...).toBeCloseTo(value, 3)` precision.
  </behavior>
  <action>profileMerge.ts edit is APPEND-style — add imports + constants + blend block; preserve Phase 1's existing logic verbatim. Fixture file uses faker-free hand-rolled factories (Phase 1 didn't add faker as a dep). Test file already exists as RED from Plan 01 — replace its case bodies with the assertions above. For `now` injection: profileMerge accepts an optional 5th arg `now: Date = new Date()` so tests pin time deterministically; production code never passes the 5th arg. Document this in JSDoc as test-only escape hatch. Use `.toBeCloseTo` not `.toBe` for floating-point comparisons.</action>
  <verify>
    <automated>npm run test -- --run src/__tests__/profileMerge.test.ts src/__tests__/profileMerge.inferred.test.ts 2>&1 | grep -E "(Tests +[0-9]+ passed|failed)" | tail -5</automated>
  </verify>
  <done>Both profileMerge test files GREEN (Phase 1 regression suite + Plan 04 4-case inferred suite). Total green tests = Phase 1 baseline + 4 new. `npx tsc --noEmit` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add listActiveInferredSkillsForTeam helper + wire dispatcher</name>
  <files>
    src/lib/recgon/dispatcher.ts,
    src/lib/recgon/inferredSkillsStorage.ts,
    src/__tests__/dispatcher.threeSourceBlend.test.ts
  </files>
  <read_first>
    src/lib/recgon/dispatcher.ts (entire — find every call site of profileMerge to update, including runDispatch + dispatchTask + dispatchSingleTask + backfillLegacySchedules; CONTEXT.md notes backfillLegacySchedules keeps RAW teammates),
    src/lib/recgon/inferredSkillsStorage.ts (Plan 01 listActiveInferredSkills),
    .planning/phases/01-profile-foundation/01-04-SUMMARY.md (dispatcher wiring pattern: listProfiles → profileMerge → rankMatches),
    src/lib/recgon/profileMerge.ts (Plan 04 Task 1 widened signature),
    src/__tests__/dispatcher.test.ts (Phase 1 dispatcher baseline test — find an analog to extend)
  </read_first>
  <behavior>
    inferredSkillsStorage.ts: add `listActiveInferredSkillsForTeam(teamId: string): Promise<Map<teammateId, InferredSkillMap>>`. Single SQL query joining teammate_inferred_skills on agent_teammates by team. Returns a nested map keyed by teammateId, each value a Map<canonicalTag, InferredSkill>. Excludes rejected rows at SQL level via `.is('rejected_at', null)`. JSDoc cites SKILL-04 and the dispatcher consumer.

    dispatcher.ts:
    - In `runDispatch(teamId)` near the top where `listProfiles(teamId)` is already loaded, add `const inferredByTeammate = await listActiveInferredSkillsForTeam(teamId);` ONCE per cron run.
    - At every existing call site of `profileMerge(t, profile, null, t.fitProfile)`, replace `null` with `inferredByTeammate.get(t.id) ?? null`. Per CONTEXT.md, `dispatchSingleTask` receives `mergedTeammates`; `backfillLegacySchedules` keeps raw `teammates` — leave backfill UNTOUCHED (it doesn't need inferred data).
    - Add log line `logger.info('dispatcher: loaded inferred skills', { teamId, teammateCount: inferredByTeammate.size })` for observability.
    - Do NOT change the order of operations or any other part of dispatcher — single targeted insert + call-site replacements.

    dispatcher.threeSourceBlend.test.ts (NEW integration test):
    - Setup: 1 team, 2 teammates A and B. Both have self-declared `frontend`. Teammate A also has accepted inferred `python` (lastSeen 7d, score 0.9). Teammate B has REJECTED inferred `python` (rejectedAt set).
    - Action: enqueue a Python-tagged task; trigger `runDispatch(teamId)`.
    - Assertion: the task is assigned to A, NOT B. Even though B's self-declared frontend matches similarly, A's inferred-python boost wins, and B's rejection means B has no python in their merged profile at all.
    - Use the existing dispatcher test scaffolding pattern from Phase 1's dispatcher.test.ts (mock Supabase + mock task store). `listActiveInferredSkillsForTeam` is mocked at the dispatcher seam — the real SQL shape is verified separately in Task 2.5.
  </behavior>
  <action>One new storage helper + 2-3 line dispatcher edit + 1 integration test. The dispatcher edit MUST be localized: search for every `profileMerge(` call site (likely 2-3 in dispatcher.ts per Phase 1 SUMMARY) and update each with `inferredByTeammate.get(t.id) ?? null` in the 3rd arg slot. Do NOT touch `backfillLegacySchedules` per CONTEXT.md note. Add the log line ONCE near the top of runDispatch after the load. The integration test mocks `listProfiles` to return both teammates' self profiles, mocks `listActiveInferredSkillsForTeam` to return the python+rejected fixture, and asserts the chosen `assigneeId` is A. Test file may import existing scaffolding from Phase 1's dispatcher.test.ts — DRY across both is acceptable since they share a setup.</action>
  <verify>
    <automated>npm run test -- --run src/__tests__/dispatcher.test.ts src/__tests__/dispatcher.threeSourceBlend.test.ts 2>&1 | grep -E "(Tests +[0-9]+ passed|failed)" | tail -5 && grep -c "listActiveInferredSkillsForTeam" src/lib/recgon/dispatcher.ts | awk '{if($1<1) exit 1}' && grep -c "inferredByTeammate.get" src/lib/recgon/dispatcher.ts | awk '{if($1<1) exit 1}'</automated>
  </verify>
  <done>Both dispatcher tests GREEN. Phase 1 dispatcher.test.ts stays green (regression check). listActiveInferredSkillsForTeam exported + called once in runDispatch. `npx tsc --noEmit` clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.5: Storage-layer test — verify listActiveInferredSkillsForTeam SQL shape against real fixtures</name>
  <files>
    src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts
  </files>
  <read_first>
    src/lib/recgon/inferredSkillsStorage.ts (Task 2 just added listActiveInferredSkillsForTeam — read the actual implementation),
    src/__tests__/profileMerge.test.ts (analog for vitest + supabase service-role test pattern from Phase 1),
    .planning/phases/02-github-skill-inference/02-PATTERNS.md ("Shared Patterns → Service-role Supabase access" + the inferredSkillsStorage.ts section — mirror the storage-test pattern this analog implies),
    src/lib/recgon/profileStorage.ts (analog production storage module — its tests follow the same fixture-insert + read-back pattern if present)
  </read_first>
  <behavior>
    This test exists to catch SQL typos that the dispatcher unit test (Task 2) cannot — that test mocks `listActiveInferredSkillsForTeam` at the seam, so a wrong column name in the `.select(...)` or a missing `.is('rejected_at', null)` would never surface. This storage test exercises the REAL SQL against a real Supabase fixture set.

    Test runtime requirements:
    - Uses the service-role Supabase client (matches `inferredSkillsStorage.ts` import).
    - Runs against the local Supabase stack if available (`supabase start`) OR uses a dedicated test schema namespace; if neither is available in CI, mark the test `describe.skip` with a comment pinning the manual-run command. The default path is local-Supabase happy path — the test file IS the regression contract.

    Fixture setup (`beforeAll` or `beforeEach`):
    - Insert 1 team row (`teams.id = 'team-storage-test'`).
    - Insert 2 agent_teammates rows belonging to that team (`'teammate-A'`, `'teammate-B'`).
    - Insert 4 `teammate_inferred_skills` rows:
      - Row 1: teammate-A, canonical_tag='typescript', score=0.9, source='llm_commit', last_seen_at=now()-7d, rejected_at=NULL, confirmed_at=now() (CONFIRMED + active)
      - Row 2: teammate-A, canonical_tag='react', score=0.7, source='linguist', last_seen_at=now()-30d, rejected_at=NULL, confirmed_at=NULL (INFERRED + active)
      - Row 3: teammate-A, canonical_tag='python', score=0.5, source='llm_commit', last_seen_at=now()-14d, rejected_at=now()-1d, confirmed_at=NULL (REJECTED — MUST be excluded)
      - Row 4: teammate-B, canonical_tag='go', score=0.6, source='llm_commit', last_seen_at=now()-10d, rejected_at=NULL, confirmed_at=NULL (active — different teammate)

    Teardown (`afterAll`): delete those rows + the test team + teammates (cascade should handle most via FK on delete cascade).

    Assertions:
    - **Case 1 — Returns a Map keyed by teammateId:** `const result = await listActiveInferredSkillsForTeam('team-storage-test')`. Assert `result instanceof Map`. Assert `result.size === 2` (teammate-A and teammate-B both have at least one active row).
    - **Case 2 — Rejected row excluded:** `const aMap = result.get('teammate-A')`. Assert `aMap instanceof Map`. Assert `aMap.size === 2` (typescript + react; python is filtered). Assert `aMap.has('python') === false`. Assert `aMap.has('typescript') === true`. Assert `aMap.has('react') === true`.
    - **Case 3 — Confirmed-status row preserved:** `aMap.get('typescript')?.confirmedAt` is NOT null. `aMap.get('react')?.confirmedAt` IS null. (Proves the SQL `select` returns confirmed_at, not just the columns currently used by dispatcher — guards against a future code path needing confirmedAt.)
    - **Case 4 — Score type is number, not string:** `typeof aMap.get('typescript').score === 'number'`. (Catches the Supabase numeric-as-string coercion bug — `mapInferredSkill` should `Number(row.score)` per PATTERNS.md.)
    - **Case 5 — Cross-team isolation:** different team's rows do NOT appear. `result.get('teammate-from-other-team')` is undefined (we only inserted under `team-storage-test`).
    - **Case 6 — Other-teammate's row present:** `result.get('teammate-B')?.has('go') === true`.

    Test runs with `npm run test -- --run src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts`. If local Supabase is not running, tests are `describe.skip`-marked with the message `Skipping: local Supabase not detected. Run \`supabase start\` then re-run.`
  </behavior>
  <action>Mirror the storage-test pattern from PATTERNS.md ("Shared Patterns → Service-role Supabase access" + the analog profileStorage.ts file-header conventions). Use the existing service-role supabase client (`import { supabase } from '@/lib/supabase'`). Wrap fixture insertion in `try/finally` with a `beforeAll`/`afterAll` cleanup so a failed assertion does not leave orphan rows. Use a fixed timestamp helper (`const now = new Date('2026-05-15T00:00:00Z')`) for `last_seen_at` arithmetic so assertions are deterministic. Skip-gate when local Supabase is not running so CI without a stack does not fail (mirrors Phase 1 convention if precedent exists; otherwise document as future work).</action>
  <verify>
    <automated>test -f src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts && grep -c "listActiveInferredSkillsForTeam" src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts | awk '{if($1<2) exit 1}' && grep -c "rejected_at" src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts | awk '{if($1<1) exit 1}' && npm run test -- --run src/__tests__/inferredSkillsStorage.listActiveForTeam.test.ts 2>&1 | grep -E "(Tests +[0-9]+ passed|skipped)" | head -3</automated>
  </verify>
  <done>Test file exists. When local Supabase IS running: all 6 cases pass. When local Supabase is NOT running: file is `describe.skip`-marked with a clear message — the file existing is enough to lock the contract for the next CI run that has a stack. Either way, `npm run test --run` shows no FAILED test for this file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end smoke + Phase 2 acceptance review</name>
  <what-built>
    Phase 2 is complete: Plan 01 created the schema + storage + RED scaffolds, Plan 02 shipped the worker + cron + LLM pipeline, Plan 03 shipped the UI + APIs, Plan 04 closed the loop into dispatcher AND added the storage-layer test that catches SQL typos in `listActiveInferredSkillsForTeam`. After this checkpoint, Phase 2 ships.
  </what-built>
  <how-to-verify>
    1. **Full test suite green:** `npm run test -- --run` reports 0 failures. All 7 Wave-0 test files GREEN. Plan 03's `inferredSkills.api.test.ts` GREEN (4 cases). Plan 04's `profileMerge.inferred.test.ts` + `dispatcher.threeSourceBlend.test.ts` + `inferredSkillsStorage.listActiveForTeam.test.ts` GREEN (or skip-gated on local-Supabase availability with a clear message — not failing). Phase 1 regression suite GREEN.
    2. **Build + lint clean:** `npm run build && npm run lint` both pass.
    3. **End-to-end manual flow** (combines Plan 03 Task 4 with dispatcher verification):
       a. Sign in as a user in a team with at least one project pointing to a real team-connected GitHub repo.
       b. On /teams/[id]/me click "Connect GitHub for skill mining"; complete OAuth (scope upgrade to `repo`); confirm post-consent state.
       c. POST /api/teams/[id]/inferred-skills/scan; force-drain via POST /api/cron/llm-jobs (with bearer if prod-like) or wait for the cron tick.
       d. Refresh /teams/[id]/me; confirm inferred pills land with provenance chips.
       e. Reject one pill (e.g. "python" if it appeared). Confirm optimistic flip + toast + 6s undo window.
       f. Manually enqueue a Python-tagged task in the team's brain (or trigger via existing test path). Trigger runDispatch (cron drain or direct call to `/api/cron/recgon-schedule`).
       g. Inspect assignment audit: the Python task MUST NOT have been assigned to you (the rejected-python user). If another teammate in the team has python as inferred or self, it goes to them; otherwise it stays unassigned.
       h. Visit /teams/[id]/me; the "Likely matched to" rail section reflects the merged-profile blend (frontend pills + any inferred + EMA-decayed history).
    4. **Acceptance against ROADMAP success criteria:**
       - [ ] Criterion 1: Consent gate works — verified in Plan 03 Task 4 + step 3a-b above
       - [ ] Criterion 2: Commits mined in team-connected 6-month window, populated, visible — step 3c-d
       - [ ] Criterion 3: 3-source blend with rejection exclusion — step 3e-g (rejected python not assigned) AND `inferredSkillsStorage.listActiveForTeam.test.ts` Case 2 (SQL excludes rejected rows)
       - [ ] Criterion 4: Time-decay EMA visible — old skills fade in the blended scores (verify by inspecting profileMerge output for a teammate with old EMA data)
       - [ ] Criterion 5: <user_content> wrapping — verified by Plan 02 Task 1's wrapUntrusted tests AND by inspecting a production LLM call's logged user prompt for the delimiters
    5. **Verify no banned-copy regressions** anywhere on /teams/[id]/me.
    6. **Cost sanity:** check Gemini usage dashboard for the manual scan — Standard depth = 1 LLM call per teammate per scan (per VALIDATION.md manual verification).
  </how-to-verify>
  <resume-signal>Type "ship it" when all 6 verification steps pass + ROADMAP criteria checked off, or list specific failures for me to address.</resume-signal>
</task>

</tasks>

<verification>
- All 7 Wave-0 tests GREEN (Plan 01 RED → Plans 02/03/04 GREEN)
- Phase 1 regression suite stays GREEN (profileMerge.test, dispatcher.test, fitLearning.test, etc.)
- `inferredSkillsStorage.listActiveForTeam.test.ts` exists and either passes (local Supabase running) or skip-gates with a clear message
- npm run build + npm run lint clean
- End-to-end manual flow (Task 3) confirmed by human reviewer
- All 5 ROADMAP Phase 2 success criteria checked off
</verification>

<success_criteria>
- [ ] profileMerge.ts signature widened: `inferred: InferredSkillMap | null`
- [ ] Three-source blend math implemented with weights 0.5/0.3/0.2 (constants exported or commented for tuning)
- [ ] Rejected inferred rows excluded from blend (defense-in-depth check inside profileMerge, even though listActiveInferredSkillsForTeam also filters at SQL level)
- [ ] Time decay applied at READ time to BOTH inferred and EMA sources; never persisted decayed
- [ ] Dispatcher loads inferred skills ONCE per runDispatch via listActiveInferredSkillsForTeam (no N+1)
- [ ] Phase 1 regression: profileMerge(t, p, null, ema) identical to Phase 1 output
- [ ] Integration test: rejected-Python teammate is never assigned a Python task off inferred signal alone
- [ ] Storage test: `listActiveInferredSkillsForTeam` SQL shape verified against real fixtures (rejected row excluded, confirmed row preserved, score is number not string, cross-team isolation holds)
- [ ] Human verifies the end-to-end flow per Task 3 checklist
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-skill-inference/02-04-SUMMARY.md` listing: profileMerge.ts widening delta (imports + constants + blend block), dispatcher.ts loader insertion + call-site changes, new storage helper, dispatcher integration test fixture + assertion, the new storage-layer test (case count + skip-gate behavior), the final Wave-0 test green count, and a Phase-2-complete sign-off with all 5 ROADMAP criteria checked off.
</output>
</content>
</invoke>