---
phase: 02-github-skill-inference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260513_inferred_skills.sql
  - src/lib/recgon/inferredSkillsStorage.ts
  - src/lib/recgon/types.ts
  - src/__tests__/mocks/octokit.ts
  - src/__tests__/mocks/llm.ts
  - src/__tests__/githubSkills.consent.test.ts
  - src/__tests__/githubSkills.mining.test.ts
  - src/__tests__/wrapUntrusted.test.ts
  - src/__tests__/inferredSkills.ui.test.tsx
  - src/__tests__/fitLearning.timeDecay.test.ts
  - src/__tests__/githubSkills.empty.test.ts
  - src/__tests__/profileMerge.inferred.test.ts
autonomous: false
requirements:
  - SKILL-01
  - SKILL-03
tags:
  - schema
  - supabase
  - migration
  - test-scaffold
user_setup:
  - service: supabase
    why: "Push schema migration to remote (and reset local stack if running)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens"

must_haves:
  truths:
    - "teammate_inferred_skills table exists with (teammate_id, canonical_tag) unique index"
    - "teammate_profiles has additive columns github_mining_consent_at + last_scan_at, both nullable"
    - "teams has additive column inference_depth not null default 'standard' with cheap/standard/deep check"
    - "Service-role-only access matches CLAUDE.md key rule (no RLS, no UI imports)"
    - "Every Wave-0 vitest file exists with red (failing) placeholders so subsequent plans hit GREEN expectations"
  decisions:
    - "D-21: schema enables inline-on-/teams/[id]/me consent via teammate_profiles.github_mining_consent_at column"
    - "D-22: schema separates consent state (github_mining_consent_at) from skill state (teammate_inferred_skills rows persist after revoke); unique (teammate_id, canonical_tag) index preserves rejected rows across future scans"
    - "D-23: teams.inference_depth column (cheap | standard | deep) with default 'standard' enables per-team depth configuration"
    - "D-24: status column on teammate_inferred_skills records permanent rejection per (teammate_id, canonical_tag); unique index prevents accidental resurfacing"
  artifacts:
    - path: "supabase/migrations/20260513_inferred_skills.sql"
      provides: "Additive DDL for inferred-skill storage + consent + depth columns"
      contains: "create table if not exists teammate_inferred_skills"
    - path: "src/lib/recgon/inferredSkillsStorage.ts"
      provides: "Service-role CRUD: listInferredSkills, listActiveInferredSkills, getInferredSkill, upsertInferredSkill, rejectInferredSkill, undoRejection, listRejectedTags, markBannerReviewed, getTeammateUserId"
      exports: ["listInferredSkills","listActiveInferredSkills","getInferredSkill","upsertInferredSkill","rejectInferredSkill","undoRejection","listRejectedTags","markBannerReviewed","getTeammateUserId","mapInferredSkill"]
    - path: "src/lib/recgon/types.ts"
      provides: "Phase 2 type contracts shared by storage / worker / profileMerge / UI"
      exports: ["InferredSkill","InferredSkillSource","InferredSkillMap","UpsertInferredSkillInput"]
    - path: "src/__tests__/mocks/octokit.ts"
      provides: "Shared Octokit mock seam (commits, paginate iterator, throttle)"
    - path: "src/__tests__/mocks/llm.ts"
      provides: "chatViaChain stub helper reused across Wave-0 test files"
  key_links:
    - from: "src/lib/recgon/inferredSkillsStorage.ts"
      to: "src/lib/supabase.ts"
      via: "service-role import (never client-side)"
      pattern: "from\\s+['\"]\\.\\./supabase['\"]"
    - from: "src/lib/recgon/inferredSkillsStorage.ts"
      to: "supabase/migrations/20260513_inferred_skills.sql"
      via: "row shape mapping snake_case ↔ camelCase"
      pattern: "teammate_inferred_skills"
---

## Phase Goal

**As a** Recgon teammate, **I want to** have my GitHub commits inferred into skills (with consent + per-skill reject) and time-decayed into the dispatcher, **so that** the next cron run matches me to tasks I'm actually shipping today, not what I thought to type.

<objective>
Ship the additive schema + storage layer + Wave-0 test scaffolds that the remaining three plans build on. This plan touches zero user-visible behavior on its own — it exists to make Plans 02–04 each shippable as a thin vertical slice without re-deriving the data shape mid-stream.

Purpose: SKILL-01 needs a consent timestamp column; SKILL-03 needs a `teammate_inferred_skills` table; SKILL-04/05/06 need a queryable map of (canonical_tag → score, lastSeenAt, rejectedAt). Nyquist sampling in 02-VALIDATION.md is RED until the test files exist — this plan creates the failing placeholders so every subsequent task has a measurable target.

Output: Migration, service-role storage module, type contracts, and 7 red vitest files (6 unit + 1 integration scaffold).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-github-skill-inference/02-CONTEXT.md
@.planning/phases/02-github-skill-inference/02-RESEARCH.md
@.planning/phases/02-github-skill-inference/02-PATTERNS.md
@.planning/phases/02-github-skill-inference/02-VALIDATION.md
@CLAUDE.md

<interfaces>
Existing analogs the executor MUST mirror, NOT diverge from:

From supabase/migrations/20260512_teammate_profiles.sql:
- `create extension if not exists pgcrypto;`
- Service-role-only comment header (no RLS)
- `touch_updated_at` trigger pattern
- `teams.id` and `users.id` are TEXT; `agent_teammates.id` is UUID — match exactly

From src/lib/recgon/profileStorage.ts (lines 1-110):
- File-header comment + service-role contract
- Row type + `mapXxx(row)` mapper with snake_case ↔ camelCase
- `maybeSingle` for absent rows; `throw new Error(...)` on supabase error
- `upsert(..., { onConflict: '...' }).select('*').single()`

From src/lib/recgon/types.ts (if present) or src/lib/recgon/profileMerge.ts:
- Pure TypeScript types — no runtime imports from worker modules
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API → service-role Supabase | UI (untrusted user input from teammate's browser) crosses into service-role DB writes only via `/api/teams/[id]/inferred-skills/**` (Plan 02–03) — this plan establishes the data shape that those writes target |
| worker (LLM consumer) → service-role Supabase | github_skill_inference job (Plan 02) writes inferred rows; payload contains userId-not-token (token resolved at runtime per PATTERNS.md) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `teammate_inferred_skills` rows | mitigate | Unique index `uq_tis_teammate_tag` on `(teammate_id, canonical_tag)` prevents duplicate-tag race; `agent_teammates(id)` FK with `on delete cascade` prevents orphaned rows |
| T-02-02 | Information Disclosure | cross-team leakage | mitigate | `team_id` column denormalized + FK; `listInferredSkills(teammateId)` joins on teammate's team membership in worker layer; service-role-only access — UI never imports storage module |
| T-02-03 | Repudiation | rejected skill resurfacing | mitigate | `rejected_at timestamptz null` + partial index `idx_tis_teammate_active where rejected_at is null` — Plan 02's worker MUST query `listRejectedTags()` before emit so a once-rejected `(teammate_id, canonical_tag)` pair never re-suggests, matching ROADMAP success criterion 3 |
| T-02-04 | Spoofing | consent gate bypass | mitigate | `github_mining_consent_at timestamptz null` on `teammate_profiles` — worker (Plan 02) early-returns when null; this plan creates the column + writes the gate test `githubSkills.consent.test.ts` (currently RED — Plan 02 makes it green) |
| T-02-05 | Elevation of Privilege | RLS bypass | accept | No RLS by CLAUDE.md key rule (service-role-only). `inferredSkillsStorage.ts` carries the same file-header comment as `profileStorage.ts` stating UI never imports it — defense lives at the API-route auth layer (Plan 03), not at row level |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Wave-0 test scaffolds + shared mocks (RED)</name>
  <files>
    src/__tests__/mocks/octokit.ts,
    src/__tests__/mocks/llm.ts,
    src/__tests__/githubSkills.consent.test.ts,
    src/__tests__/githubSkills.mining.test.ts,
    src/__tests__/wrapUntrusted.test.ts,
    src/__tests__/inferredSkills.ui.test.tsx,
    src/__tests__/fitLearning.timeDecay.test.ts,
    src/__tests__/githubSkills.empty.test.ts,
    src/__tests__/profileMerge.inferred.test.ts
  </files>
  <read_first>
    src/__tests__/profileMerge.test.ts,
    src/__tests__/fitLearning.test.ts,
    .planning/phases/02-github-skill-inference/02-VALIDATION.md,
    .planning/phases/02-github-skill-inference/02-RESEARCH.md (search for "Wave 0", "Landmines", "octokit mock", "delimiter-strip")
  </read_first>
  <behavior>
    - `mocks/octokit.ts` exports `createMockOctokit({ commitsByRepo: Record<string, MockCommit[]>, paginateImpl?, throttleHooks?: { onRateLimit, onSecondaryRateLimit } })` and a `MockCommit` type matching the shape Octokit returns from `repos.listCommits`.
    - `mocks/llm.ts` exports `stubChatViaChain(response: string | object)` that swaps the runtime implementation per the existing Phase 1 mock pattern in `chatViaChain.test.ts` (search the test tree to find prior precedent before authoring).
    - `githubSkills.consent.test.ts`: 3 cases — (a) no consent timestamp → worker returns `{ skipped: true, reason: 'no_consent' }` and writes ZERO rows; (b) consent timestamp set but `users.githubAccessToken` null → `{ skipped: true, reason: 'no_token' }`; (c) consent + token + 0 repos → `{ skipped: true, reason: 'no_team_repos' }`. All assertions on Supabase mock and worker return value.
    - `githubSkills.mining.test.ts`: 3 cases — (a) `since` parameter passed to Octokit equals "now minus 6 months ± 1 day"; (b) personal repo (no `team_id` row in `projects`) appears in commit input → worker NEVER includes it (filter at `resolveTeamConnectedRepos`); (c) commit count cap = 200 per repo (cost guard) verified by mock returning 500 commits → worker only processes first 200.
    - `wrapUntrusted.test.ts`: 4 cases — (a) plain text wrapped in `<user_content>...</user_content>`; (b) text containing `</user_content>` smuggle attempt → replaced with `⟦⟧` BEFORE wrapping; (c) text containing `<user_content>` opening tag smuggle → replaced; (d) text longer than 2000 chars → truncated to 2000 with ellipsis suffix `…`.
    - `inferredSkills.ui.test.tsx`: 1 integration-shape case — render `<InferredFromGitHub items={[acceptedFixture, rejectedFixture]} onReject onUndoReject />`; assert reject-button click fires `onReject(id)` AND optimistically flips the pill to rejected DOM state. (Tests against a component that Plan 03 will create — file is RED until then.)
    - `fitLearning.timeDecay.test.ts`: 5 cases — (a) Δt=0 → score unchanged; (b) Δt=90d → score × `exp(-1)` ≈ 0.3679 within 1e-4; (c) Δt=180d → score × `exp(-2)` ≈ 0.1353; (d) `lastSeenAt` as ISO string accepted; (e) `now` parameter injectable for deterministic tests (no `Date.now()` call inside).
    - `githubSkills.empty.test.ts`: 1 case — consent + token + repos present + ZERO commits in window → worker writes ZERO new inferred-skill rows AND updates `teammate_profiles.last_scan_at = now()` AND returns `{ skipped: false, written: 0 }`. Banner-suppression behavior (SKILL-06) is downstream: banner queries unreviewed count > 0; zero new rows → zero unreviewed.
    - `profileMerge.inferred.test.ts`: 4 cases — (a) inferred=null → identical output to Phase 1 baseline (regression guard); (b) self+inferred+ema present → weighted blend 0.5/0.3/0.2 per ROADMAP success #3; (c) row with `rejected_at` not null in `inferred` map → EXCLUDED from blend; (d) row with `lastSeenAt` 180 days old → decayed to `score × exp(-2)` before blending (read-time decay, never stored decayed).
    All assertions use `expect(...).toBe(...)`, `.toEqual(...)`, `.toBeCloseTo(value, precision)` or `.toMatchObject(...)`. NO `.toMatchSnapshot()` — Phase 1 convention.
  </behavior>
  <action>Create 9 files. Mocks first (Octokit + LLM stub helpers used by the 7 test files). Each test file uses vitest `describe`/`it`/`expect`, imports the symbol-under-test from its planned production path (which does not exist yet — that is the point), and asserts the behaviors above. The test files MUST fail with "module not found" or "function not exported" — that is the RED state Nyquist requires. Do NOT stub the production modules to make tests pass; that is Plan 02's job. Reference 02-RESEARCH.md §Landmines for the exact prompt-injection corpus to seed wrapUntrusted.test.ts. The component tested by inferredSkills.ui.test.tsx is `<InferredFromGitHub />` exported from `src/app/teams/[id]/me/InferredFromGitHub.tsx` — Plan 03 will create it; this test scaffolds the contract. Use `@testing-library/react` (already in deps per Phase 1 P03 SUMMARY).</action>
  <verify>
    <automated>npm run test -- --run src/__tests__/githubSkills.consent.test.ts src/__tests__/githubSkills.mining.test.ts src/__tests__/wrapUntrusted.test.ts src/__tests__/inferredSkills.ui.test.tsx src/__tests__/fitLearning.timeDecay.test.ts src/__tests__/githubSkills.empty.test.ts src/__tests__/profileMerge.inferred.test.ts 2>&1 | grep -E "(FAIL|RED|Cannot find module|is not a function)" | head -20</automated>
  </verify>
  <done>All 7 vitest files exist; each fails with module-not-found or symbol-not-exported errors (NOT assertion errors — those come once Plans 02–04 implement the production code). `npm run test -- --run` reports ≥ 7 failing tests AND ≥ 0 passing (existing Phase 1 suite stays green).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write additive SQL migration (teammate_inferred_skills + consent columns + inference_depth)</name>
  <files>supabase/migrations/20260513_inferred_skills.sql</files>
  <read_first>
    supabase/migrations/20260512_teammate_profiles.sql (full file — copy header + service-role comment + touch_updated_at pattern),
    supabase/migrations/20260420_agent_teammates.sql or equivalent (find via `ls supabase/migrations/ | grep teammates` then read to confirm `agent_teammates.id` column type is uuid),
    .planning/phases/02-github-skill-inference/02-PATTERNS.md (search "teammate_inferred_skills.sql")
  </read_first>
  <behavior>
    Migration must, when applied to an empty database followed by the prior Phase 1 migrations:
    - Create `teammate_inferred_skills` with columns: `id uuid primary key default gen_random_uuid()`, `teammate_id uuid not null references agent_teammates(id) on delete cascade`, `team_id text not null references teams(id) on delete cascade`, `canonical_tag text not null`, `score numeric(5,4) not null check (score >= 0 and score <= 1)`, `source text not null check (source in ('linguist','extension','llm_commit','llm_import'))`, `last_seen_at timestamptz not null default now()`, `confirmed_at timestamptz null`, `rejected_at timestamptz null`, `user_reviewed_at timestamptz null`, `created_at timestamptz not null default now()`, `updated_at timestamptz not null default now()`.
    - Create unique index `uq_tis_teammate_tag on teammate_inferred_skills(teammate_id, canonical_tag)`.
    - Create partial index `idx_tis_teammate_active on teammate_inferred_skills(teammate_id) where rejected_at is null`.
    - Create function `teammate_inferred_skills_touch_updated_at()` returning trigger + `before update` trigger.
    - `alter table teammate_profiles add column if not exists github_mining_consent_at timestamptz null, add column if not exists last_scan_at timestamptz null;`
    - `alter table teams add column if not exists inference_depth text not null default 'standard' check (inference_depth in ('cheap','standard','deep'));`
    - File MUST be idempotent: every `create table` / `create index` / `create function` uses `if not exists` or `or replace`; every `alter table add column` uses `if not exists`. Running the migration twice in a row MUST succeed.
  </behavior>
  <action>Author migration as a single transactional SQL file. Follow 20260512_teammate_profiles.sql header style verbatim. Reference SKILL-01, SKILL-03, D-21..D-26 in the file-top comment. Type discipline per PATTERNS.md: `teams.id` and `users.id` are TEXT; `agent_teammates.id` is UUID — read the prior migration before writing the FK column types to confirm. Use `gen_random_uuid()` (requires `pgcrypto`, already enabled from prior migration but include `create extension if not exists pgcrypto;` as defense-in-depth). Mirror analog's `touch_updated_at` trigger naming convention. Do NOT add RLS — CLAUDE.md key rule. Do NOT push the migration in this task — Task 4 handles push (blocking gate).</action>
  <verify>
    <automated>test -f supabase/migrations/20260513_inferred_skills.sql && grep -c "create table if not exists teammate_inferred_skills" supabase/migrations/20260513_inferred_skills.sql | grep -q "^1$" && grep -c "uq_tis_teammate_tag" supabase/migrations/20260513_inferred_skills.sql | grep -q "^1$" && grep -c "idx_tis_teammate_active" supabase/migrations/20260513_inferred_skills.sql | grep -q "^1$" && grep -c "github_mining_consent_at" supabase/migrations/20260513_inferred_skills.sql | grep -q "^1$" && grep -c "inference_depth" supabase/migrations/20260513_inferred_skills.sql | grep -q "^1$" && grep -v '^--' supabase/migrations/20260513_inferred_skills.sql | grep -c "rejected_at" | awk '{if($1 < 3) exit 1}'</automated>
  </verify>
  <done>Migration file exists with all required columns + indexes + constraints + idempotent guards. Header comment references SKILL-01, SKILL-03, D-21..D-26.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement inferredSkillsStorage.ts + shared types.ts</name>
  <files>src/lib/recgon/inferredSkillsStorage.ts, src/lib/recgon/types.ts</files>
  <read_first>
    src/lib/recgon/profileStorage.ts (entire 110-line file — direct analog),
    src/lib/supabase.ts (confirm service-role client export name),
    src/lib/recgon/profileMerge.ts (existing types — extend, don't duplicate)
  </read_first>
  <behavior>
    - `src/lib/recgon/types.ts` exports `InferredSkillSource = 'linguist' | 'extension' | 'llm_commit' | 'llm_import'`, `InferredSkill { id, teammateId, teamId, canonicalTag, score, source, lastSeenAt, confirmedAt: string|null, rejectedAt: string|null, userReviewedAt: string|null, createdAt, updatedAt }`, `InferredSkillMap = Map<string, InferredSkill>` (key = canonicalTag, used by `profileMerge` in Plan 04), `UpsertInferredSkillInput { teammateId, teamId, canonicalTag, score, source, lastSeenAt }`.
    - `inferredSkillsStorage.ts` exports the 9 functions listed in must_haves.artifacts. Every function is async, uses the service-role `supabase` client, snake_case ↔ camelCase mapping via `mapInferredSkill(row)`.
    - `listInferredSkills(teammateId)` returns all rows (accepted + rejected) ordered by `last_seen_at desc`.
    - `listActiveInferredSkills(teammateId)` returns only rows where `rejected_at IS NULL` (consumed by `profileMerge` and the rail UI's accepted-pill list).
    - `listRejectedTags(teammateId)` returns `string[]` of canonical_tag values where `rejected_at IS NOT NULL` (consumed by worker to filter emissions — SKILL-03 round-trip).
    - `upsertInferredSkill(input)` uses `onConflict: 'teammate_id,canonical_tag'`. On conflict, updates `score`, `source`, `last_seen_at` but PRESERVES `rejected_at`, `confirmed_at`, `user_reviewed_at` (UPDATE list explicit; rejected stays rejected).
    - `rejectInferredSkill(id)` sets `rejected_at = now()` for the row id. `undoRejection(id)` sets `rejected_at = null` (supports the 6s undo window).
    - `markBannerReviewed(teammateId)` bulk `update ... set user_reviewed_at = now() where teammate_id = ? and user_reviewed_at is null and rejected_at is null`.
    - `getTeammateUserId(teammateId)` joins to `agent_teammates` and returns the `user_id` (TEXT) — used by API-route IDOR check in Plan 03.
    - All errors throw `new Error(\`<fn> failed: ${error.message}\`)` matching profileStorage.ts pattern.
    - Module top-of-file comment MUST state: service-role only, UI never imports, references SKILL-03 + the migration filename, mirrors profileStorage.ts.
  </behavior>
  <action>Mirror profileStorage.ts function-by-function. Use the exact patterns called out in 02-PATTERNS.md §inferredSkillsStorage.ts. Score type narrowing: Supabase returns `numeric` columns as `string | number`; coerce with `Number(row.score)` in `mapInferredSkill`. Date columns return as ISO strings — keep them strings on the camelCase side (matches profileMerge's expectations and avoids `Date` serialization across server/client boundaries). Export the storage functions named, not default. Add JSDoc one-liner above each exported function citing the requirement ID it serves.</action>
  <verify>
    <automated>npx tsc --noEmit src/lib/recgon/types.ts src/lib/recgon/inferredSkillsStorage.ts 2>&1 | grep -v "TS6133" | grep -E "error TS" ; if [ $? -eq 0 ]; then exit 1; else exit 0; fi</automated>
  </verify>
  <done>Both files compile under `tsc --noEmit`. All 9 storage functions exported. `mapInferredSkill` produces camelCase output. `upsertInferredSkill` preserves `rejected_at` on conflict (proven by inspection of the UPDATE list).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Push Supabase migration</name>
  <what-built>
    Task 2 wrote `supabase/migrations/20260513_inferred_skills.sql`. The schema is NOT applied to the remote database until `supabase db push` runs. Build / type checks pass without the push (Supabase types come from config, not the live DB), which would create a false-positive verification state for Plans 02–04.
  </what-built>
  <how-to-verify>
    1. Ensure `SUPABASE_ACCESS_TOKEN` is set in your shell (export from Supabase Dashboard → Account → Access Tokens if missing).
    2. Run `supabase db push` from the repo root.
    3. Confirm the CLI reports `Applying migration 20260513_inferred_skills.sql...` and exits 0.
    4. Verify in Supabase Dashboard → Table Editor → `teammate_inferred_skills` exists with the 12 columns from Task 2's behavior block.
    5. Verify `teammate_profiles` now has `github_mining_consent_at` and `last_scan_at` columns.
    6. Verify `teams` now has `inference_depth` column with default `'standard'`.
    7. **DETERMINISTIC SQL VERIFICATION (REQUIRED — Dashboard inspection alone is NOT sufficient evidence to type "pushed"):**

       a. **No drift between local and remote:** Run `supabase db diff --schema public --linked` from the repo root. Output MUST be empty (exit 0 with no SQL emitted). If anything prints, the remote drifted from the migration set — investigate before resuming.

       b. **Column-count + presence assertions via SQL:** Run the following queries using either `supabase db query --linked --file -` (piping the SQL via heredoc) OR `psql "$SUPABASE_DB_URL" -c '...'` (URL from Supabase Dashboard → Project Settings → Database → Connection string). Every assertion MUST hold; if ANY fails, do NOT type "pushed".

       ```sql
       -- Q1: teammate_inferred_skills must have exactly 12 columns
       select count(*) as col_count from information_schema.columns
         where table_schema = 'public' and table_name = 'teammate_inferred_skills';
       -- EXPECT: col_count = 12

       -- Q2: teammate_profiles must have both new nullable timestamptz columns
       select column_name, data_type, is_nullable from information_schema.columns
         where table_schema = 'public' and table_name = 'teammate_profiles'
           and column_name in ('github_mining_consent_at','last_scan_at')
         order by column_name;
       -- EXPECT: exactly 2 rows; both data_type='timestamp with time zone'; both is_nullable='YES'

       -- Q3: teams must have inference_depth with default 'standard'
       select column_name, column_default, is_nullable from information_schema.columns
         where table_schema = 'public' and table_name = 'teams'
           and column_name = 'inference_depth';
       -- EXPECT: exactly 1 row; column_default contains 'standard'; is_nullable='NO'

       -- Q4: both indexes on teammate_inferred_skills must exist
       select indexname from pg_indexes
         where schemaname = 'public' and tablename = 'teammate_inferred_skills'
           and indexname in ('uq_tis_teammate_tag','idx_tis_teammate_active')
         order by indexname;
       -- EXPECT: exactly 2 rows
       ```

       Resume signal is gated on ALL FOUR queries returning the expected results. If Q1 ≠ 12, Q2 ≠ 2 rows, Q3 ≠ 1 row with default `'standard'`, or Q4 ≠ 2 rows — paste the actual output instead of typing "pushed" so we can diagnose.
  </how-to-verify>
  <resume-signal>Type "pushed" ONLY after `supabase db diff --schema public --linked` is empty AND all four SQL queries above return their expected results. Otherwise paste the CLI error or the actual query output.</resume-signal>
</task>

</tasks>

<verification>
- All 7 Wave-0 test files exist and fail with RED status (module-not-found / symbol-not-exported)
- `npx tsc --noEmit` clean across new files
- Migration applied remotely (Task 4 checkpoint confirmed via `supabase db diff --schema public --linked` empty + four SQL assertions pass)
- Existing test suite (Phase 1) stays green: `npm run test -- --run` shows ≥ Phase-1-count tests passing
</verification>

<success_criteria>
- [ ] `supabase/migrations/20260513_inferred_skills.sql` exists, idempotent, applied
- [ ] `src/lib/recgon/types.ts` exports `InferredSkill`, `InferredSkillMap`, `InferredSkillSource`, `UpsertInferredSkillInput`
- [ ] `src/lib/recgon/inferredSkillsStorage.ts` exports 9 functions (list/listActive/get/upsert/reject/undo/listRejectedTags/markBannerReviewed/getTeammateUserId) + `mapInferredSkill`
- [ ] All 7 Wave-0 vitest files exist and currently FAIL (RED) — exact failure mode = module-not-found for production symbols Plans 02–04 will export
- [ ] Migration push confirmed by checkpoint AND by `supabase db diff --schema public --linked` returning empty AND by the four `information_schema` / `pg_indexes` assertions in Task 4 step 7
- [ ] `npm run lint` clean on new files
- [ ] `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-skill-inference/02-01-SUMMARY.md` listing: migration filename, storage exports, test file names + RED counts, migration push confirmation (including the `supabase db diff` empty result + Q1/Q2/Q3/Q4 outputs), and a note on what Plans 02–04 each turn GREEN.
</output>
</content>
</invoke>