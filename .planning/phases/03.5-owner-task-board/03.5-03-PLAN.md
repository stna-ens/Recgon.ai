---
phase: 03.5
plan: 03
type: execute
wave: 3
depends_on:
  - "03.5-01"
  - "03.5-02"
files_modified:
  - supabase/migrations/20260517_owner_dock_dismissals.sql
  - src/lib/recgon/storage.ts
  - src/app/api/recgon/owner/dock/route.ts
  - src/app/api/recgon/owner/dock/dismiss/route.ts
  - src/components/v2/projects/owner/TriageDock.tsx
  - src/components/v2/projects/owner/TriageDockRow.tsx
  - src/components/v2/projects/owner/AssignTeammatePicker.tsx
  - src/components/v2/projects/owner/ReschedulePicker.tsx
  - src/components/v2/projects/owner/OwnerWorkloadGrid.tsx
  - src/app/projects/OwnerWorkloadBoard.tsx
  - src/__tests__/ownerBoard.dismissApi.test.ts
  - src/__tests__/ownerBoard.dockManualAssign.test.tsx
  - src/__tests__/ownerBoard.dockDismiss.test.tsx
  - src/__tests__/ownerBoard.reschedulePicker.test.tsx
  - src/__tests__/ownerBoard.dnd.reassign.test.tsx
  - src/__tests__/ownerBoard.dnd.reschedule.test.tsx
  - src/__tests__/ownerBoard.dnd.dockToCell.test.tsx
  - src/__tests__/ownerBoard.dnd.rollback.test.tsx
autonomous: true
requirements:
  - SUCCESS-2
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
tags:
  - recgon
  - owner-task-board
  - mvp-vertical-slice
  - inline-actions
  - drag-drop
  - dnd-kit-wired

must_haves:
  truths:
    - "Owner clicks 'assign manually' on a triaged dock row → Radix dropdown of active teammates → confirms → row leaves the dock + chip lands in the grid + toast confirms 'assigned to {name}'."
    - "Owner clicks 'reschedule' menu on any chip in the grid → Radix popover with date picker → picks a date → chip moves to new day in the grid + toast confirms 'moved to {MMM D}'."
    - "Owner clicks the dismiss `×` on a deferred dock row → row disappears from dock for THAT user only (other owners still see it) + chip stays in grid at the deferred date."
    - "Owner drags a chip from teammate A's lane → drops on teammate B's lane (same date) → chip immediately moves (optimistic) → POST `/api/recgon/tasks/[id]/assign` with new teammateId fires → on success: toast 'assigned to {B}'; on failure: chip snaps back + toast 'couldn't move — try again'."
    - "Owner drags a chip from day X → drops on day Y (same teammate) → chip immediately moves → POST `/api/teams/{teamId}/tasks/{taskId}/schedule` (REUSED — confirmed owner-gated) fires → on success: toast 'moved to {MMM D}'; on failure: chip snaps back."
    - "Owner drags a dock row → drops on a teammate × day cell → BOTH assign + reschedule fire in one operation → on success: row leaves dock AND chip lands in cell."
    - "Keyboard drag works: Tab to chip, Space to grab, Arrow to move, Enter/Space to drop, Esc to cancel — same API calls as mouse drag, with screen-reader announcements via dnd-kit's accessibility config."
    - "Member or viewer constructing the URL `POST /api/recgon/owner/dock/dismiss` is rejected with 403."
    - "In-flight (`accepted` or `in_progress`) chips trigger a Radix `ConfirmDialog` BEFORE reassign — copy per UI-SPEC §7 destructive section."
  artifacts:
    - path: "supabase/migrations/20260517_owner_dock_dismissals.sql"
      provides: "NEW table `owner_dock_dismissals(user_id UUID, task_id UUID, dismissed_at TIMESTAMPTZ, PRIMARY KEY (user_id, task_id))` with cascade deletes on both FKs + index on user_id."
    - path: "src/lib/recgon/storage.ts"
      provides: "NEW helpers: `dismissDockItem(userId, taskId)`, `listDismissedTaskIds(userId, teamId)`. UPDATE: `listOwnerDockTasks(teamId)` extended to optionally accept `userId` and filter out dismissed task ids. NOTE: no separate `ownerRescheduleTask` helper — both the day-drag path AND the dock-row 'reschedule menu' POST path REUSE the same `/api/teams/[id]/tasks/[taskId]/schedule` endpoint (verified shipped + owner-gated)."
    - path: "src/app/api/recgon/owner/dock/dismiss/route.ts"
      provides: "Owner-only `POST /api/recgon/owner/dock/dismiss` — body: `{ taskId: string }`. Inserts a row in `owner_dock_dismissals` for `(session.user.id, taskId)`. Idempotent on PK conflict (use UPSERT or ON CONFLICT DO NOTHING)."
    - path: "src/app/api/recgon/owner/dock/route.ts (UPDATED)"
      provides: "GET extended to read the current user's dismissed task ids and exclude them from the `deferred` array (triaged rows are never dismissable per D-17)."
    - path: "src/components/v2/projects/owner/AssignTeammatePicker.tsx"
      provides: "Radix `DropdownMenu` (or wrapper around existing `src/components/Select.tsx`) listing active teammates, click → POST `/api/recgon/tasks/[id]/assign` with `{ teammateId }` → optimistic remove from dock + addToast on success."
    - path: "src/components/v2/projects/owner/ReschedulePicker.tsx"
      provides: "Radix `Popover` containing `<input type='date'>` + 'apply'/'cancel' buttons. On apply: POST to existing `/api/teams/{teamId}/tasks/{taskId}/schedule` with `{ teammateId: task.assigneeId, scheduledDate, scheduledUntilDate: scheduledDate }` (mirroring WeekCalendar's existing call shape) — optimistic chip move + toast."
    - path: "src/components/v2/projects/owner/OwnerWorkloadGrid.tsx (UPDATED)"
      provides: "Wraps each chip in dnd-kit `useDraggable` and each cell in `useDroppable`. Wraps each lane row in a secondary `useDroppable` so a chip dropped on the lane label gets the lane's teammate id with the chip's existing date. ConfirmDialog interception for in-flight chips."
    - path: "src/app/projects/OwnerWorkloadBoard.tsx (UPDATED)"
      provides: "Replaces the no-op `onDragEnd` from 03.5-01 with a real handler that decodes drop-target-id strings ('{teammateId}__{dateISO}' for cells, '{teammateId}__lane' for lane drops, 'dock' for dock drops in reverse), runs optimistic-update + API call + rollback. Add `optimisticOverrides: Record<string, Partial<AgentTask>>` state mirroring `src/app/tasks/page.tsx` pattern."
  key_links:
    - from: "src/components/v2/projects/owner/AssignTeammatePicker.tsx"
      to: "src/app/api/recgon/tasks/[id]/assign/route.ts"
      via: "fetch POST { teammateId }"
      pattern: "fetch.*api/recgon/tasks.*assign"
    - from: "src/components/v2/projects/owner/ReschedulePicker.tsx"
      to: "src/app/api/teams/[id]/tasks/[taskId]/schedule/route.ts (EXISTING, owner-gated)"
      via: "fetch POST { teammateId, scheduledDate, scheduledUntilDate }"
      pattern: "fetch.*api/teams.*tasks.*schedule"
    - from: "src/components/v2/projects/owner/TriageDockRow.tsx (deferred dismiss button)"
      to: "src/app/api/recgon/owner/dock/dismiss/route.ts"
      via: "fetch POST { taskId }"
      pattern: "fetch.*api/recgon/owner/dock/dismiss"
    - from: "src/app/projects/OwnerWorkloadBoard.tsx onDragEnd"
      to: "src/app/api/recgon/tasks/[id]/assign/route.ts AND src/app/api/teams/[id]/tasks/[taskId]/schedule/route.ts"
      via: "decoded drop-target → optimistic state → fetch → revert on error"
      pattern: "setOptimistic.*fetch.*assign\\|schedule"
    - from: "src/components/v2/projects/owner/OwnerWorkloadGrid.tsx (chip wrapper)"
      to: "@dnd-kit/core useDraggable + useDroppable"
      via: "JSX wrapper with attributes/listeners spread"
      pattern: "useDraggable\\|useDroppable"
    - from: "supabase/migrations/20260517_owner_dock_dismissals.sql"
      to: "users(id) AND agent_tasks(id)"
      via: "FK cascade delete"
      pattern: "REFERENCES.*ON DELETE CASCADE"

execution_note:
  size_warning: "This plan is the densest in the phase (3 tasks, ~18 files including 8 test files, spans migration → storage → API → 4 components → DnD wiring + ConfirmDialog). If the executor hits context limits during Task 3 (DnD wiring), wave-split is allowed: 03a = migration + dismiss endpoint + AssignTeammatePicker + ReschedulePicker (Tasks 1+2); 03b = DnD + ConfirmDialog + optimistic rollback (Task 3). Both halves remain vertical-slice valid (each ships user-visible capability)."

threat_model:
  trust_boundaries:
    - "Browser → /api/recgon/owner/dock/dismiss (untrusted taskId crosses)"
    - "Browser → /api/recgon/tasks/[id]/assign (existing — owner-gated)"
    - "Browser → /api/teams/[id]/tasks/[taskId]/schedule (existing — owner-gated, REUSED)"
    - "Drag-drop: dnd-kit drop-target-id encoded strings ('{teammateId}__{dateISO}', etc.) decoded client-side before API call"
  threats:
    - id: T-3.5-01
      category: Authorization (server-side defense in depth)
      component: "src/app/api/recgon/owner/dock/dismiss/route.ts AND OwnerWorkloadBoard.onDragEnd → existing endpoints"
      disposition: mitigate
      mitigation: "Dismiss endpoint mirrors `/assign` owner-gate (session + verifyTeamAccess via task.teamId → role !== 'owner' → 403). Drag-driven `/assign` calls use the SHIPPED endpoint which already gates. Drag-driven `/schedule` calls use the EXISTING owner-gated endpoint at lines 24-26 of `src/app/api/teams/[id]/tasks/[taskId]/schedule/route.ts` (`if (role !== 'owner') return 403`). Verified during PATTERNS gathering."
    - id: T-3.5-02
      category: Authorization (mutation by non-owner)
      component: "All three mutation endpoints invoked by drag-drop or inline actions"
      disposition: mitigate
      mitigation: "Dismiss endpoint: owner-only. Assign endpoint: shipped owner-only (Plan 03-07). Schedule endpoint: existing owner-only. Member/viewer who somehow constructs the request gets 403. Tested in `ownerBoard.dismissApi.test.ts`."
    - id: T-3.5-03
      category: Information Disclosure
      component: "Drag-drop UI fetches per-chip detail via existing /api/recgon/tasks/[id]"
      disposition: mitigate
      mitigation: "Carry-over from 03.5-02. The drag handler does NOT need per-chip detail; it operates on the chip's existing data. TaskDetailPanel (chip click, separate from drag) continues to use /api/recgon/tasks/[id] which applies the privacy filter."
    - id: T-3.5-04
      category: Concurrency (multi-owner + cron race)
      component: "OwnerWorkloadBoard mutation paths"
      disposition: accept
      mitigation: "Multi-owner concurrent drag = last-write-wins per CONTEXT § Deferred Ideas. Owner-vs-cron mid-drag race = same. Document in OwnerWorkloadBoard.tsx file header (carry-over from 03.5-01) AND in the new reschedule endpoint code comment (Pitfall 4). v1 acceptance."
---

<objective>
Make the owner board ACTIONABLE: every visible task can be acted on directly without leaving the page. Inline picker on dock rows fires manual-assign. Inline date picker on chips fires reschedule. Dismiss `×` on deferred rows hides them per-user. Drag-and-drop reassigns (chip → lane), reschedules (chip → day), or BOTH (dock → cell). Optimistic UI commits immediately, rolls back on API failure with a toast. In-flight chips trigger a confirm dialog before reassign.

Purpose: Deliver Success Criteria 2 fully (act on triage + deferred items inline) + the D-18 drag-drop hard requirement. This plan is the user's primary correction loop — it's where the "right task → right person → right time" promise becomes the owner's lever.

Output:
  - NEW migration `20260517_owner_dock_dismissals.sql` (table + cascades + index).
  - NEW storage helpers: `dismissDockItem`, `listDismissedTaskIds`. UPDATED `listOwnerDockTasks` to filter dismissed.
  - NEW `POST /api/recgon/owner/dock/dismiss` (owner-only).
  - UPDATED `GET /api/recgon/owner/dock` to filter dismissed for the current viewer.
  - NEW `AssignTeammatePicker.tsx` (dropdown picker for dock rows).
  - NEW `ReschedulePicker.tsx` (date popover for chip context menu).
  - UPDATED `OwnerWorkloadGrid.tsx` — wraps chips in `useDraggable`, cells in `useDroppable`, lane labels in secondary `useDroppable`. Reschedule menu hook on chip click overflow.
  - UPDATED `OwnerWorkloadBoard.tsx` — real `onDragEnd` handler: decode → optimistic → fetch → rollback. Optimistic state. ConfirmDialog interception for in-flight chips.
  - 8 new test files: dismiss API contract, dock manual-assign integration, dock dismiss integration, reschedule picker integration, drag-reassign, drag-reschedule, drag-dock-to-cell, drag-rollback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-owner-task-board/03.5-CONTEXT.md
@.planning/phases/03.5-owner-task-board/03.5-RESEARCH.md
@.planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md
@.planning/phases/03.5-owner-task-board/03.5-PATTERNS.md
@.planning/phases/03.5-owner-task-board/03.5-VALIDATION.md
@.planning/phases/03.5-owner-task-board/03.5-01-SUMMARY.md
@.planning/phases/03.5-owner-task-board/03.5-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. -->

EXISTING — REUSE for chip → day drag (PATTERNS.md confirms owner-gated):
  POST /api/teams/{teamId}/tasks/{taskId}/schedule
  Body: { teammateId: string; scheduledDate: string (YYYY-MM-DD); scheduledUntilDate: string (YYYY-MM-DD) }
  Auth: session + verifyTeamAccess(teamId, userId) → role === 'owner' or 403
  See: src/app/api/teams/[id]/tasks/[taskId]/schedule/route.ts lines 24-26 (owner gate confirmed)
  Existing caller: src/components/v2/calendar/WeekCalendar.tsx handleTaskDrop (lines ~193-210) — read this and reuse the call shape EXACTLY.

EXISTING — REUSE for chip → teammate drag and dock manual-assign (Plan 03-07):
  POST /api/recgon/tasks/{taskId}/assign
  Body: { teammateId: string }
  Auth: owner-only (lines 49-66 of src/app/api/recgon/tasks/[id]/assign/route.ts)
  Side effects: clearTriageNote(taskId), assignTask with synthetic { kind: 'manual_assignment' } AssignmentReasoning, logEvent('manually_assigned')

NEW this plan:
  POST /api/recgon/owner/dock/dismiss
  Body: { taskId: string }
  Auth: session + verifyTeamAccess(task.teamId, userId) → role === 'owner' or 403
  Side effects: INSERT INTO owner_dock_dismissals (user_id, task_id, dismissed_at) ON CONFLICT DO NOTHING
  Returns: { ok: true } or 4xx

dnd-kit drop-target-id encoding (RESEARCH § Pattern 1):
  - Cell: `{teammateId}__{dateISO}` (e.g., `tm-abc__2026-05-17`)
  - Lane (dropped on label/row, not specific day): `{teammateId}__lane`  (uses chip's existing scheduledDate)
  - Dock (chip dragged back to dock — for completeness, treat as no-op or unschedule + clear assignment if planner wants): `dock` — RECOMMENDATION: ignore for v1 (not in CONTEXT D-18 explicit list); chip → cell and dock → cell are the two locked directions.

dnd-kit useDraggable / useDroppable:
  import { useDraggable, useDroppable } from '@dnd-kit/core';
  import { CSS } from '@dnd-kit/utilities';

  const { attributes, listeners, setNodeRef, transform, isDragging } = useDraggable({
    id: task.id,
    data: { kind: 'task' | 'dock-row', task },
  });
  const style = { transform: CSS.Transform.toString(transform), opacity: isDragging ? 0.5 : 1 };
  // Spread attributes + listeners onto an outer wrapper around <EventChip /> (do NOT modify EventChip internals).

  const { isOver, setNodeRef } = useDroppable({
    id: `${teammateId}__${dateISO}`,
    data: { kind: 'cell', teammateId, dateISO },
  });

Optimistic state pattern (canonical Recgon — src/app/tasks/page.tsx lines 134-217):
  const [optimisticOverrides, setOptimisticOverrides] = useState<Record<string, Partial<AgentTask>>>({});
  // Apply override on top of real task before render: const renderTask = { ...realTask, ...optimisticOverrides[realTask.id] };
  // Clear stale entries via useEffect when real state catches up.

Existing components to reuse:
  - src/components/Select.tsx — Radix DropdownMenu wrapper (use for teammate picker)
  - src/components/ConfirmDialog.tsx — Radix Dialog wrapper (use for in-flight reassign confirm)
  - src/components/Toast.tsx + useToast() — for action feedback

Migration file location convention:
  supabase/migrations/{YYYYMMDD}_{descriptive_name}.sql
  Use 20260517_owner_dock_dismissals.sql (today is 2026-05-16; file dated next-day to avoid collision)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration + dismiss endpoint + storage helpers + dismiss API contract tests</name>
  <files>supabase/migrations/20260517_owner_dock_dismissals.sql, src/lib/recgon/storage.ts, src/app/api/recgon/owner/dock/dismiss/route.ts, src/app/api/recgon/owner/dock/route.ts, src/__tests__/ownerBoard.dismissApi.test.ts</files>
  <read_first>
    - supabase/migrations/ (list directory and read 1-2 recent migrations to match the existing file style — column types, FK syntax, index syntax)
    - src/app/api/recgon/tasks/[id]/assign/route.ts (auth + body validation pattern, 158 lines)
    - src/lib/recgon/storage.ts (existing helpers around lines 638-700 — `deferTaskScheduledDate`, `clearTriageNote`; the `listOwnerDockTasks` from 03.5-02)
    - src/__tests__/tasks.manualAssign.api.test.ts (vi.mock layout)
    - src/__tests__/ownerBoard/setup.ts
    - .planning/phases/03.5-owner-task-board/03.5-PATTERNS.md (lines 332-346 — migration analog; lines 290-329 — storage append pattern)
  </read_first>
  <behavior>
    - Test 1 (auth): Unauthenticated POST → 401.
    - Test 2 (auth): Authenticated as member → 403.
    - Test 3 (auth): Authenticated as viewer → 403.
    - Test 4 (auth): Authenticated as owner of a different team than the task → 403.
    - Test 5 (validation): Missing taskId in body → 400.
    - Test 6 (validation): taskId references non-existent task → 404.
    - Test 7 (success): Owner dismisses a deferred task → 200 + INSERT into owner_dock_dismissals (verified by mock or actual DB call assertion).
    - Test 8 (idempotent): Owner dismisses the same task twice → both calls return 200; ON CONFLICT DO NOTHING means the second is a no-op.
    - Test 9 (filter — listOwnerDockTasks excludes dismissed): Direct unit test on the storage helper. Set up a mock returning 2 deferred tasks (taskA, taskB). For user U1, taskA is in `owner_dock_dismissals`. `listOwnerDockTasks(teamId, U1)` returns deferred=[taskB]. For user U2 (no dismissals), returns deferred=[taskA, taskB].
    - Test 10 (triaged not dismissable): Owner POSTs dismiss for a triaged task (triage_note IS NOT NULL) → 400 with error message "only deferred tasks can be dismissed" (per D-17).
  </behavior>
  <action>
    Create `supabase/migrations/20260517_owner_dock_dismissals.sql`:
      ```
      CREATE TABLE IF NOT EXISTS owner_dock_dismissals (
        user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        task_id UUID NOT NULL REFERENCES agent_tasks(id) ON DELETE CASCADE,
        dismissed_at TIMESTAMPTZ NOT NULL DEFAULT now(),
        PRIMARY KEY (user_id, task_id)
      );
      CREATE INDEX IF NOT EXISTS owner_dock_dismissals_user_idx ON owner_dock_dismissals(user_id);
      ```
    Match the existing migration file style (read 1-2 recent migrations first to confirm types are compatible — agent_tasks.id may be `text` not `uuid`; verify and match). If agent_tasks.id is text, change `task_id UUID` to `task_id TEXT`. If users.id is text, change `user_id UUID` to `user_id TEXT`. Mirror the actual column types from existing FK references.

    Append to `src/lib/recgon/storage.ts`:
      - `dismissDockItem(userId: string, taskId: string): Promise<void>` — INSERT into owner_dock_dismissals; ON CONFLICT DO NOTHING. Use the supabase client `.from('owner_dock_dismissals').upsert({ user_id: userId, task_id: taskId, dismissed_at: new Date().toISOString() }, { onConflict: 'user_id,task_id', ignoreDuplicates: true })`.
      - `listDismissedTaskIds(userId: string, teamId: string): Promise<string[]>` — SELECT task_id FROM owner_dock_dismissals JOIN agent_tasks ON task_id = agent_tasks.id WHERE user_id = $1 AND agent_tasks.team_id = $2.
      - UPDATE `listOwnerDockTasks(teamId: string, viewerUserId?: string): Promise<...>` — accept optional viewerUserId. If provided, fetch dismissed ids via listDismissedTaskIds, then filter the deferred array to exclude those ids. Triaged array is never filtered (per D-17 — triaged items can't be dismissed).

    Create `src/app/api/recgon/owner/dock/dismiss/route.ts`:
      - Imports: `NextResponse`, `auth`, `getTask`, `dismissDockItem` from storage, `verifyTeamAccess`.
      - `export async function POST(request: Request)`.
      - Body parse: `{ taskId: string }` — validate taskId is non-empty string; 400 otherwise.
      - Session check → 401 if absent.
      - getTask(taskId) → 404 if absent.
      - Triaged guard: if `task.triageNote !== null && task.scheduledDate is null/past` → 400 with error "only deferred tasks can be dismissed" (mirror the D-17 rule). Deferred = `triageNote === null && scheduledDate > today`. The simplest check: if `task.triageNote !== null` → 400.
      - verifyTeamAccess(task.teamId, session.user.id) → 403 if role !== 'owner'.
      - `await dismissDockItem(session.user.id, taskId)`.
      - Return `NextResponse.json({ ok: true })`.

    UPDATE `src/app/api/recgon/owner/dock/route.ts` (from 03.5-02):
      - Pass `session.user.id` as second arg to `listOwnerDockTasks(teamId, session.user.id)` so dismissed deferred items are filtered for THIS owner.

    Create `src/__tests__/ownerBoard.dismissApi.test.ts` covering Tests 1-8 + Test 10 (Test 9 is in this same file as a direct storage helper test). Use vi.mock layout from `tasks.manualAssign.api.test.ts`. For Test 9, mock the supabase client at the storage layer to return the dismissal rows + assert filtering.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.dismissApi.test.ts</automated>
  </verify>
  <done>
    - Migration file created with correct column types matching existing schema.
    - `POST /api/recgon/owner/dock/dismiss` returns 401/403/400/404/200 per Tests 1-7, idempotent on duplicate (Test 8), 400 for triaged (Test 10).
    - `listOwnerDockTasks(teamId, userId)` filters out dismissed deferred items for that user only (Test 9).
    - All 10 tests pass; full suite no NEW regressions.
    - User must apply the migration to live Supabase before this lands in production cron — note in summary.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire inline actions on dock + chip context menu (manual-assign picker, dismiss button, reschedule picker)</name>
  <files>src/components/v2/projects/owner/AssignTeammatePicker.tsx, src/components/v2/projects/owner/ReschedulePicker.tsx, src/components/v2/projects/owner/TriageDock.tsx, src/components/v2/projects/owner/TriageDockRow.tsx, src/components/v2/projects/owner/OwnerWorkloadGrid.tsx, src/app/projects/OwnerWorkloadBoard.tsx, src/__tests__/ownerBoard.dockManualAssign.test.tsx, src/__tests__/ownerBoard.dockDismiss.test.tsx, src/__tests__/ownerBoard.reschedulePicker.test.tsx</files>
  <read_first>
    - src/components/v2/calendar/WeekCalendar.tsx handleTaskDrop (lines ~193-210) — exact call shape for /api/teams/{teamId}/tasks/{taskId}/schedule (REUSE this verbatim)
    - src/app/api/recgon/tasks/[id]/assign/route.ts (POST contract: { teammateId })
    - src/components/Select.tsx (Radix DropdownMenu wrapper — use for AssignTeammatePicker)
    - src/components/ConfirmDialog.tsx (Radix Dialog wrapper)
    - src/components/Toast.tsx (useToast hook)
    - src/components/v2/calendar/EventChip.tsx (chip context menu hook — does the chip already expose a hover/right-click menu, or does the click handler need to be split between "open detail" and "open chip menu"?)
    - .planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md (§4 ReschedulePicker, §6 destructive actions, §7 toasts)
    - src/__tests__/ownerBoard/setup.ts
    - 03.5-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1 (manual-assign picker — render): Render <AssignTeammatePicker teammates={[t1, t2, t3]} onAssign={spy} /> (closed state). Click trigger → dropdown opens with 3 teammate items. Click t2 → spy called with t2.id.
    - Test 2 (manual-assign — dock integration): Render <TriageDock /> with 1 triaged task + mock fetch to /api/recgon/tasks/.../assign returning 200. Click "assign manually" on the row → picker opens → click first teammate → fetch fires with correct body → on success: row leaves dock (count drops to 0; collapses to "TRIAGE · CLEAR"); toast "assigned to {name}".
    - Test 3 (manual-assign — failure): Same as 2 but mock fetch returns 500 → row stays in dock; toast "couldn't assign — try again" (warning).
    - Test 4 (dismiss — render): Render <TriageDockRow isDeferred={true} /> → dismiss `×` button visible; with `isDeferred={false}` → button absent.
    - Test 5 (dismiss — integration): Render <TriageDock /> with 1 deferred task + mock POST /api/recgon/owner/dock/dismiss returning 200. Click `×` → optimistic remove from dock; fetch fires; on success: row stays absent. (Chip stays in calendar — verify via mock state.)
    - Test 6 (dismiss — failure): Mock POST returns 500 → row reappears (rollback); toast "couldn't dismiss — try again".
    - Test 7 (reschedule picker — render): Render <ReschedulePicker open={true} taskId="t1" currentDate="2026-05-17" onReschedule={spy} /> → popover with date input pre-populated to currentDate + apply / cancel buttons. Pick a new date, click apply → spy called with new date.
    - Test 8 (reschedule picker — integration via chip menu): In the workload grid, simulate clicking the chip's "reschedule" menu option (planner-decided UI: e.g., a small kebab on hover, or right-click, or an entry in TaskDetailPanel — pick whichever is least intrusive). Picker opens. Pick a date. Mock fetch to existing /api/teams/{teamId}/tasks/{taskId}/schedule returning 200 → chip moves to new day in grid; toast "moved to {MMM D}".
    - Test 9 (reschedule — failure): Mock fetch returns 500 → chip snaps back to original day; toast "couldn't move".
    - Test 10 (reschedule — body shape): Assert the POST body matches what WeekCalendar.handleTaskDrop sends EXACTLY: `{ teammateId: task.assigneeId, scheduledDate: newDate, scheduledUntilDate: newDate }` (or whatever the existing endpoint expects). If task is unassigned (e.g., manual-assign + reschedule combined later), the picker is gated to only-assigned tasks for now; if the planner wants the dock-assign + reschedule combo to use this picker, that's a 03.5-03 stretch.
  </behavior>
  <action>
    Build `src/components/v2/projects/owner/AssignTeammatePicker.tsx`:
      - Props: `{ teammates: TeammateWithStats[]; onAssign: (teammateId: string) => Promise<void> | void; trigger?: React.ReactNode; }`
      - Use Radix DropdownMenu (via existing `src/components/Select.tsx` if its API fits, else Radix DropdownMenu primitive directly).
      - Trigger button (default if not provided): "assign manually" pill (mono 10px UPPER, `cal-panel-btn` style scaled).
      - Items: list teammates by displayName + title. ARIA: each item has `aria-label="assign to {name}"`.
      - On select: call onAssign(teammateId). Component handles open state + close on select; loading state on the trigger while onAssign is pending (button text "working").
      - Reuse `src/components/Select.tsx` if it accepts arbitrary item rendering; otherwise build a thin Radix DropdownMenu wrapper.

    Build `src/components/v2/projects/owner/ReschedulePicker.tsx`:
      - Props: `{ open: boolean; onOpenChange: (open: boolean) => void; taskId: string; currentDate: string | null; onReschedule: (newDateISO: string) => Promise<void> | void; }`
      - Radix Popover (already installed). Inside: an `<input type="date">` defaulted to currentDate, "apply" + "cancel" buttons. Apply → call onReschedule(value); cancel → close.
      - Loading state on apply button while onReschedule is pending.

    UPDATE `src/components/v2/projects/owner/TriageDockRow.tsx` (from 03.5-02):
      - For triaged rows: render `<AssignTeammatePicker teammates={teammates} onAssign={async (teammateId) => onAssignClick?.(taskId, teammateId)} />` instead of the placeholder button.
      - For deferred rows: wire the `×` dismiss button to call `onDismissClick?.(taskId)`.

    UPDATE `src/components/v2/projects/owner/TriageDock.tsx` (from 03.5-02):
      - Accept additional prop: `teammates: TeammateWithStats[]` (passed down to TriageDockRow → AssignTeammatePicker).
      - Pass through `onAssignClick(taskId, teammateId)` (note new signature with teammateId) and `onDismissClick(taskId)` to each row.

    UPDATE `src/components/v2/projects/owner/OwnerWorkloadGrid.tsx` (from 03.5-02):
      - Add a chip context menu trigger. Cleanest path: pass an `onRescheduleRequest(taskId)` callback up to OwnerWorkloadBoard. The trigger UI: keep chip primary onClick = open TaskDetailPanel; add a small "reschedule" menu option to the chip. SIMPLEST: extend TaskDetailPanel's existing actions with a "reschedule" button (cleanest — panel is already the chip-detail surface). PLANNER PICK: if TaskDetailPanel modification is too invasive for this plan, use a small kebab `⋯` menu on chip hover (Radix DropdownMenu); otherwise prefer the panel-button path.
      - Document the choice in OwnerWorkloadBoard.tsx file header.

    UPDATE `src/app/projects/OwnerWorkloadBoard.tsx` (from 03.5-02):
      - Add `optimisticOverrides: Record<string, Partial<AgentTask>>` state mirroring `src/app/tasks/page.tsx:134-217`.
      - Wire `onAssignClick(taskId, teammateId)` from TriageDock:
         * Optimistic: `setOptimisticOverrides(prev => ({ ...prev, [taskId]: { assigneeId: teammateId, status: 'assigned', triageNote: null } }))`. The dock derived state reads from `tasks` filtered by triage/deferred predicates → optimistic override moves the task out of those predicates → row leaves dock.
         * fetch POST `/api/recgon/tasks/${taskId}/assign` body `{ teammateId }`.
         * On 2xx: addToast("assigned to {name}", 'success'); refresh dock + window-tasks fetches.
         * On non-2xx or throw: clear the optimistic override; addToast("couldn't assign — try again", 'error').
      - Wire `onDismissClick(taskId)` from TriageDock:
         * Optimistic: track dismissed ids in a `dismissedOptimistic: Set<string>` state; the dock render filters deferred by !dismissedOptimistic.has(id).
         * fetch POST `/api/recgon/owner/dock/dismiss` body `{ taskId }`.
         * On success: addToast (or no toast — UI-SPEC §7 doesn't list one; planner picks; if user feedback is silent dismissal, no toast).
         * On failure: revert dismissedOptimistic; addToast("couldn't dismiss — try again").
      - Wire `onRescheduleRequest(taskId)` from chip menu:
         * Open ReschedulePicker with taskId + currentDate.
         * On apply: optimistic override `{ scheduledDate: newDate }`.
         * fetch POST `/api/teams/${teamId}/tasks/${taskId}/schedule` with body MATCHING WeekCalendar.handleTaskDrop EXACTLY: `{ teammateId: task.assigneeId, scheduledDate: newDate, scheduledUntilDate: newDate }`. (Read WeekCalendar.tsx to confirm this is the exact body it sends; if it differs, copy whatever it actually sends.)
         * On success: addToast("moved to {MMM D}", 'success'); refresh window-tasks.
         * On failure: clear override; addToast("couldn't move — try again").

    Create the 3 test files for Tests 1-10. Use mocked fetch from setup.ts. For UI integration tests, render the relevant component subtree and use fireEvent / userEvent to drive interactions.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.dockManualAssign.test.tsx src/__tests__/ownerBoard.dockDismiss.test.tsx src/__tests__/ownerBoard.reschedulePicker.test.tsx</automated>
  </verify>
  <done>
    - Manual-assign picker on triaged dock rows fires the existing /assign endpoint with correct body shape; row leaves dock on success; toast confirms.
    - Dismiss `×` on deferred rows hides the row optimistically + persists per-user via the new endpoint.
    - Reschedule picker on chip menu fires the existing /schedule endpoint with WeekCalendar's exact body shape; chip moves; toast confirms.
    - All 10 tests across the 3 new test files pass.
    - Full suite no NEW regressions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire dnd-kit drag-drop — chip→teammate (reassign), chip→day (reschedule), dock→cell (both), with optimistic UI + rollback + ConfirmDialog for in-flight tasks</name>
  <files>src/components/v2/projects/owner/OwnerWorkloadGrid.tsx, src/components/v2/projects/owner/TriageDockRow.tsx, src/app/projects/OwnerWorkloadBoard.tsx, src/__tests__/ownerBoard.dnd.reassign.test.tsx, src/__tests__/ownerBoard.dnd.reschedule.test.tsx, src/__tests__/ownerBoard.dnd.dockToCell.test.tsx, src/__tests__/ownerBoard.dnd.rollback.test.tsx</files>
  <read_first>
    - src/components/v2/calendar/WeekCalendar.tsx handleTaskDrop (lines ~140-215 — full pattern: optimistic state, fetch, error handling, toast)
    - src/components/v2/calendar/SwimLane.tsx — confirm dragMode='dnd-kit' branch is wired (Plan 03.5-01)
    - src/components/v2/calendar/EventChip.tsx — confirm draggable={false} disables HTML5 drag attribute
    - src/components/ConfirmDialog.tsx — Dialog primitive shape
    - src/components/Toast.tsx — useToast
    - .planning/phases/03.5-owner-task-board/03.5-RESEARCH.md (lines 122-220 — Patterns 1-3, especially Pattern 3 for optimistic + rollback; Pitfall 4 — race with cron)
    - .planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md (§7 destructive — in-flight reassign confirm; §6 drag states; §8 motion + drag affordances)
    - 03.5-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1 (drag chip A → lane B): Render OwnerWorkloadBoard with 1 chip in alice's lane on Mon. Programmatically invoke the DndContext onDragEnd handler with `{ active: { id: 'task-1' }, over: { id: 'tm-bob__lane' } }`. Mock fetch /api/recgon/tasks/task-1/assign returns 200. Assert: optimistic override sets assigneeId=tm-bob; fetch fires with body {teammateId: 'tm-bob'}; toast "assigned to bob"; on success refresh fires.
    - Test 2 (drag chip Mon → Thu, same teammate): Invoke onDragEnd with `{ active: { id: 'task-1' }, over: { id: 'tm-alice__2026-05-21' } }` (same teammate, different date). Assert: optimistic override sets scheduledDate; fetch fires to /api/teams/{teamId}/tasks/task-1/schedule with body matching WeekCalendar shape; toast "moved to MAY 21".
    - Test 3 (drag chip A → cell B/Thu): Invoke onDragEnd with `{ active: { id: 'task-1' }, over: { id: 'tm-bob__2026-05-21' } }` (different teammate AND different date). Assert: BOTH /assign AND /schedule fetches fire; toast "assigned to bob on MAY 21"; both optimistic fields set.
    - Test 4 (drag dock row → cell): Render with 1 triaged task in dock. Invoke onDragEnd with `{ active: { id: 'task-2', data: { current: { kind: 'dock-row' } } }, over: { id: 'tm-alice__2026-05-19' } }`. Assert: BOTH /assign AND /schedule fetches fire; row leaves dock; chip lands in cell; toast "assigned to alice on MAY 19".
    - Test 5 (rollback): Same as Test 1 but mock /assign returns 500. Assert: optimistic override CLEARED after fetch fails; chip returns to alice's lane on Mon; toast "couldn't move — try again" (error).
    - Test 6 (rollback — partial failure on combined op): Test 4 path but /assign succeeds and /schedule fails. Decide policy: PLANNER PICK — either (a) treat as full-rollback (both fields revert), or (b) accept partial success (chip is reassigned but stays at original date) + warning toast. Recommendation: full rollback for simplicity in v1 (matches user mental model: "the drop didn't work"). Issue compensating /assign call to revert is overengineering for v1 — instead, accept the assign succeeded state and only revert the date override; toast "reassigned but couldn't reschedule".
    - Test 7 (in-flight chip — confirm dialog): Render a chip with status='in_progress'. Invoke onDragEnd with new teammate. Assert: ConfirmDialog opens with copy "Reassign in-flight task?" + body matching UI-SPEC §7. If confirmed: Test 1 path proceeds. If cancelled: no fetch fires; no optimistic update.
    - Test 8 (cancel mid-drag): Invoke onDragEnd with `over: null` (drop outside any droppable). Assert: no optimistic update; no fetch fires; no toast.
    - Test 9 (drag-source = dock-row — TriageDockRow is wrapped in useDraggable with kind='dock-row'): Programmatic test — render <TriageDockRow /> inside a test DndContext, assert the row's setNodeRef is bound. This is a structural test confirming the dock can be a drag source.
  </behavior>
  <action>
    UPDATE `src/components/v2/projects/owner/OwnerWorkloadGrid.tsx` (from 03.5-02):
      - Wrap each rendered chip in a dnd-kit `useDraggable` wrapper (outer `<div ref={setNodeRef} {...listeners} {...attributes}>` around the existing `<EventChip />`). Pass `id: task.id`, `data: { kind: 'task', task }`. Apply transform via `CSS.Transform.toString(transform)` and reduce opacity to 0.5 when `isDragging`.
      - Wrap each day-cell rendered inside `<SwimLane />` in a `useDroppable`. Cell id format: `${teammateId}__${dateISO}`. The cleanest path: extend SwimLane with an optional `cellWrapper?: (props: { teammateId, dateISO, children }) => ReactNode` prop, OR make SwimLane render its cells as already wrapped by a passed-in component. PLANNER PICK based on least-disruptive change. ALTERNATIVE: leave SwimLane as-is and apply useDroppable refs by querying the rendered cells via refs in OwnerWorkloadGrid (more brittle, not recommended). Recommendation: extend SwimLane with a `cellWrapper` prop; default = identity; owner-board passes a wrapper that wraps each cell in `useDroppable`.
      - Wrap each lane label (in OwnerWorkloadGrid's lane render, before SwimLane) in a `useDroppable` with id `${teammateId}__lane`. This gives the "drop on the lane label = reassign keeping current date" affordance.

    UPDATE `src/components/v2/projects/owner/TriageDockRow.tsx` (from 03.5-02):
      - Wrap the entire row in a `useDraggable` with id = task.id, data = `{ kind: 'dock-row', task }`. Spread attributes + listeners onto the wrapper.

    UPDATE `src/app/projects/OwnerWorkloadBoard.tsx` (from 03.5-01 + 03.5-02):
      - Replace the no-op `onDragEnd` placeholder with a real handler:
        ```
        function onDragEnd(event: DragEndEvent) {
          const { active, over } = event;
          if (!over) return;  // Test 8 path
          const taskId = String(active.id);
          const dropTargetId = String(over.id);
          const task = findTask(taskId);  // helper that finds task across windowTasks + dock.deferred + dock.triaged
          if (!task) return;
          handleDrop(task, dropTargetId, active.data.current?.kind === 'dock-row');
        }
        ```
      - Implement `handleDrop(task, dropTargetId, isFromDock)`:
        * Decode dropTargetId: if matches `^(.+)__lane$` → `{ teammateId, dateISO: task.scheduledDate }` (lane drop = keep date). If matches `^(.+)__(\d{4}-\d{2}-\d{2})$` → `{ teammateId, dateISO }`. Else return.
        * Compute deltas: assigneeChanged = teammateId !== task.assigneeId; dateChanged = dateISO !== task.scheduledDate; isFromDockOrTriaged = isFromDock || task.status === 'unassigned'.
        * In-flight guard (Test 7): if assigneeChanged && task.status in ['accepted', 'in_progress'] → open ConfirmDialog before proceeding.
        * Optimistic: setOptimisticOverrides({ ...prev, [task.id]: { ...(assigneeChanged ? { assigneeId: teammateId, status: 'assigned', triageNote: null } : {}), ...(dateChanged ? { scheduledDate: dateISO, scheduledUntilDate: dateISO } : {}) } }).
        * If assigneeChanged or isFromDockOrTriaged: fetch POST /api/recgon/tasks/${task.id}/assign with body `{ teammateId }`. Await response.
        * If dateChanged: fetch POST /api/teams/${teamId}/tasks/${task.id}/schedule with body matching WeekCalendar's exact shape `{ teammateId, scheduledDate: dateISO, scheduledUntilDate: dateISO }`.
        * On all success: addToast — phrasing per UI-SPEC §7 (single op = "assigned to X" or "moved to MAY D"; combined = "assigned to X on MAY D").
        * On any failure: clear optimistic override for that task; addToast("couldn't move — try again", 'error'). For partial failure (Test 6 policy): keep the succeeded change's override, clear the failed change's override; toast "reassigned but couldn't reschedule" (or chosen variant).
        * After all fetches complete (success or failure): trigger refresh of dock + window-tasks fetches so server state catches up.

      - Add a useEffect mirroring `src/app/tasks/page.tsx:134-217` to clear stale optimistic overrides when the real task state catches up.

      - Wire ConfirmDialog: state for `pendingDrop: { task, dropTargetId, isFromDock } | null`. When in-flight reassign detected, set this state instead of immediately calling handleDrop's fetches. Render `<ConfirmDialog open={pendingDrop !== null} title="Reassign in-flight task?" body={...UI-SPEC §7 copy with teammate names interpolated} primary="reassign" secondary="cancel" onConfirm={() => { handleDropReal(pendingDrop); setPendingDrop(null); }} onCancel={() => setPendingDrop(null)} />`.

    Add Pitfall 4 hedge: comment in OwnerWorkloadBoard.tsx near onDragEnd: `// Owner-vs-cron mid-drag race = last-write-wins per CONTEXT § Deferred Ideas. v1 acceptance.`

    Create the 4 test files for Tests 1-9. Per RESEARCH § Wave 0 Gaps: vitest can call the DndContext's onDragEnd handler directly. Render <OwnerWorkloadBoard />, find the rendered DndContext via React Testing Library queries (or expose onDragEnd via a test-only ref if direct access proves brittle), and invoke it with synthetic DragEndEvent shapes. Assert fetch was called with the right URL + body using the setup.ts mock helpers.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.dnd.reassign.test.tsx src/__tests__/ownerBoard.dnd.reschedule.test.tsx src/__tests__/ownerBoard.dnd.dockToCell.test.tsx src/__tests__/ownerBoard.dnd.rollback.test.tsx</automated>
  </verify>
  <done>
    - Mouse drag chip → lane reassigns; chip → day reschedules; chip → cell does both; dock → cell does both; all with optimistic UI + rollback + toasts per UI-SPEC §7.
    - In-flight (`accepted`/`in_progress`) chips trigger ConfirmDialog before reassign per UI-SPEC §7 destructive copy.
    - Drop outside any droppable = no-op (Test 8).
    - All 9 tests across the 4 new test files pass.
    - Full suite no NEW regressions.
    - `npm run build` succeeds (type-checks the dnd-kit integration end-to-end).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/recgon/owner/dock/dismiss | taskId in body, untrusted; validated as required + via getTask + verifyTeamAccess. |
| Browser → /api/recgon/tasks/[id]/assign (existing) | Plan 03-07 owner gate; reused by manual-assign picker AND drag-drop. |
| Browser → /api/teams/[id]/tasks/[taskId]/schedule (existing) | Already owner-gated (verified at lines 24-26 of route.ts during PATTERNS gathering); reused by reschedule picker AND drag-drop. |
| dnd-kit drop-target-id strings | Encoded client-side ('{teammateId}__{dateISO}', '{teammateId}__lane'); decoded with regex. teammateId is then passed to server which re-validates it belongs to the team via existing /assign body validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-01 | Authorization (server-side) | Dismiss + assign + schedule endpoints | mitigate | All three endpoints require owner role on the relevant team. Dismiss endpoint pattern matches /assign. Assign + schedule are reused, both shipped with owner gates. |
| T-3.5-02 | Authorization (mutation by non-owner) | Drag-drop UI mutation paths | mitigate | All mutations go through owner-gated endpoints. Even if a member crafts the URLs manually, server returns 403. Tested in `ownerBoard.dismissApi.test.ts`; assign + schedule contracts already tested in their respective shipped suites. |
| T-3.5-03 | Information Disclosure | Carry-over | mitigate | No new bulk endpoints in this plan. TaskDetailPanel still uses /api/recgon/tasks/[id] privacy filter. |
| T-3.5-04 | Concurrency (multi-owner + cron race) | OwnerWorkloadBoard onDragEnd + handleDrop | accept | Multi-owner concurrent drag = last-write-wins per CONTEXT. Cron-vs-owner mid-drag = same. Documented inline (Pitfall 4 hedge). v1 acceptance. |
</threat_model>

<verification>
- `npm run test` (full suite) shows no NEW regressions.
- `npm run lint` clean.
- `npm run build` succeeds.
- Manual UAT (per VALIDATION.md Manual UAT scenarios A-G — defer Playwright):
  * A — mouse drag chip A → B reassigns; refresh persists.
  * B — mouse drag chip Mon → Thu reschedules; refresh persists.
  * C — mouse drag dock → cell does both.
  * D — keyboard drag (Tab → Space → Arrow → Enter / Esc).
  * E — DevTools throttle to Offline → drag → snap-back + error toast.
  * F — log in as member → /projects shows tiles, no dock fetch.
  * G — touch device → 5px activation, drop fires.
- Manual: dismiss `×` on deferred row hides for current owner; sign in as a different owner → row still visible.
- Manual: drag in-flight chip → ConfirmDialog opens with destructive copy.
</verification>

<success_criteria>
- All 4 inline action paths work end-to-end: manual-assign picker, dismiss, reschedule picker, drag-drop (4 sub-modes: chip→lane, chip→day, chip→cell, dock→cell).
- Optimistic UI commits immediately; rolls back on API failure with a toast.
- In-flight chips trigger ConfirmDialog before reassign.
- Per-user dismissal persists (verified by sign-in-as-different-owner).
- Reschedule REUSES `/api/teams/[id]/tasks/[taskId]/schedule` (no duplicate endpoint introduced).
- Member/viewer hitting any of the 3 mutation endpoints is 403'd.
- `dnd-kit` keyboard accessibility works (manual UAT scenario D).
- Migration `20260517_owner_dock_dismissals.sql` ready to apply (user must run before live cron interacts with the table).
- All new tests green; full suite no NEW regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-owner-task-board/03.5-03-SUMMARY.md` describing:
- The 4 inline action paths + the 4 drag-drop modes shipped
- The endpoint reuse decision (existing /assign + existing /schedule; only new route is /dock/dismiss)
- The optimistic state pattern (key shape + clear-stale useEffect)
- The ConfirmDialog interception for in-flight chips
- The cellWrapper extension to SwimLane (or alternative chosen)
- Migration delivery — user must apply 20260517_owner_dock_dismissals.sql to live Supabase
- Manual UAT pass/fail per scenarios A-G
- Test count + green status
</output>
</content>
</invoke>