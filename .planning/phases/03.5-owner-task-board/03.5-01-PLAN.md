---
phase: 03.5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/v2/calendar/SwimLane.tsx
  - src/app/projects/page.tsx
  - src/app/projects/OwnerWorkloadBoard.tsx
  - src/components/v2/projects/owner/NonOwnerProjectsView.tsx
  - src/__tests__/ownerBoard/setup.ts
  - src/__tests__/ownerBoard.roleBranch.test.tsx
  - src/__tests__/ownerBoard.swimLaneDragMode.test.tsx
  - src/__tests__/ownerBoard.shellRender.test.tsx
autonomous: true
requirements:
  - SUCCESS-1
  - SUCCESS-5
  - D-01
  - D-03
  - D-05
  - D-06
  - D-19
tags:
  - recgon
  - owner-task-board
  - mvp-vertical-slice
  - dnd-kit-foundation
  - role-router

must_haves:
  truths:
    - "Owner navigates to /projects and lands on a NEW page (OwnerWorkloadBoard) — not the existing project tiles."
    - "Member or viewer navigates to /projects and still sees the existing project tiles (zero regression to non-owner experience)."
    - "OwnerWorkloadBoard renders an empty 14-day workload grid with a teammate row per active team member — no chips yet, but the scaffold is visible and the dnd-kit DndContext is mounted at page root."
    - "/calendar (personal) and /projects/[id]/tasks behavior is byte-identical pre/post this plan — SwimLane gains a `dragMode` prop that defaults to 'native', so existing callers are unaffected."
    - "Vitest suite includes the Wave-0 owner-board test scaffold — `src/__tests__/ownerBoard/setup.ts` exports mocks for useTeam/useToast/fetch and the 3 test files render without throwing."
  artifacts:
    - path: "src/app/projects/OwnerWorkloadBoard.tsx"
      provides: "Owner-only client page shell — DndContext at root, sensors (Pointer + Keyboard), 14-day grid scaffold, lane-per-teammate empty render."
    - path: "src/app/projects/page.tsx"
      provides: "Role-router page: extracts existing tile-list logic into <NonOwnerProjectsView/>, branches on currentTeam.role === 'owner' → <OwnerWorkloadBoard/>, else NonOwnerProjectsView. ALL hooks declared before any branch return."
    - path: "src/components/v2/projects/owner/NonOwnerProjectsView.tsx"
      provides: "Verbatim extraction of the current /projects/page.tsx tile-list render path — same hooks, same UI, same behavior."
    - path: "src/components/v2/calendar/SwimLane.tsx"
      provides: "Existing component + NEW `dragMode?: 'native' | 'dnd-kit' | 'none'` prop (default 'native'); HTML5 onDragOver/onDrop/draggable wiring gated by `dragMode === 'native'`."
    - path: "package.json"
      provides: "Adds @dnd-kit/core@^6.3.1 and @dnd-kit/utilities@^3.2.2 to dependencies. NOT @dnd-kit/react (v2-pre)."
    - path: "src/__tests__/ownerBoard/setup.ts"
      provides: "Shared test mocks — useTeam (owner/member/viewer fixtures), useToast (no-op spy), fetch (assign/reschedule/dock stubs), AgentTask fixtures (assigned, triaged, deferred)."
  key_links:
    - from: "src/app/projects/page.tsx"
      to: "src/components/TeamProvider.tsx (useTeam hook)"
      via: "currentTeam.role string check"
      pattern: "currentTeam\\.role === 'owner'"
    - from: "src/app/projects/page.tsx"
      to: "src/app/projects/OwnerWorkloadBoard.tsx"
      via: "named import + JSX render in owner branch"
      pattern: "<OwnerWorkloadBoard"
    - from: "src/app/projects/OwnerWorkloadBoard.tsx"
      to: "@dnd-kit/core"
      via: "DndContext + useSensor(PointerSensor) + useSensor(KeyboardSensor) at page root"
      pattern: "from '@dnd-kit/core'"
    - from: "src/components/v2/calendar/SwimLane.tsx"
      to: "self (extension point)"
      via: "new dragMode prop gates existing HTML5 listeners"
      pattern: "dragMode === 'native'"

threat_model:
  trust_boundaries:
    - "Browser → Next.js client component (OwnerWorkloadBoard renders in user's browser; role check from useTeam())"
    - "Next.js client → useTeam() context (server-derived role flows down via TeamProvider)"
  threats:
    - id: T-3.5-01
      category: Authorization
      component: "src/app/projects/page.tsx role branch"
      disposition: mitigate
      mitigation: "Client-side role check via useTeam(); when currentTeam.role !== 'owner', render NonOwnerProjectsView (existing tiles). Defense-in-depth lives in Plans 03.5-02 + 03 (server-side owner gate on every owner-board API call)."
    - id: T-3.5-04
      category: Concurrency
      component: "OwnerWorkloadBoard (multi-owner)"
      disposition: accept
      mitigation: "Multi-owner drag conflict = last-write-wins per CONTEXT D-deferred. Plan 03.5-01 ships scaffold only (no mutations); risk surfaces in Plans 03.5-03. Document inline in OwnerWorkloadBoard.tsx file header."
---

<objective>
Ship the owner-only entry point and the shared dnd-kit foundation. After this plan: an owner navigating to /projects sees a NEW empty workload board (14-day grid with empty teammate rows + dnd-kit DndContext at root); a member or viewer sees the existing project tiles. Zero regression to /calendar or /projects/[id]/tasks. The Wave-0 test scaffold is in place so subsequent plans can write component tests without re-mocking from scratch.

Purpose: Establish the role gate (Success Criteria 5) + the rendering shell (Success Criteria 1 partial — empty grid; chips arrive in 03.5-02) + the dnd-kit foundation that 03.5-03 will wire interactions through. Splitting this off keeps 03.5-02/03/04 focused on user-visible capability and prevents the SwimLane refactor + dependency install + page restructure from blocking later slices.

Output:
  - `package.json` + `package-lock.json` updated with `@dnd-kit/core@^6.3.1` and `@dnd-kit/utilities@^3.2.2`.
  - `src/components/v2/calendar/SwimLane.tsx` extended with `dragMode` prop (default `'native'` — zero behavior change for existing callers).
  - `src/app/projects/page.tsx` refactored as a thin role router; non-owner path extracted verbatim into `src/components/v2/projects/owner/NonOwnerProjectsView.tsx`.
  - NEW `src/app/projects/OwnerWorkloadBoard.tsx` renders an empty 14-day grid with one row per active teammate, wraps the grid in a `DndContext` with PointerSensor (5px activation) + KeyboardSensor, exposes `dragMode="dnd-kit"` to SwimLane, and bears a file-header comment locking the import path to `@dnd-kit/core` (Pitfall 5 hedge).
  - Wave-0 test scaffold under `src/__tests__/ownerBoard/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-owner-task-board/03.5-CONTEXT.md
@.planning/phases/03.5-owner-task-board/03.5-RESEARCH.md
@.planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md
@.planning/phases/03.5-owner-task-board/03.5-PATTERNS.md
@.planning/phases/03.5-owner-task-board/03.5-VALIDATION.md
@./CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from existing codebase. Use directly — no exploration needed. -->

useTeam() — from src/components/TeamProvider.tsx — returns:
  { currentTeam: { id: string; role: 'owner' | 'member' | 'viewer'; ... } | null; loading: boolean; ... }

SwimLane current Props (src/components/v2/calendar/SwimLane.tsx lines ~10-23):
  type Props = {
    teammate: TeammateWithStats;
    dayDates: Date[];
    cards: CalendarCard[];
    draggingTaskId: string | null;
    onTaskDrop: ((taskId: string, dropDate: Date) => void) | null;
    canReschedule: boolean;
    showLoadBar: boolean;
    onChipClick: ((card: CalendarCard) => void) | null;
    // ... existing fields ...
  };
  // ADD this plan: dragMode?: 'native' | 'dnd-kit' | 'none'  (default 'native')

@dnd-kit/core@^6.3.1 — root context import path:
  import {
    DndContext,
    PointerSensor,
    KeyboardSensor,
    useSensor,
    useSensors,
    closestCenter,
    type DragEndEvent,
  } from '@dnd-kit/core';

Reference pattern — src/app/projects/[id]/tasks/page.tsx lines 13-19 (canonical owner-only client guard):
  const { currentTeam, loading } = useTeam();
  if (loading || !currentTeam) return null;
  if (currentTeam.role !== 'owner') {
    return <LockedView />;
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dnd-kit dependencies + extend SwimLane with dragMode prop + scaffold owner-board test directory</name>
  <files>package.json, package-lock.json, src/components/v2/calendar/SwimLane.tsx, src/__tests__/ownerBoard/setup.ts, src/__tests__/ownerBoard.swimLaneDragMode.test.tsx</files>
  <read_first>
    - src/components/v2/calendar/SwimLane.tsx (entire file — current Props type ~lines 10-23, HTML5 wiring lines 252-267, draggable={canReschedule} on EventChip lines 294 and 329)
    - src/components/v2/calendar/EventChip.tsx (confirm `draggable?: boolean` prop exists at lines 24, 39, 83 and gates HTML5 drag attribute)
    - src/__tests__/inferredSkills.api.test.ts (vi.mock layout pattern — apply to setup.ts)
    - .planning/phases/03.5-owner-task-board/03.5-PATTERNS.md (lines 168-197 — SwimLane dragMode extension contract; lines 350-370 — test setup pattern)
    - .planning/phases/03.5-owner-task-board/03.5-RESEARCH.md (Pitfall 2 — both HTML5 + dnd-kit firing; Pitfall 5 — wrong @dnd-kit/* import path)
  </read_first>
  <behavior>
    - Test 1: SwimLane with `dragMode="native"` (default, when prop omitted) attaches HTML5 onDragOver / onDrop handlers to its day cells AND passes `draggable={canReschedule && true}` to each EventChip — i.e. byte-identical to current behavior; mounting <SwimLane /> without the prop produces a tree where the cells expose the handlers.
    - Test 2: SwimLane with `dragMode="dnd-kit"` does NOT attach HTML5 onDragOver / onDrop handlers to its day cells AND passes `draggable={false}` to each EventChip; HTML5 wiring is suppressed but the cell `<div>` still renders so an outer dnd-kit `useDroppable` wrapper can attach a ref.
    - Test 3: SwimLane with `dragMode="none"` attaches no HTML5 handlers AND passes `draggable={false}` to each EventChip.
    - Test 4 (regression): Default Props of SwimLane (no `dragMode` passed at all) behaves identically to `dragMode="native"` — explicit assertion that `/calendar` callers cannot regress.
  </behavior>
  <action>
    Install `@dnd-kit/core@^6.3.1` and `@dnd-kit/utilities@^3.2.2` via `npm install` (per D-19 + RESEARCH § Standard Stack — DO NOT install `@dnd-kit/react`, `@dnd-kit/dom`, `@dnd-kit/abstract`, or `@dnd-kit/sortable`; those are either v2-pre or unneeded for positional drag). Verify package.json reflects both new dependencies.

    Extend `SwimLane.tsx`'s `Props` type with an optional `dragMode?: 'native' | 'dnd-kit' | 'none'` field defaulting to `'native'`. Refactor the existing HTML5 wiring at the cell level (`onDragOver`, `onDrop`, `onDragLeave` — current lines ~252-267) so each handler is wrapped in `if (dragMode !== 'native') return;` (or omitted from JSX entirely when `dragMode !== 'native'` — choose whichever keeps the diff smallest while preserving native behavior). Pass `draggable={canReschedule && dragMode === 'native'}` (instead of bare `draggable={canReschedule}`) to BOTH EventChip render sites (current lines ~294 and ~329).

    Create `src/__tests__/ownerBoard/setup.ts` exporting:
      - `mockUseTeam(role: 'owner' | 'member' | 'viewer')` — returns a vi.fn-backed useTeam stub with `{ currentTeam: { id: 'team-1', role, name: 'Test Team' }, loading: false }`.
      - `mockUseToast()` — returns `{ addToast: vi.fn() }`.
      - `mockFetchOk(payload)` / `mockFetchFail(status, message)` helpers wrapping global.fetch via vi.spyOn (mirror the layout in `src/__tests__/inferredSkills.api.test.ts`).
      - `fixtureTask(overrides)` — returns a complete `AgentTask` with sensible defaults; allow override of `assigneeId`, `scheduledDate`, `triageNote`, `status`.
      - Three named fixtures pre-built: `assignedTask` (status='assigned', scheduledDate set, triageNote=null), `triagedTask` (status='unassigned', triageNote='no_clear_fit', scheduledDate=null), `deferredTask` (status='unassigned', triageNote=null, scheduledDate set 7 days in future).

    Create `src/__tests__/ownerBoard.swimLaneDragMode.test.tsx` with the 4 tests in `<behavior>`. Use React Testing Library + vitest (existing project setup). For each test, render <SwimLane /> with the relevant prop, then probe the resulting DOM via container.querySelectorAll('[data-cell-date]') (or whatever attribute the cell currently uses — read SwimLane.tsx and use the actual marker) and assert presence/absence of `ondragover` / `ondrop` props on the cell. For draggable assertion, find rendered chip nodes and assert their `draggable` HTML attribute matches expectation.

    Add a comment at the top of `OwnerWorkloadBoard.tsx` (created in Task 2) locking the import path: `// dnd-kit STABLE v6 — import from @dnd-kit/core only. Do NOT import from @dnd-kit/react (v2.0-pre).` (Pitfall 5 hedge.) Do NOT add this comment in this task; reserve for Task 2 where the import lives.

    Per D-19 + Pitfall 5: confirm via `grep -r "@dnd-kit/react" src/` returns no matches at end of task. CONVENTIONS hedge.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.swimLaneDragMode.test.tsx</automated>
  </verify>
  <done>
    - `package.json` shows `@dnd-kit/core` `^6.3.1` and `@dnd-kit/utilities` `^3.2.2`; `@dnd-kit/react` / `@dnd-kit/dom` / `@dnd-kit/abstract` / `@dnd-kit/sortable` absent.
    - `SwimLane.tsx` Props includes optional `dragMode`; HTML5 handlers and `draggable` flag both gated by `dragMode === 'native'`; existing `/calendar` and `/projects/[id]/tasks` continue to work because both omit the prop (default `'native'` preserves behavior).
    - All 4 swimLaneDragMode tests pass.
    - `src/__tests__/ownerBoard/setup.ts` exists and exports all named helpers + fixtures listed in <action>.
    - `npm run test -- --run` (full suite) shows no NEW failures introduced (any pre-existing failures from in-flight uncommitted work are out of scope).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor /projects/page.tsx as role router; build OwnerWorkloadBoard shell + extract NonOwnerProjectsView verbatim</name>
  <files>src/app/projects/page.tsx, src/app/projects/OwnerWorkloadBoard.tsx, src/components/v2/projects/owner/NonOwnerProjectsView.tsx, src/__tests__/ownerBoard.roleBranch.test.tsx, src/__tests__/ownerBoard.shellRender.test.tsx</files>
  <read_first>
    - src/app/projects/page.tsx (entire file — current ~1011 lines per PATTERNS.md; identify hook count + current render shape so the extraction is verbatim)
    - src/app/projects/[id]/tasks/page.tsx lines 1-93 (canonical owner-only role-branch + LockedView pattern; NOT a copy target — use `<NonOwnerProjectsView/>` instead of LockedView so members keep their existing tiles per D-03)
    - src/components/v2/calendar/WeekCalendar.tsx lines 1-150 (shell shape for OwnerWorkloadBoard — week-range state, paging, view-toggle slots; lines 270-340 lane render loop)
    - src/components/v2/calendar/WeekHeader.tsx (sticky day header — owner board renders 14 columns instead of 7 by passing a 14-element `dayDates` array)
    - src/components/v2/calendar/SwimLane.tsx (now with dragMode prop from Task 1)
    - src/components/TeamProvider.tsx (useTeam hook return shape)
    - src/lib/teamStorage.ts (verify `getTeamMembers` or equivalent — owner board needs the active teammate list; use whatever `WeekCalendar.tsx` uses today)
    - src/__tests__/ownerBoard/setup.ts (Task 1 output — use mockUseTeam helper)
    - .planning/phases/03.5-owner-task-board/03.5-RESEARCH.md (lines 284-342 — Owner-only page guard; Pitfall 1 — hook order violation)
    - .planning/phases/03.5-owner-task-board/03.5-PATTERNS.md (lines 32-95 — page role-router + OwnerWorkloadBoard shell analog)
    - .planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md (§4 Layout — page chrome, view toggle slot for 03.5-04, 14-col grid)
  </read_first>
  <behavior>
    - Test 1 (roleBranch — owner): Render <V2ProjectsListPage/> with mockUseTeam('owner'). Assert <OwnerWorkloadBoard/> is in the tree and <NonOwnerProjectsView/> is NOT.
    - Test 2 (roleBranch — member): Render with mockUseTeam('member'). Assert <NonOwnerProjectsView/> is in the tree and <OwnerWorkloadBoard/> is NOT.
    - Test 3 (roleBranch — viewer): Render with mockUseTeam('viewer'). Assert <NonOwnerProjectsView/> is in the tree and <OwnerWorkloadBoard/> is NOT.
    - Test 4 (roleBranch — loading): Render with mockUseTeam returning `{ currentTeam: null, loading: true }`. Assert the page returns null (no flash of either view).
    - Test 5 (network — member): Render <V2ProjectsListPage/> with mockUseTeam('member'). Spy on global.fetch. Wait for next tick. Assert fetch was NOT called for any URL containing `/api/recgon/owner/dock` (member never fires owner-board API calls — Pitfall 3 hedge; the owner dock endpoint doesn't ship until 03.5-02 but the contract is locked here).
    - Test 6 (shell — empty render): Render <OwnerWorkloadBoard teamId="team-1" /> with mockUseTeam('owner') and an empty teammate list. Assert: page renders without throwing, the page eyebrow "PROJECTS" text appears, the page headline contains "Workload" + a date range, and a `<DndContext>` is in the rendered tree (probe by class or test-id).
    - Test 7 (shell — populated grid): Render <OwnerWorkloadBoard teamId="team-1" /> with 3 mock teammates, empty task list. Assert 3 lane labels render (one per teammate name), zero chips render, and the grid renders 14 day-header cells (per D-06).
    - Test 8 (regression — non-owner unaffected): Render <NonOwnerProjectsView/> directly. Assert its render output is byte-identical (or as close as possible — same root element, same data-test markers if any) to what the original `/projects/page.tsx` produced for non-owner before refactor. Use a snapshot if helpful.
  </behavior>
  <action>
    Read current `src/app/projects/page.tsx` end-to-end. Catalog every hook call (useState, useEffect, useCallback, useMemo, useTeam, useToast, useSession, etc.) and the order they appear. Per Pitfall 1, all hooks MUST run before any branch return.

    Create `src/components/v2/projects/owner/NonOwnerProjectsView.tsx` as a `'use client'` component that accepts a Props shape matching whatever the current /projects/page.tsx needs as inputs (or, if the current page reads everything from hooks/context with no parent-passed inputs, takes no props and re-runs the hooks itself). Move the entire current render path (JSX + helper hooks + handlers) into this component VERBATIM. The goal: a member/viewer hitting /projects sees an indistinguishable rendering vs. before this plan.

    Refactor `src/app/projects/page.tsx` into a thin role router. Final shape:
      ```
      'use client';
      import { useTeam } from '@/components/TeamProvider';
      import { OwnerWorkloadBoard } from './OwnerWorkloadBoard';
      import { NonOwnerProjectsView } from '@/components/v2/projects/owner/NonOwnerProjectsView';

      export default function V2ProjectsListPage() {
        const { currentTeam, loading } = useTeam();
        if (loading || !currentTeam) return null;
        if (currentTeam.role === 'owner') {
          return <OwnerWorkloadBoard teamId={currentTeam.id} />;
        }
        return <NonOwnerProjectsView />;
      }
      ```
    Do NOT add other hooks to this file. The page becomes a 5-hook (zero-state) shell + role branch. NonOwnerProjectsView owns ALL the prior hooks and JSX.

    Create `src/app/projects/OwnerWorkloadBoard.tsx` as a `'use client'` component. Top of file (line 1): `// dnd-kit STABLE v6 — import from @dnd-kit/core only. Do NOT import from @dnd-kit/react (v2.0-pre).` (Pitfall 5 hedge.) File contents:
      - Mirror the shell shape of `WeekCalendar.tsx`: week-range state via useState (default to current week + next, 14 days total per D-06), `dayDates` computed via useMemo as a 14-element Date[] array, a paging handler (prev/next 2-week chunk), `today` button.
      - Render the page chrome per UI-SPEC §4: `PROJECTS` eyebrow (signature pink, JetBrains Mono), `Workload — {MMM D} → {MMM D}` headline, year suffix, paging arrows. Reuse the existing `WeekNav.tsx` structure if it accepts a passed-in date-range; if not, render an inline header that uses the SAME CSS classes (`cal-nav-eyebrow`, `cal-nav-headline-primary`, `cal-nav-headline-year`) per UI-SPEC §2 hard inheritance rule.
      - Below the header, render a placeholder slot for the TriageDock (will be filled in 03.5-02; for now render an empty `<aside aria-label="Triage dock placeholder" />` or omit and add a code comment `// TriageDock lands in 03.5-02`).
      - Below the dock slot, render the workload grid: WeekHeader (14 day cells) + one SwimLane per active teammate. Pass `dragMode="dnd-kit"` and `cards={[]}` (empty) to each SwimLane this plan; chips arrive in 03.5-02. `draggingTaskId={null}` and `onTaskDrop={null}` for now (drag wiring lands in 03.5-03).
      - Wrap the entire grid (WeekHeader + SwimLanes; do NOT include the page header) in a single `<DndContext>` at the page-render root with sensors per RESEARCH § Pattern 1: `useSensor(PointerSensor, { activationConstraint: { distance: 5 } })` + `useSensor(KeyboardSensor)`. `collisionDetection={closestCenter}`. `onDragEnd` is a no-op `() => {}` placeholder this plan (real handler lands in 03.5-03 — add a code comment `// onDragEnd wired in 03.5-03`).
      - Fetch the active teammate list using whatever data hook the existing /projects/page.tsx (or WeekCalendar) uses today — DO NOT introduce a new fetch pattern. If the existing pattern is `useEffect + fetch('/api/teams/.../members')`, mirror it. If TeamProvider already exposes the teammate list, read from there.
      - File header comment (after the dnd-kit import lock comment): `// Multi-owner concurrent drag = last-write-wins per CONTEXT D-deferred. Document only; no defensive code in v1.` (T-3.5-04 hedge.)

    Create `src/__tests__/ownerBoard.roleBranch.test.tsx` with Tests 1-5 from <behavior>. Use mockUseTeam from `src/__tests__/ownerBoard/setup.ts`.

    Create `src/__tests__/ownerBoard.shellRender.test.tsx` with Tests 6-8 from <behavior>. For Test 6 + 7, mock the teammate-list fetch (whatever the actual data hook is) via the setup helper. For Test 8 (regression), render NonOwnerProjectsView directly and use a snapshot or assert at least 3 stable structural markers (page eyebrow, project tile container class, etc.) match the pre-refactor /projects/page.tsx output — read the file before refactor to capture the markers.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.roleBranch.test.tsx src/__tests__/ownerBoard.shellRender.test.tsx</automated>
  </verify>
  <done>
    - `/projects` opened as owner renders OwnerWorkloadBoard with empty workload grid (one empty row per active teammate, 14 day-header cells, page chrome per UI-SPEC §4). The DndContext is mounted at page root.
    - `/projects` opened as member or viewer renders NonOwnerProjectsView, which is byte-equivalent to the prior /projects experience for non-owners.
    - `/calendar` and `/projects/[id]/tasks` are unchanged (verified by full test suite — `npm run test` shows no NEW regressions).
    - All 8 tests across the two new test files pass.
    - Member hitting /projects fires zero owner-board API calls (Test 5 confirms via fetch spy).
    - Hook count in `src/app/projects/page.tsx` is ≤ 1 (just `useTeam()`); all prior hooks moved verbatim to NonOwnerProjectsView.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js client | OwnerWorkloadBoard is a 'use client' component; user-controlled inputs (URL nav, drag events later) cross here. |
| Client → useTeam() context | currentTeam.role flows from server-derived session via TeamProvider; client trusts the provided role for UX gating only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-01 | Authorization (Elevation of Privilege) | `src/app/projects/page.tsx` role branch | mitigate | Client-side `useTeam()` role check before rendering OwnerWorkloadBoard. Plans 03.5-02 + 03 add server-side defense-in-depth on every owner-board API endpoint. This plan ships scaffold only (no mutating endpoints), so server-side check is not yet load-bearing. |
| T-3.5-04 | Concurrency (Tampering) | OwnerWorkloadBoard (multi-owner) | accept | Multi-owner concurrent drag = last-write-wins per CONTEXT § Deferred Ideas. v1 acceptance documented. No defensive code; risk surfaces in 03.5-03 when mutations ship. File header comment in OwnerWorkloadBoard.tsx documents the acceptance for future-us. |
| T-3.5-bundle | Spoofing (supply chain) | `@dnd-kit/core@^6.3.1` install | mitigate | Pin `^6.3.1` (not `*`); explicit comment in OwnerWorkloadBoard.tsx prohibits importing from `@dnd-kit/react` (v2-pre, API may shift). Pitfall 5 hedge. |
</threat_model>

<verification>
Phase-level checks for this plan:
- `npm run test` (full suite) shows no NEW regressions. Pre-existing failures from in-flight uncommitted work outside this plan's scope are not gating.
- `npm run lint` passes for all touched files.
- `npm run build` succeeds (Next.js production build — confirms tsc + bundling don't break with the new dependency or refactor).
- Manual smoke: open `/projects` in dev (logged in as owner of the seeded test team) → see empty workload board with day headers and teammate rows. Switch team to one where current user is member → see the existing project tiles.
</verification>

<success_criteria>
- Owner hits /projects → empty 14-day workload board renders with one row per active teammate, page chrome per UI-SPEC §4, DndContext mounted at root with PointerSensor + KeyboardSensor.
- Member or viewer hits /projects → existing project tiles render exactly as before this plan.
- /calendar and /projects/[id]/tasks behavior unchanged (SwimLane default `dragMode="native"` preserves HTML5 wiring for those callers).
- `@dnd-kit/core@^6.3.1` + `@dnd-kit/utilities@^3.2.2` installed; no v2-pre packages.
- Wave-0 owner-board test scaffold (`src/__tests__/ownerBoard/setup.ts`) exists and is consumed by the 3 new test files in this plan.
- All new tests green; full suite shows no NEW regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-owner-task-board/03.5-01-SUMMARY.md` describing:
- The role-router refactor (hooks counted before/after, NonOwnerProjectsView extraction)
- The SwimLane `dragMode` extension contract (default behavior preserved + how new callers opt in)
- dnd-kit dependency pin + import-path lock comment
- Empty-board screenshot description (page chrome + 14-col grid + N empty teammate rows + DndContext at root)
- Test count + green status
- Any deviations from PATTERNS.md or RESEARCH.md (e.g., if the existing teammate-list fetch shape differed from what the plan assumed)
</output>
</content>
</invoke>