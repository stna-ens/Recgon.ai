---
phase: 03.5
plan: 02
type: execute
wave: 2
depends_on:
  - "03.5-01"
files_modified:
  - src/app/api/recgon/owner/dock/route.ts
  - src/lib/recgon/storage.ts
  - src/app/projects/OwnerWorkloadBoard.tsx
  - src/components/v2/projects/owner/TriageDock.tsx
  - src/components/v2/projects/owner/TriageDockRow.tsx
  - src/components/v2/projects/owner/CapacityBar.tsx
  - src/components/v2/projects/owner/OwnerWorkloadGrid.tsx
  - src/__tests__/ownerBoard.dockApi.test.ts
  - src/__tests__/ownerBoard.dockPrivacy.test.ts
  - src/__tests__/ownerBoard.dockRender.test.tsx
  - src/__tests__/ownerBoard.workloadGridRender.test.tsx
  - src/__tests__/ownerBoard.capacityBar.test.tsx
autonomous: true
requirements:
  - SUCCESS-1
  - SUCCESS-2
  - D-05
  - D-07
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
tags:
  - recgon
  - owner-task-board
  - mvp-vertical-slice
  - read-only-board
  - capacity-bar
  - triage-dock-render

must_haves:
  truths:
    - "Owner navigates to /projects and sees the FULL picture for the next 14 days: every assigned task is rendered as a chip in the right teammate row + day cell; every triaged task appears in the dock above the grid; every deferred task appears in BOTH the dock (as a 'Needs Your Eyes' row) AND in the calendar at its future scheduled date."
    - "Each teammate row shows two stacked weekly capacity bars (Wk 1 / Wk 2) with green / yellow / signature-pink fill matching utilization (<60% / 60-85% / >85%) per UI-SPEC §3 + §6."
    - "The dock collapses to a 40px-tall 'TRIAGE · CLEAR' pill when count = 0 and expands to a list of compact rows (title / reason copy / [assign manually] button placeholder) when count > 0."
    - "Per-row reason copy matches D-14 exactly: `no_clear_fit` → 'no clear fit', `no_grounded_reason` → 'no grounded reason', `no_capacity_in_window` → 'qualified but booked through 4 wks', `no_capacity_high_priority` → 'high-priority + no capacity now', deferred → 'deferred → MMM D'."
    - "The owner-only `GET /api/recgon/owner/dock?teamId=...` returns `{ triaged: AgentTask[], deferred: AgentTask[] }` and STRICTLY EXCLUDES `assignment_reasoning` from every row (privacy filter — Pitfall 3 hedge)."
    - "Member or viewer who somehow constructs the URL `GET /api/recgon/owner/dock?teamId=X` is rejected with 403."
    - "Clicking a chip on the workload grid opens the existing `TaskDetailPanel` via the existing `/api/recgon/tasks/[id]` route (which applies the privacy filter on `whyYouSentence`); the bulk owner-board fetch never carries `whyYouSentence`."
  artifacts:
    - path: "src/app/api/recgon/owner/dock/route.ts"
      provides: "Owner-only `GET /api/recgon/owner/dock?teamId=X` returning `{ triaged: AgentTask[], deferred: AgentTask[] }`. Validates session + role; runs the two SQL queries from RESEARCH § Triage Dock Data Query; STRIPS `assignmentReasoning` from every returned row."
    - path: "src/lib/recgon/storage.ts"
      provides: "NEW exported `listOwnerDockTasks(teamId): Promise<{ triaged: AgentTask[]; deferred: AgentTask[] }>` helper. NEW exported `listAssignedTasksInWindow(teamId, startDate, endDate): Promise<AgentTask[]>` returning all `status='assigned'|'accepted'|'in_progress'|'awaiting_review'` tasks with `scheduled_date` in the 14-day window. Both helpers MUST omit `assignment_reasoning` from the row mapper output (or strip it before returning)."
    - path: "src/components/v2/projects/owner/TriageDock.tsx"
      provides: "Pinned dock above workload grid. Collapsed (count=0) = 40px pill 'TRIAGE · CLEAR' with faint signature glow. Expanded (count>0) = `glass-card` shell with header 'TRIAGE · {n} NEEDS YOUR EYES' + list of TriageDockRow children. Uses Radix `Collapsible` for the toggle (UI-SPEC §6)."
    - path: "src/components/v2/projects/owner/TriageDockRow.tsx"
      provides: "Single dock row: 3-line stack (title mono 11px, reason copy system-stack 12px italic, action button placeholder 'assign manually'). For deferred items, exposes a slot for a dismiss `×` button (NOT YET WIRED — UI placeholder only this plan; wired in 03.5-03)."
    - path: "src/components/v2/projects/owner/CapacityBar.tsx"
      provides: "Per-week per-teammate utilization bar. Reads `weeklyCapacityHours(teammate)` + `weeklyScheduledHours(tasks, weekStartDate)` (helpers added in this plan), renders a 16px pill with green/yellow/signature fill per UI-SPEC §6. ARIA: `role='progressbar'` with `aria-valuenow`/`aria-valuemin`/`aria-valuemax`/`aria-label`."
    - path: "src/components/v2/projects/owner/OwnerWorkloadGrid.tsx"
      provides: "The 14-col workload grid + lane stack. Wraps WeekHeader (14 day cells) + N SwimLanes (one per teammate, `dragMode='dnd-kit'`, `cards={...}` populated, `onChipClick` opens TaskDetailPanel via existing /api/recgon/tasks/[id])."
    - path: "src/app/projects/OwnerWorkloadBoard.tsx"
      provides: "Updated from 03.5-01: now fetches dock data + assigned-task-window data via the new endpoints, renders TriageDock + CapacityBar + OwnerWorkloadGrid + chip-click → TaskDetailPanel."
  key_links:
    - from: "src/app/projects/OwnerWorkloadBoard.tsx"
      to: "src/app/api/recgon/owner/dock/route.ts"
      via: "useEffect + fetch on mount + on team change"
      pattern: "fetch.*api/recgon/owner/dock"
    - from: "src/app/projects/OwnerWorkloadBoard.tsx"
      to: "src/app/api/teams/[id]/tasks/route.ts (or equivalent existing assigned-task list endpoint — confirm via WeekCalendar.tsx)"
      via: "useEffect + fetch — REUSE the existing assigned-task fetch from WeekCalendar; DO NOT introduce a parallel endpoint"
      pattern: "fetch.*api/teams.*tasks"
    - from: "src/app/api/recgon/owner/dock/route.ts"
      to: "src/lib/recgon/storage.ts (listOwnerDockTasks)"
      via: "import + await"
      pattern: "listOwnerDockTasks"
    - from: "src/components/v2/projects/owner/OwnerWorkloadGrid.tsx"
      to: "src/components/v2/calendar/SwimLane.tsx"
      via: "JSX render with dragMode='dnd-kit'"
      pattern: "dragMode=\"dnd-kit\""
    - from: "src/components/v2/projects/owner/OwnerWorkloadGrid.tsx (chip click)"
      to: "src/app/api/recgon/tasks/[id]/route.ts (existing — privacy filter on whyYouSentence)"
      via: "click handler → open TaskDetailPanel → panel fetches detail"
      pattern: "fetch.*api/recgon/tasks/"
    - from: "src/components/v2/projects/owner/CapacityBar.tsx"
      to: "src/lib/recgon/match.ts capacity helpers"
      via: "import + math"
      pattern: "weeklyCapacityHours\\|capacityHours"

threat_model:
  trust_boundaries:
    - "Browser → /api/recgon/owner/dock (untrusted teamId query param crosses)"
    - "Browser → /api/recgon/tasks/[id] (existing — privacy filter on whyYouSentence already enforced)"
    - "Server (storage row mapper) → JSON response (assignment_reasoning JSONB MUST NOT cross)"
  threats:
    - id: T-3.5-01
      category: Authorization (server-side defense in depth for the role gate)
      component: "src/app/api/recgon/owner/dock/route.ts"
      disposition: mitigate
      mitigation: "Mirror the shipped `/api/recgon/tasks/[id]/assign` route pattern (lines 49-66): require session, verifyTeamAccess(teamId, userId), reject if role !== 'owner' with 403. teamId comes from query string but is validated as a UUID and used only as a lookup key into verifyTeamAccess — no SQL injection surface (all queries go through Supabase parameterized client)."
    - id: T-3.5-03
      category: Information Disclosure (privacy leak)
      component: "src/app/api/recgon/owner/dock/route.ts AND `listAssignedTasksInWindow` in storage.ts"
      disposition: mitigate
      mitigation: "Both bulk endpoints MUST strip `assignment_reasoning` from every row before returning JSON. Add a contract test (`ownerBoard.dockPrivacy.test.ts`) asserting the response shape excludes `assignmentReasoning` and `whyYouSentence` keys. Per-chip detail (when owner clicks) goes through the existing `/api/recgon/tasks/[id]` route, which applies the Phase-3 privacy filter on `whyYouSentence`. Pitfall 3 hedge."
    - id: T-3.5-04
      category: Concurrency (multi-owner)
      component: "OwnerWorkloadBoard read path"
      disposition: accept
      mitigation: "Read-only this plan; no mutation surface. Concurrency surfaces in 03.5-03."
---

<objective>
Make the owner board USEFUL READ-ONLY: the owner sees the team's full task picture in one glance — every assigned task as a chip in the right cell, every triaged task in the dock above, every deferred task in BOTH dock + calendar, per-teammate per-week capacity bars showing who's slammed and who's free. No actions yet (those land in 03.5-03), but every chip is clickable → opens the existing TaskDetailPanel.

Purpose: Deliver Success Criteria 1 fully (structured grid surfacing assignee + scheduled date + kind + status + why-you + triage/defer state) and Success Criteria 2 partially (triage + deferred are first-class — visible inline; act-on lands in 03.5-03). The privacy filter on whyYouSentence (Pitfall 3) is enforced server-side at the bulk endpoint by stripping `assignment_reasoning` entirely from list responses.

Output:
  - NEW `src/app/api/recgon/owner/dock/route.ts` — owner-only GET, returns `{ triaged, deferred }`, strips `assignmentReasoning`.
  - NEW `src/lib/recgon/storage.ts` helpers: `listOwnerDockTasks(teamId)`, `listAssignedTasksInWindow(teamId, start, end)`. Both omit `assignment_reasoning` from output.
  - NEW components under `src/components/v2/projects/owner/`: `TriageDock.tsx`, `TriageDockRow.tsx`, `CapacityBar.tsx`, `OwnerWorkloadGrid.tsx`.
  - UPDATED `src/app/projects/OwnerWorkloadBoard.tsx` — fetches dock + window-of-assigned-tasks, wires TriageDock + CapacityBar + OwnerWorkloadGrid into the shell from 03.5-01, click-on-chip opens existing `TaskDetailPanel`.
  - 5 new test files covering API contract, privacy invariant, dock render states (collapsed + expanded), workload grid render, and capacity bar utilization bands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-owner-task-board/03.5-CONTEXT.md
@.planning/phases/03.5-owner-task-board/03.5-RESEARCH.md
@.planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md
@.planning/phases/03.5-owner-task-board/03.5-PATTERNS.md
@.planning/phases/03.5-owner-task-board/03.5-VALIDATION.md
@.planning/phases/03.5-owner-task-board/03.5-01-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from existing codebase + Plan 03.5-01 outputs. -->

AgentTask shape (src/lib/recgon/types.ts ~lines 250-280):
  type AgentTask = {
    id: string;
    teamId: string;
    title: string;
    description: string | null;
    requiredSkills: string[];
    kind: TaskKind;  // 'codebase' | 'analytics' | 'content' | 'general' | 'manual'
    status: TaskStatus;  // 'unassigned' | 'assigned' | 'accepted' | 'in_progress' | 'awaiting_review' | 'completed' | 'verified' | 'cancelled' | 'failed'
    priority: number;
    assigneeId: string | null;  // teammate id (NOT user id)
    scheduledDate: string | null;  // YYYY-MM-DD
    scheduledUntilDate: string | null;
    triageNote: TriageNote | null;  // 4-value union
    assignmentReasoning: AssignmentReasoning | null;  // <— MUST NOT cross bulk endpoint boundary
    // ... other fields ...
  };
  type TriageNote = 'no_clear_fit' | 'no_grounded_reason' | 'no_capacity_in_window' | 'no_capacity_high_priority';

Existing storage helper analogs (src/lib/recgon/storage.ts):
  - deferTaskScheduledDate(taskId, newDate): lines 638-653 — read for query/update style
  - clearTriageNote(taskId): lines 660-666
  - logEvent(...): lines 940-970 — event union includes 'rescheduled', 'manually_assigned'
  - The supabase client is imported as `supabase` from `@/lib/supabase` (service-role)

Existing assigned-task window fetch — confirm in WeekCalendar.tsx (likely `/api/teams/[id]/tasks?from=...&to=...`); REUSE that exact endpoint. If WeekCalendar uses a different pattern (e.g., per-teammate fetch), reuse that pattern verbatim — do NOT introduce a third path.

verifyTeamAccess from src/lib/teamStorage.ts:
  function verifyTeamAccess(teamId: string, userId: string): Promise<'owner' | 'member' | 'viewer' | null>

dnd-kit imports (from Plan 03.5-01 install):
  import { DndContext, useDraggable, useDroppable } from '@dnd-kit/core';
  import { CSS } from '@dnd-kit/utilities';

Reference SQL (RESEARCH § Triage Dock Data Query):
  -- Triaged: status='unassigned' AND triage_note IS NOT NULL AND (scheduled_date IS NULL OR scheduled_date <= CURRENT_DATE)
  -- Deferred: status='unassigned' AND triage_note IS NULL AND scheduled_date > CURRENT_DATE

Reference component pattern (src/components/v2/calendar/UnscheduledSidebar.tsx — full file):
  - cal-unsched-sidebar / cal-unsched-header / cal-unsched-eyebrow / cal-unsched-list / cal-unsched-item layout
  - DOES use button + draggable={canDrag} (HTML5) — TriageDock SHOULD NOT copy the HTML5 drag wiring; rows are drag-source-eligible only in 03.5-03 via dnd-kit useDraggable wrapper

Reference capacity helpers (src/components/v2/calendar/SwimLane.tsx ~lines 32-36):
  function dailyCapacityHours(t: TeammateWithStats): number {
    const cap = Number(t.capacityHours) || 0;
    const days = t.workingHours?.days?.length ?? 7;
    return cap > 0 ? cap / Math.max(1, days) : 0;
  }
  // ADD this plan: weeklyCapacityHours(t) = dailyCapacityHours(t) × workingDaysCount

Existing chip-click → TaskDetailPanel flow (src/components/v2/calendar/WeekCalendar.tsx — read for the pattern):
  - chip onClick sets selectedTaskId state
  - <TaskDetailPanel taskId={selectedTaskId} onClose={...} /> renders when set
  - Panel internally fetches /api/recgon/tasks/[id] and applies its own state
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build owner-dock GET endpoint + storage helpers + privacy contract tests</name>
  <files>src/app/api/recgon/owner/dock/route.ts, src/lib/recgon/storage.ts, src/__tests__/ownerBoard.dockApi.test.ts, src/__tests__/ownerBoard.dockPrivacy.test.ts</files>
  <read_first>
    - src/app/api/recgon/tasks/[id]/assign/route.ts (entire file, 158 lines — exact owner-only contract pattern + body validation + audit log shape)
    - src/lib/recgon/storage.ts lines 1-50 (imports + supabase client) and lines 600-700 (existing reschedule/clearTriageNote/listTasks helpers — copy the row-mapper conventions)
    - src/lib/recgon/types.ts lines 250-280 (AgentTask + TriageNote)
    - src/__tests__/tasks.manualAssign.api.test.ts (entire — 9 test scenarios, exact vi.mock layout)
    - src/__tests__/ownerBoard/setup.ts (Plan 03.5-01 output)
    - .planning/phases/03.5-owner-task-board/03.5-PATTERNS.md (lines 262-289 — owner/dock route analog; lines 290-329 — storage append pattern)
    - .planning/phases/03.5-owner-task-board/03.5-RESEARCH.md (Pitfall 3 — bulk endpoint privacy leak)
  </read_first>
  <behavior>
    - Test 1 (auth): Unauthenticated GET → 401. (no session)
    - Test 2 (auth): Authenticated as member of the team → 403.
    - Test 3 (auth): Authenticated as viewer of the team → 403.
    - Test 4 (auth): Authenticated as owner of a DIFFERENT team → 403.
    - Test 5 (validation): Missing teamId query param → 400.
    - Test 6 (validation): teamId present but team doesn't exist (verifyTeamAccess returns null) → 403.
    - Test 7 (success — empty): Owner of empty team → 200 with `{ triaged: [], deferred: [] }`.
    - Test 8 (success — populated): Owner of team with 1 triaged task (no_clear_fit, no scheduled_date) + 1 deferred task (triage_note null, scheduled_date 7 days in future) + 3 normal assigned tasks → response includes the triaged task in `triaged` array, the deferred task in `deferred` array, and the 3 normal assigned tasks in NEITHER array.
    - Test 9 (privacy — assignmentReasoning stripped): Construct a triaged row with `assignment_reasoning` JSONB present in DB → response row MUST have `assignmentReasoning === undefined` (or the key absent entirely; assert `'assignmentReasoning' in row === false` AND `'whyYouSentence' in row === false`).
    - Test 10 (privacy — same for deferred): Same as 9 but for a deferred row.
    - Test 11 (privacy — listAssignedTasksInWindow): Direct unit test on the storage helper — given a fixture row with `assignment_reasoning` populated, the returned object MUST NOT contain that field.
  </behavior>
  <action>
    Append to `src/lib/recgon/storage.ts` two new exported helpers, mirroring the row-mapping style of `listTasks*` (find existing `listTasks` or similar helpers in the file and follow the same shape):

    1. `listOwnerDockTasks(teamId: string): Promise<{ triaged: AgentTask[]; deferred: AgentTask[] }>` — runs the two SELECT queries from RESEARCH § Triage Dock Data Query against `agent_tasks`. Use the existing supabase client + `.eq('team_id', teamId)` + `.eq('status', 'unassigned')` + `.not('triage_note', 'is', null)` for the triaged query (and the inverse + `.gt('scheduled_date', today)` for deferred). Map rows to AgentTask via existing row mapper but EXPLICITLY STRIP `assignment_reasoning` (set to `undefined` or omit from the returned object). Return both arrays.

    2. `listAssignedTasksInWindow(teamId: string, startDateISO: string, endDateISO: string): Promise<AgentTask[]>` — selects `agent_tasks` where `team_id = teamId` AND `status IN ('assigned', 'accepted', 'in_progress', 'awaiting_review')` AND `scheduled_date BETWEEN startDateISO AND endDateISO`. Same row-mapper hygiene: strip `assignment_reasoning`.

    Critical: the existing row mapper likely returns the full record. To stay defense-in-depth, write the new helpers to call the existing mapper THEN apply a `.map(row => { const { assignmentReasoning, ...rest } = row; return rest; })` (or equivalent destructure-and-discard) before returning. Add an ESLint-disable-line comment if the unused-var rule complains about the discarded field; the comment IS the documentation that this is intentional.

    Create `src/app/api/recgon/owner/dock/route.ts`:
      - Imports: `NextResponse`, `auth` from `@/auth`, `listOwnerDockTasks` from `@/lib/recgon/storage`, `verifyTeamAccess` from `@/lib/teamStorage`.
      - Export `async function GET(request: Request)`.
      - Read `teamId` from `new URL(request.url).searchParams.get('teamId')`. Return 400 if missing.
      - Mirror the auth + role pattern from `/api/recgon/tasks/[id]/assign/route.ts` lines 49-66: session check → 401 if absent; `verifyTeamAccess(teamId, session.user.id)` → 403 if role !== 'owner' (also 403 if role is null, which covers "team doesn't exist" or "user not on team").
      - Call `await listOwnerDockTasks(teamId)`; return `NextResponse.json({ triaged, deferred })`.
      - DO NOT log any event for the GET (read-only; audit log is for mutations only — RESEARCH recommendation).

    Create `src/__tests__/ownerBoard.dockApi.test.ts` covering Tests 1-8 from <behavior>. Use the vi.mock layout from `src/__tests__/tasks.manualAssign.api.test.ts` lines 30-46:
      ```
      vi.mock('@/auth', () => ({ auth: () => mockAuth() }));
      vi.mock('@/lib/recgon/storage', () => ({ listOwnerDockTasks: mockListDock }));
      vi.mock('@/lib/teamStorage', () => ({ verifyTeamAccess: vi.fn() }));
      ```
      Construct Request objects via `new Request('http://localhost/api/recgon/owner/dock?teamId=team-1')` and call the imported GET handler directly. Assert response status + body shape per each scenario.

    Create `src/__tests__/ownerBoard.dockPrivacy.test.ts` covering Tests 9-11. For Tests 9 and 10, mock `listOwnerDockTasks` to return rows with `assignmentReasoning` populated → call GET → parse response JSON → assert the assignmentReasoning key is absent from each row. For Test 11, mock the supabase client at the storage layer (use the existing test pattern from any storage-level test) and call `listAssignedTasksInWindow` directly with a fixture row that has `assignment_reasoning` populated → assert the returned object lacks the field. If no storage-level test pattern exists, exercise the helper through a thin integration that mocks supabase via `vi.mock('@/lib/supabase', ...)`.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.dockApi.test.ts src/__tests__/ownerBoard.dockPrivacy.test.ts</automated>
  </verify>
  <done>
    - `GET /api/recgon/owner/dock?teamId=X` returns 401 / 403 / 400 / 200 per Tests 1-8.
    - Bulk response NEVER carries `assignmentReasoning` or `whyYouSentence` (Tests 9-11 confirm).
    - `listOwnerDockTasks` and `listAssignedTasksInWindow` exported from storage.ts; both strip `assignment_reasoning` defensively.
    - All 11 tests across the two new test files pass.
    - Full suite shows no NEW regressions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build TriageDock + TriageDockRow + CapacityBar + OwnerWorkloadGrid components; wire into OwnerWorkloadBoard with chip-click → TaskDetailPanel</name>
  <files>src/components/v2/projects/owner/TriageDock.tsx, src/components/v2/projects/owner/TriageDockRow.tsx, src/components/v2/projects/owner/CapacityBar.tsx, src/components/v2/projects/owner/OwnerWorkloadGrid.tsx, src/app/projects/OwnerWorkloadBoard.tsx, src/__tests__/ownerBoard.dockRender.test.tsx, src/__tests__/ownerBoard.workloadGridRender.test.tsx, src/__tests__/ownerBoard.capacityBar.test.tsx</files>
  <read_first>
    - src/components/v2/calendar/UnscheduledSidebar.tsx (entire file — analog for TriageDock card-shell + sticky-header + list pattern; cal-unsched-* CSS classes)
    - src/components/v2/calendar/SwimLane.tsx (entire file — for OwnerWorkloadGrid render shape; .cal-day-load* render block at lines 269-275 + CSS at 483-510 — analog for CapacityBar)
    - src/components/v2/calendar/EventChip.tsx (chip render + click handler shape — owner board reuses this verbatim with draggable={false} via SwimLane dragMode='dnd-kit')
    - src/components/v2/calendar/WeekHeader.tsx (sticky day header — owner grid renders 14 day cells)
    - src/components/v2/calendar/TaskDetailPanel.tsx (chip-click target panel — already integrates whyYouSentence + triage_note via /api/recgon/tasks/[id])
    - src/components/Select.tsx (Radix primitive style — for the placeholder "assign manually" trigger this plan; full picker wiring lands in 03.5-03)
    - src/components/v2/calendar/WeekCalendar.tsx (entire file — find the existing assigned-task window fetch pattern; reuse it in OwnerWorkloadBoard)
    - src/lib/recgon/match.ts (find existing capacity helpers — `dailyCapacityHours` likely exists; weekly variant either exists or needs to be added in this task)
    - .planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md (entire — §3 color contract, §4 layout, §6 states, §7 copywriting, §9 a11y)
    - .planning/phases/03.5-owner-task-board/03.5-PATTERNS.md (lines 98-165 — TriageDock + CapacityBar analogs)
  </read_first>
  <behavior>
    - Test 1 (TriageDock — collapsed): Render <TriageDock triaged={[]} deferred={[]} />. Assert: rendered tree contains the text "TRIAGE · CLEAR" and the container has approximate height of 40px (or has a class like `is-collapsed` per UI-SPEC §6); no list items rendered.
    - Test 2 (TriageDock — expanded with triaged): Render with one triaged task (triageNote='no_clear_fit', title='Refactor auth module'). Assert: header text matches "TRIAGE · 1 NEEDS YOUR EYES" (case per UI-SPEC §7), one row renders with the title and reason copy "no clear fit" (per D-14).
    - Test 3 (TriageDock — expanded with deferred): Render with one deferred task (triageNote=null, scheduledDate='2026-05-22'). Assert: header "TRIAGE · 1 NEEDS YOUR EYES", one row with reason copy "deferred → MAY 22" (date formatted MMM D, all-caps month, per UI-SPEC §7).
    - Test 4 (TriageDock — mixed counts): 2 triaged + 3 deferred → header "TRIAGE · 5 NEEDS YOUR EYES", 5 rows total.
    - Test 5 (TriageDockRow — reason copy mapping): Parametrize over the 4 triage_note values + deferred. Assert each renders the exact D-14 string. Triaged rows MUST NOT render a dismiss `×` button slot; deferred rows MUST render the dismiss slot (placeholder this plan).
    - Test 6 (CapacityBar — healthy): Render with scheduledHours=12, capacityHours=30 (40% util) → bar fill width 40%, fill class is healthy (e.g. `is-healthy`), trailing text "12h / 30h · HEALTHY".
    - Test 7 (CapacityBar — approaching): scheduledHours=21, capacityHours=30 (70%) → fill class is `is-warn`, text "21h / 30h · APPROACHING".
    - Test 8 (CapacityBar — near cap): scheduledHours=27, capacityHours=30 (90%) → fill class is `is-near-cap` (or signature without glow), text "27h / 30h · NEAR CAP".
    - Test 9 (CapacityBar — over): scheduledHours=33, capacityHours=30 (110%) → fill class is `is-overloaded`, text "33h / 30h · OVER".
    - Test 10 (CapacityBar — idle): scheduledHours=0, capacityHours=30 → text contains "IDLE", bar empty.
    - Test 11 (CapacityBar — a11y): role='progressbar', aria-valuenow={scheduledHours}, aria-valuemin=0, aria-valuemax={capacityHours}, aria-label includes teammate name + "week N utilization".
    - Test 12 (OwnerWorkloadGrid — render): Render with 2 teammates and a fixture of 3 assigned tasks (one per teammate, scheduled in the visible window). Assert: 14 day-header cells, 2 lane labels, 3 chips total in the grid; each chip lands in the correct (teammate × date) cell.
    - Test 13 (OwnerWorkloadGrid — chip click): Render the grid with 1 chip. Click it. Assert: the parent (mocked via prop callback) receives the clicked task's id (parent is OwnerWorkloadBoard, which sets selectedTaskId state and renders TaskDetailPanel — verified via callback spy here).
    - Test 14 (OwnerWorkloadGrid — uses dragMode='dnd-kit'): Snapshot or DOM-probe assertion that the rendered SwimLane child receives `dragMode="dnd-kit"` (mock SwimLane and assert prop value).
    - Test 15 (OwnerWorkloadBoard end-to-end render): With mocked dock fetch returning 1 triaged + 1 deferred, mocked window fetch returning 2 assigned tasks for 2 teammates, render the board → assert the dock is expanded with 2 rows AND the grid contains 2 chips AND the deferred task's chip is also present in the grid (D-13 "appears in BOTH").
  </behavior>
  <action>
    Read the existing assigned-task fetch in `src/components/v2/calendar/WeekCalendar.tsx`. Identify the endpoint URL pattern + response shape. Reuse it for the OwnerWorkloadBoard's window-of-assigned-tasks fetch — DO NOT introduce a third path. If WeekCalendar uses a per-teammate fetch loop, do the same. Document the chosen pattern in the OwnerWorkloadBoard.tsx file header.

    If `weeklyCapacityHours(teammate)` does NOT already exist in `src/lib/recgon/match.ts` or `src/components/v2/calendar/SwimLane.tsx`, add it: `weeklyCapacityHours(t) = dailyCapacityHours(t) × workingDaysPerWeek` where `workingDaysPerWeek = t.workingHours?.days?.length ?? 5`. Export from match.ts so CapacityBar can import. Also add `weeklyScheduledHours(tasks: AgentTask[], weekStartISO: string): number` that sums hours of tasks whose scheduled_date falls in the week starting at weekStartISO. Both helpers go alongside the existing capacity functions.

    Build `src/components/v2/projects/owner/CapacityBar.tsx`:
      - Props: `{ teammate: TeammateWithStats; scheduledHoursThisWeek: number; weekIndex: 1 | 2; weekStartLabel: string; }`
      - Render a 16px-tall pill (full width of parent, border-radius 8px). Inside: a fill bar (left-aligned, width = `Math.min(100, (scheduled/cap) * 100)%`) + trailing text "{scheduled}h / {cap}h · {STATE}".
      - Tier rule per UI-SPEC §6: `<60%` → `.capacity-bar.is-healthy` (`--success` fill, "HEALTHY"); `60-85%` → `.capacity-bar.is-warn` (`--warning` fill, "APPROACHING"); `86-100%` → `.capacity-bar.is-near-cap` (`--signature` no glow, "NEAR CAP"); `>100%` → `.capacity-bar.is-overloaded` (`--signature` with glow — reuse `.cal-day-load.is-overloaded` exact styling); `0` → "IDLE", empty bar.
      - ARIA: per UI-SPEC §9 — `role="progressbar"`, `aria-valuemin=0`, `aria-valuemax={capacityHours}`, `aria-valuenow={scheduledHours}`, `aria-label="{teammate.displayName} week {weekIndex} utilization"`.
      - Above the bar render an eyebrow `WK {weekIndex}` (9px JetBrains Mono, faint, 1.2px letter-spacing).
      - Optionally wire a Radix `Tooltip` showing breakdown ("{scheduled} of {cap} hours scheduled · {N} tasks across {D} days") — this is per UI-SPEC §4 capacity bar block; if scope creeps, a plain `title` attribute is acceptable for v1 (note in summary).
      - Add corresponding CSS to globals.css (or co-located CSS module) — NEW classes in UI-SPEC §1: respects 4px / 8px grid, no off-grid pixels.

    Build `src/components/v2/projects/owner/TriageDockRow.tsx`:
      - Props: `{ task: AgentTask; isDeferred: boolean; onAssignClick?: (taskId: string) => void; onDismissClick?: (taskId: string) => void; }`
      - Render a 3-line stack: title (mono 11px, `cal-chip-title`-style — confirm class match), reason copy (system stack, 12px, italic, `--txt-muted`), action button row.
      - Reason copy mapping (D-14):
         * `triageNote === 'no_clear_fit'` → "no clear fit"
         * `triageNote === 'no_grounded_reason'` → "no grounded reason"
         * `triageNote === 'no_capacity_in_window'` → "qualified but booked through 4 wks"
         * `triageNote === 'no_capacity_high_priority'` → "high-priority + no capacity now"
         * `isDeferred && task.scheduledDate` → `deferred → ${formatMMMD(task.scheduledDate)}` (date in mono span, fragment in system stack)
      - Action button: `assign manually` (mono, 10px UPPER, `cal-panel-btn` style scaled). Click → calls `onAssignClick?.(task.id)`. This plan: the click handler is a no-op stub that prop-drills up — dropdown picker wiring lands in 03.5-03.
      - Dismiss `×` (only when `isDeferred`): wrap in Radix `Tooltip` with text "Dismiss from dock — task stays in calendar". Click → calls `onDismissClick?.(task.id)`. This plan: stub.
      - ARIA per UI-SPEC §9: row has `role="listitem"`; action button has `aria-label="assign {task.title}"`.

    Build `src/components/v2/projects/owner/TriageDock.tsx`:
      - Props: `{ triaged: AgentTask[]; deferred: AgentTask[]; onAssignClick?: (taskId: string) => void; onDismissClick?: (taskId: string) => void; }`
      - Total count = triaged.length + deferred.length.
      - When count = 0: render a 40px-tall pill with text "TRIAGE · CLEAR" (mono 10px UPPER, faint signature glow on hover). Use Radix `Collapsible` with `defaultOpen={false}`.
      - When count > 0: render expanded card. Card shell = `glass-card` class (DO NOT nest a second one — `feedback_glass_treatment`). Header: "TRIAGE · {n} NEEDS YOUR EYES" (mono 10px UPPER, signature pink). Body: `<ul role="list">` with `<li>` per task; render TriageDockRow for each. Triaged rows first, then deferred rows. 8px gap between rows.

    Build `src/components/v2/projects/owner/OwnerWorkloadGrid.tsx`:
      - Props: `{ teammates: TeammateWithStats[]; tasks: AgentTask[]; dayDates: Date[] (length 14); onChipClick: (taskId: string) => void; }`
      - Render `WeekHeader` with the 14-element dayDates.
      - For each teammate: render the lane label cell (sticky 180px column) — name + title + TWO stacked CapacityBars (Wk 1 + Wk 2, computed by splitting the 14-day window in half).
      - For each teammate row: render `<SwimLane teammate={t} dayDates={dayDates} cards={chipsForTeammate(t)} dragMode="dnd-kit" draggingTaskId={null} onTaskDrop={null} canReschedule={true} showLoadBar={true} onChipClick={(card) => onChipClick(card.taskId)} />`. (The `onTaskDrop={null}` + dnd-kit's outer DndContext mean drops won't fire HTML5 OR dnd-kit yet — drag wiring is 03.5-03's concern.)
      - For deferred tasks (scheduledDate > today, status='unassigned'), render a "MOVED BY AI" badge clone of `.cal-chip-request` styling (UI-SPEC §3 reserved-for #8). Wrap chip in Radix `Tooltip` — this plan: simple title attribute on the chip is acceptable; Tooltip can land later.
      - Empty teammate row: cells render with no chips; CapacityBars show "0h / 30h · IDLE".

    UPDATE `src/app/projects/OwnerWorkloadBoard.tsx` (existing from 03.5-01):
      - Add useState for: `dock: { triaged: AgentTask[]; deferred: AgentTask[] } | null`, `windowTasks: AgentTask[] | null`, `selectedTaskId: string | null`.
      - Add useEffect to fetch `/api/recgon/owner/dock?teamId={teamId}` on mount + on teamId change → setDock.
      - Add useEffect to fetch the window-of-assigned-tasks for the visible 14-day range using whatever endpoint WeekCalendar reuses → setWindowTasks.
      - Combine `dock.deferred + windowTasks` for the grid render so deferred tasks appear in BOTH dock and grid (D-13).
      - Render `<TriageDock triaged={dock?.triaged ?? []} deferred={dock?.deferred ?? []} onAssignClick={(id) => { /* placeholder — 03.5-03 */ }} onDismissClick={(id) => { /* placeholder — 03.5-03 */ }} />`.
      - Render `<OwnerWorkloadGrid teammates={...} tasks={[...windowTasks, ...dock.deferred]} dayDates={dayDates} onChipClick={setSelectedTaskId} />` inside the existing DndContext.
      - Render `<TaskDetailPanel taskId={selectedTaskId} onClose={() => setSelectedTaskId(null)} />` (or whatever the existing panel-open prop shape is — read TaskDetailPanel.tsx). The panel internally fetches `/api/recgon/tasks/[id]` which applies the privacy filter — we never pass `whyYouSentence` from the bulk fetch.
      - Loading state: while either fetch is in flight, show the existing `.week-cal-loading` skeleton (per UI-SPEC §6) — reuse the WeekCalendar skeleton or render a simple shimmer.
      - Error state: if either fetch fails, render an inline error pill in the dock header per UI-SPEC §6 ("couldn't reach recgon — retry") + a retry button.

    Create test files for Tests 1-15:
      - `src/__tests__/ownerBoard.dockRender.test.tsx` — Tests 1-5 (TriageDock + TriageDockRow render states + reason copy mapping).
      - `src/__tests__/ownerBoard.capacityBar.test.tsx` — Tests 6-11.
      - `src/__tests__/ownerBoard.workloadGridRender.test.tsx` — Tests 12-15.

    Use fixtures from `src/__tests__/ownerBoard/setup.ts` (Plan 03.5-01 output). For Test 14 (dragMode prop), mock the SwimLane component via `vi.mock('@/components/v2/calendar/SwimLane', ...)` and assert the prop received. For Test 15 (end-to-end render), mock the two fetches via the setup helpers.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.dockRender.test.tsx src/__tests__/ownerBoard.capacityBar.test.tsx src/__tests__/ownerBoard.workloadGridRender.test.tsx</automated>
  </verify>
  <done>
    - Owner navigating to /projects sees: TriageDock (collapsed if empty, expanded with rows if not) + workload grid with chips in the right cells + per-teammate per-week CapacityBars.
    - Reason copy in dock matches D-14 exactly across all 5 cases (4 triage + deferred).
    - Capacity bar fill color shifts at 60% / 85% / 100% boundaries per UI-SPEC §6.
    - Clicking any chip opens TaskDetailPanel (existing component) which fetches the per-task detail via `/api/recgon/tasks/[id]` (existing privacy filter applies — bulk fetch never carried whyYouSentence).
    - Deferred tasks appear in BOTH the dock and the grid (D-13).
    - All 15 tests across the three new test files pass.
    - Full suite (`npm run test`) shows no NEW regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/recgon/owner/dock | teamId query param, untrusted. Validated as required field; use only as Supabase parameterized lookup. |
| Server (storage row mapper) → JSON response | Defense-in-depth: `assignment_reasoning` JSONB MUST NOT cross — stripped at storage helper boundary. |
| Browser → /api/recgon/tasks/[id] (existing) | Per-chip detail. Existing privacy filter on `whyYouSentence` applies (Plan 03-03 contract). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-01 | Authorization | `src/app/api/recgon/owner/dock/route.ts` | mitigate | Mirror shipped `/api/recgon/tasks/[id]/assign` pattern (lines 49-66): session check → 401; `verifyTeamAccess` → 403 if role !== 'owner'. teamId from query string validated as required field. |
| T-3.5-03 | Information Disclosure | bulk owner-dock GET + `listAssignedTasksInWindow` storage helper | mitigate | Both bulk paths strip `assignment_reasoning` from row mapper output BEFORE returning. Contract test (`ownerBoard.dockPrivacy.test.ts`) asserts response shape excludes `assignmentReasoning` AND `whyYouSentence`. Per-chip detail goes through existing `/api/recgon/tasks/[id]` which applies the Phase-3 privacy filter on `whyYouSentence`. |
| T-3.5-04 | Concurrency (multi-owner) | OwnerWorkloadBoard read path | accept | Read-only this plan; no mutation surface. Concurrency surfaces in 03.5-03. |
</threat_model>

<verification>
- `npm run test` (full suite) shows no NEW regressions.
- `npm run lint` clean for touched files.
- `npm run build` succeeds.
- Manual smoke (owner-account dev): /projects renders with dock above grid. If your dev DB has triaged tasks (Phase 3 should be writing them via the cron), the dock expands with rows; otherwise the dock pill says "TRIAGE · CLEAR". Capacity bars per teammate per week show colored fills. Click any chip → TaskDetailPanel slides in.
- Manual privacy check (use browser devtools Network tab): GET /api/recgon/owner/dock response JSON contains zero rows with `assignmentReasoning` or `whyYouSentence` keys.
</verification>

<success_criteria>
- Owner sees the FULL team task picture for the next 14 days in one URL: every assigned chip in the right cell, every triaged item in the dock, every deferred item in BOTH dock and grid, per-week capacity bars per teammate.
- Bulk owner-board endpoint NEVER carries `assignmentReasoning` or `whyYouSentence` (server contract test confirms; Pitfall 3 mitigated).
- Dock collapses to a 40px "TRIAGE · CLEAR" pill when count=0; expands to a `glass-card` row list when count>0.
- Reason copy per row matches D-14 exactly.
- Capacity bar 3-tier color rule works at 60% / 85% / 100% boundaries.
- Member or viewer hitting `GET /api/recgon/owner/dock` is 403'd.
- All new tests green; full suite no NEW regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-owner-task-board/03.5-02-SUMMARY.md` describing:
- The dock + window-of-assigned-tasks fetch pattern (which existing endpoint was reused for assigned tasks)
- The privacy strip implementation (where in storage.ts the discard happens, and the contract test that locks it)
- TriageDock states (collapsed pill ↔ expanded card)
- CapacityBar tier boundaries + colors (which existing CSS classes were cloned)
- Whether `weeklyCapacityHours` was added to match.ts or already existed
- Test count + green status
- Any Tooltip / Radix Popover wiring deferred to 03.5-03
</output>
</content>
</invoke>