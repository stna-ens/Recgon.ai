---
phase: 03.5
plan: 04
type: execute
wave: 4
depends_on:
  - "03.5-01"
  - "03.5-02"
  - "03.5-03"
files_modified:
  - src/components/v2/projects/owner/TableBoard.tsx
  - src/app/projects/OwnerWorkloadBoard.tsx
  - src/app/tasks/page.tsx
  - src/__tests__/ownerBoard.tableBoardRender.test.tsx
  - src/__tests__/ownerBoard.tableBoardSort.test.tsx
  - src/__tests__/ownerBoard.viewToggle.test.tsx
  - src/__tests__/tasks.personalHeaderStrip.test.tsx
autonomous: true
requirements:
  - SUCCESS-1
  - SUCCESS-3
  - SUCCESS-4
  - D-08
tags:
  - recgon
  - owner-task-board
  - mvp-vertical-slice
  - table-view
  - tasks-page-clarification

must_haves:
  truths:
    - "Owner on /projects sees a `workload` / `table` view toggle pill in the OwnerWorkloadBoard nav row. `workload` is selected by default. One click switches to `table` view."
    - "In table view: the same 14-day window of tasks renders as a sortable Radix-styled all-mono table with columns TASK / ASSIGNEE / PROJECT / DATE / PRIORITY / STATE. Header click toggles ascending/descending sort on that column."
    - "Clicking any row in the table view opens the existing TaskDetailPanel (same path as a workload-grid chip click) — privacy filter on whyYouSentence still applied via the existing /api/recgon/tasks/[id] route."
    - "Each row in the table has a kebab (`⋯`) menu that opens a Radix DropdownMenu with `assign manually` (when triaged / unassigned) and `reschedule` (when assigned) options. These reuse the AssignTeammatePicker + ReschedulePicker components shipped in 03.5-03 — NO new mutation endpoints."
    - "Drag-drop is NOT wired in table view (workload-only per UI-SPEC §4). Switching from workload → table preserves any in-flight optimistic state (no chip flicker)."
    - "Member or viewer (any non-owner) navigating to /tasks sees a top header strip above the existing kanban columns with the text 'Your personal tasks — owners can see the full team board at /projects.' The strip uses the existing `recgon-label` mono eyebrow style + signature `→ /projects` link only visible when `currentTeam.role === 'owner'`."
    - "The /tasks page kanban (assigned / in progress / in review) behavior is byte-identical pre/post this plan — the header strip is purely additive. Zero regression to the existing personal-task drag-drop, proof-drop, reschedule-request flows."
    - "**Filter chips are deferred** (ROADMAP SC-3 mentions 'filter by assignee/kind/priority'): v1 ships sortable Table view (D-08) which gives most slicing need. Explicit filter chips moved to follow-up phase per CONTEXT § Deferred Ideas (decision 2026-05-16). Plan 04 does NOT add filter chip UI."
  artifacts:
    - path: "src/components/v2/projects/owner/TableBoard.tsx"
      provides: "Sortable all-mono table renderer. Props: `tasks: AgentTask[]`, `teammates: TeammateWithStats[]`, `projects: ProjectInfo[]`, `onRowClick: (task) => void`, `onAssign: (task) => void`, `onReschedule: (task) => void`. Six columns (TASK / ASSIGNEE / PROJECT / DATE / PRIORITY / STATE). Header cells: 10px JetBrains Mono UPPER + `↑` / `↓` glyph for sorted col. Row height 44px. Per-row kebab `⋯` button opens Radix DropdownMenu with `assign manually` / `reschedule` actions delegating to props. Card shell mirrors `.week-cal-outer` 28px-radius — DO NOT nest inside another `glass-card` (per `feedback_glass_treatment`)."
    - path: "src/app/projects/OwnerWorkloadBoard.tsx"
      provides: "UPDATED from 03.5-03: adds `viewMode: 'workload' | 'table'` useState (default `'workload'`), renders the existing `WeekNav` view-toggle pattern (lines 113-118 — Radix DropdownMenu with `cal-view-trigger` styling, active option marked with `•` glyph in signature pink). In `'table'` mode renders `<TableBoard tasks={assignedTasks} ... />` instead of `<OwnerWorkloadGrid />`. TriageDock + CapacityBars remain visible in both modes (capacity bars unchanged — they live in the lane labels and only appear in workload view per UI-SPEC §4). DnD remains active in workload mode only; the DndContext wraps both views but TableBoard does not register draggable/droppable refs."
    - path: "src/app/tasks/page.tsx"
      provides: "UPDATED: adds a NEW header strip component (inline, ~30 lines) at the top of the `V2TasksInner` render path BEFORE the existing column header row. Strip content: small mono eyebrow `PERSONAL TASKS` + sentence `Your assigned + accepted + in-review tasks. Owners can see the full team board at` + signature-pink Next.js `<Link href='/projects'>/projects</Link>`. The `/projects` link is rendered ONLY when `currentTeam.role === 'owner'` (non-owner sees the sentence end at 'team board' with no link). Strip uses existing `recgon-label` + `glass-card` design tokens; NO new CSS file."
    - path: "src/__tests__/ownerBoard.tableBoardRender.test.tsx"
      provides: "RTL test: renders `<TableBoard tasks={[fixtureA, fixtureB, fixtureC]} ... />` with 3 task fixtures spanning 3 different priorities + 3 different statuses. Asserts: 6 column headers render in order TASK / ASSIGNEE / PROJECT / DATE / PRIORITY / STATE; 3 row elements render; each row exposes a kebab `⋯` button with aria-label `task actions`."
    - path: "src/__tests__/ownerBoard.tableBoardSort.test.tsx"
      provides: "RTL test: renders TableBoard with 3 tasks of mixed priorities (urgent=0, high=1, normal=3). Clicks the PRIORITY column header → asserts the rendered row order matches ascending priority (0, 1, 3); clicks again → asserts descending (3, 1, 0); same pattern for DATE column."
    - path: "src/__tests__/ownerBoard.viewToggle.test.tsx"
      provides: "RTL test: mounts `<OwnerWorkloadBoard teamId='t1' />` with mocked fetch returning fixtures. Asserts initial render shows `OwnerWorkloadGrid` (assert by querying `data-testid='owner-workload-grid'`). Clicks the view-toggle dropdown trigger → selects `table` → asserts `OwnerWorkloadGrid` is unmounted and `TableBoard` is mounted (assert by `data-testid='owner-table-board'`). TriageDock remains mounted across both modes (assert).  "
    - path: "src/__tests__/tasks.personalHeaderStrip.test.tsx"
      provides: "RTL test: mounts `/tasks` `<V2TasksInner />` twice — once with `useTeam()` returning `{ currentTeam: { id: 't1', role: 'owner' } }` (link to /projects visible), once with `role: 'member'` (link absent, sentence truncated). Asserts mono eyebrow `PERSONAL TASKS` present in both. Asserts the kanban columns (`assigned`, `in progress`, `in review`) still render below the strip in both cases — regression guard for the existing layout."
  key_links:
    - from: "src/app/projects/OwnerWorkloadBoard.tsx"
      to: "src/components/v2/projects/owner/TableBoard.tsx"
      via: "viewMode === 'table' branch in JSX render"
      pattern: "viewMode === 'table'"
    - from: "src/components/v2/projects/owner/TableBoard.tsx"
      to: "src/components/v2/projects/owner/AssignTeammatePicker.tsx (shipped 03.5-03)"
      via: "kebab DropdownMenu action item invokes the picker"
      pattern: "AssignTeammatePicker"
    - from: "src/components/v2/projects/owner/TableBoard.tsx"
      to: "src/components/v2/projects/owner/ReschedulePicker.tsx (shipped 03.5-03)"
      via: "kebab DropdownMenu action item invokes the picker"
      pattern: "ReschedulePicker"
    - from: "src/app/tasks/page.tsx"
      to: "src/components/TeamProvider.tsx (useTeam)"
      via: "conditional render of /projects link based on currentTeam.role"
      pattern: "currentTeam\\.role === 'owner'"
    - from: "src/app/tasks/page.tsx header strip /projects link"
      to: "src/app/projects/page.tsx"
      via: "Next.js <Link href='/projects'>"
      pattern: "Link.*href=['\"]/projects['\"]"

threat_model:
  trust_boundaries:
    - "Browser → /projects?view=table (no new server-side trust boundary; same useTeam role check as 03.5-01)"
    - "Browser → /tasks header strip (no trust boundary; the /projects link is gated client-side but server-side at /projects already enforces role per 03.5-01)"
  threats:
    - id: T-3.5-01
      category: Authorization
      component: "src/app/projects/OwnerWorkloadBoard.tsx view toggle"
      disposition: mitigate
      mitigation: "Already gated upstream — non-owner sees NonOwnerProjectsView per 03.5-01 role branch and never reaches OwnerWorkloadBoard. View toggle is only reachable by owners. No new auth surface this plan."
    - id: T-3.5-02
      category: Authorization
      component: "src/components/v2/projects/owner/TableBoard.tsx kebab actions"
      disposition: mitigate
      mitigation: "Kebab actions delegate to AssignTeammatePicker + ReschedulePicker (03.5-03), which fire POST /api/recgon/tasks/[id]/assign and POST /api/teams/[id]/tasks/[taskId]/schedule. Both endpoints already enforce owner-only via verifyTeamAccess per 03.5-03 threat model. No new endpoint, no new auth gap."
    - id: T-3.5-03
      category: Privacy
      component: "src/components/v2/projects/owner/TableBoard.tsx row data"
      disposition: mitigate
      mitigation: "TableBoard consumes the same task list as OwnerWorkloadGrid — sourced from the bulk owner-board GET which already strips assignment_reasoning per 03.5-02 (Pitfall 3 hedge). Row click opens TaskDetailPanel via the existing per-chip /api/recgon/tasks/[id] route which applies the privacy filter on whyYouSentence. No bulk path acquires reasoning JSONB."
    - id: T-3.5-04
      category: Concurrency
      component: "src/app/projects/OwnerWorkloadBoard.tsx (viewMode + optimistic state)"
      disposition: accept
      mitigation: "Multi-owner drag conflict = last-write-wins (D-deferred per CONTEXT). View toggle does not introduce new concurrency risk — switching mode mid-drag is impossible (the existing DragOverlay locks pointer state). Optimistic overrides from 03.5-03 are mode-agnostic and survive the toggle. Document inline."
---

<objective>
Ship the final two clarifications for the Phase 3.5 owner board: (a) the **Table view** secondary mode that owners can toggle to from the workload view (D-08, SUCCESS-3), and (b) the **/tasks header strip** that clarifies the personal-vs-team boundary (SUCCESS-4). After this plan: an owner on `/projects` has two equally-supported views (workload by default, table one click away — same data, sortable by any column). A teammate on `/tasks` sees a small mono eyebrow + sentence at the top of the page making clear that `/tasks` is their personal queue and the team-wide owner board lives at `/projects` (link only shown to owners). No other changes to `/tasks` (no redesign — deferred per CONTEXT scope_boundary).

Purpose: Finishes the cross-page coherence for Phase 3.5. Without the table view, the workload-only board falls short of D-08 ("Table view toggle exists alongside Workload — same data, sortable rows by any column"). Without the /tasks header strip, members keep wondering "where is the rest of the team's work?" — the answer ("nowhere for you; ask the owner") needs to be visible UI, not tribal knowledge.

Output:
  - NEW `src/components/v2/projects/owner/TableBoard.tsx` — sortable 6-col mono table that reuses the row-click → TaskDetailPanel path and the kebab-menu → AssignTeammatePicker / ReschedulePicker actions from 03.5-03.
  - UPDATED `src/app/projects/OwnerWorkloadBoard.tsx` — adds `viewMode` state + `WeekNav`-style view-toggle dropdown; branches between `OwnerWorkloadGrid` (workload) and `TableBoard` (table); TriageDock stays mounted in both modes.
  - UPDATED `src/app/tasks/page.tsx` — header strip prepended above the existing column header row; signature-pink `/projects` link rendered only when `currentTeam.role === 'owner'`.
  - Four new vitest files covering: TableBoard render contract, TableBoard sort behavior, view-toggle wiring on OwnerWorkloadBoard, /tasks header strip role-conditional render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-owner-task-board/03.5-CONTEXT.md
@.planning/phases/03.5-owner-task-board/03.5-RESEARCH.md
@.planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md
@.planning/phases/03.5-owner-task-board/03.5-VALIDATION.md
@.planning/phases/03.5-owner-task-board/03.5-PATTERNS.md
@.planning/phases/03.5-owner-task-board/03.5-01-SUMMARY.md
@.planning/phases/03.5-owner-task-board/03.5-02-SUMMARY.md
@.planning/phases/03.5-owner-task-board/03.5-03-SUMMARY.md

# Existing analogs (read first per task <read_first>)
@src/components/v2/calendar/WeekNav.tsx
@src/components/v2/calendar/TaskDetailPanel.tsx
@src/app/tasks/page.tsx
@src/components/v2/projects/owner/OwnerWorkloadGrid.tsx
@src/components/v2/projects/owner/AssignTeammatePicker.tsx
@src/components/v2/projects/owner/ReschedulePicker.tsx
@src/__tests__/inferredSkills.ui.test.tsx
</context>

<interfaces>
<!-- Key contracts the executor must respect. Extracted from the codebase + prior plan SUMMARYs. -->
<!-- Executor should use these directly — no codebase exploration needed beyond <read_first> per task. -->

From src/lib/recgon/types.ts (existing):
  AgentTask {
    id: string;
    teamId: string;
    title: string;
    description: string;
    kind: TaskKind;
    priority: number;       // 0=urgent, 1=high, 2=normal, 3=low
    status: 'unassigned'|'assigned'|'accepted'|'in_progress'|'awaiting_review'|'completed'|...;
    assignedTo: string|null;
    scheduledDate: string|null;   // YYYY-MM-DD
    triageNote: TriageNote|null;
    projectId: string|null;
    // assignmentReasoning is STRIPPED from owner-board bulk responses (Pitfall 3)
  }

From src/components/v2/calendar/WeekNav.tsx (view-toggle pattern, lines 33-118 — REUSE):
  type ViewMode = 'calendar' | 'list';  // for OwnerWorkloadBoard rename to 'workload' | 'table'
  // Pattern: useState + onToggleView callback;
  // Radix-styled DropdownMenu with cal-view-trigger styling;
  // active option marked with • glyph in signature pink (lines 113-118).

From src/components/v2/projects/owner/AssignTeammatePicker.tsx (shipped 03.5-03):
  Props: { task: AgentTask; teammates: TeammateWithStats[]; onAssigned: () => void; trigger: React.ReactNode }
  // Reuse as kebab-menu action — pass a DropdownMenuItem as `trigger`.

From src/components/v2/projects/owner/ReschedulePicker.tsx (shipped 03.5-03):
  Props: { task: AgentTask; onRescheduled: () => void; trigger: React.ReactNode }
  // Reuse as kebab-menu action — pass a DropdownMenuItem as `trigger`.

From src/components/TeamProvider.tsx (existing useTeam hook):
  useTeam(): { currentTeam: { id: string; role: 'owner'|'member'|'viewer'; ... } | null; loading: boolean; ... }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TableBoard component — sortable 6-column mono table with row click + kebab actions</name>
  <files>src/components/v2/projects/owner/TableBoard.tsx, src/__tests__/ownerBoard.tableBoardRender.test.tsx, src/__tests__/ownerBoard.tableBoardSort.test.tsx</files>
  <read_first>
    - src/components/v2/calendar/WeekNav.tsx (lines 1-180) — `cal-view-trigger` styling + Radix DropdownMenu pattern (active option `•` glyph in signature pink, lines 113-118)
    - src/components/v2/projects/owner/AssignTeammatePicker.tsx — props contract (re-used as kebab action item)
    - src/components/v2/projects/owner/ReschedulePicker.tsx — props contract (re-used as kebab action item)
    - .planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md §4 "Table view" (lines 245-263) — column order, header style, row height, kebab pattern
    - .planning/phases/03.5-owner-task-board/03.5-PATTERNS.md row 7 (TableBoard analog = `WeekNav` view-toggle)
    - src/__tests__/inferredSkills.ui.test.tsx — RTL render harness pattern (vi.mock layout for useTeam + useToast)
    - .planning/phases/03.5-owner-task-board/03.5-RESEARCH.md § Design system tokens (signature pink, JetBrains Mono, `.week-cal-outer` shell)
  </read_first>
  <behavior>
    - Render contract:
      - Default empty state: when `tasks.length === 0` renders centered faint text `No tasks scheduled in this window.` (matches UI-SPEC §6 table empty copy) — assertable by `getByText`.
      - With N tasks: renders exactly 6 column headers in order `TASK ASSIGNEE PROJECT DATE PRIORITY STATE` (all caps, 10px JetBrains Mono, faint). Each header is a `<button>` with `aria-label="sort by {column}"`.
      - Renders N `<tr>` rows; each row 44px tall; each row has `role="button"` + `aria-label="open task {title}"` for row-click handler; each row contains a kebab `<button aria-label="task actions">⋯</button>` triggering Radix DropdownMenu with two MenuItem children: `assign manually` (visible only when `task.assignedTo === null` OR `task.triageNote !== null`) + `reschedule` (visible when `task.assignedTo !== null`).
    - Sort contract:
      - Initial sort: by `scheduledDate` ascending (null dates last).
      - Click a header button: toggle (asc → desc → asc) only for that column; clicking a different header resets to asc for the new column.
      - Header for the active sort column shows `↑` or `↓` glyph; inactive headers show no glyph.
      - PRIORITY sort: 0 (urgent) < 1 (high) < 2 (normal) < 3 (low); STATE sort: alphabetic.
      - When two tasks tie on the sort key, fall back to `task.id` ascending for deterministic ordering (testable).
    - Click contract:
      - Clicking a row anywhere except the kebab button or its menu invokes `onRowClick(task)`.
      - Clicking the kebab button opens the Radix DropdownMenu and does NOT invoke `onRowClick` (stop propagation).
      - Selecting `assign manually` invokes `onAssign(task)`; selecting `reschedule` invokes `onReschedule(task)`.
    - Tests:
      - `ownerBoard.tableBoardRender.test.tsx`: render 3 fixtures → assert headers + rows + kebab buttons present; assert empty-state copy when fixtures = `[]`.
      - `ownerBoard.tableBoardSort.test.tsx`: render 3 fixtures with mixed priorities (0/1/3) → click PRIORITY header → assert row order `[0,1,3]`; click again → assert `[3,1,0]`. Repeat for DATE column with three different ISO dates including one `null`.
  </behavior>
  <action>
    Create `src/components/v2/projects/owner/TableBoard.tsx` as a `'use client'` React component. Define props matching the contract in `<must_haves>.artifacts`. Use Radix `DropdownMenu` from `@radix-ui/react-dropdown-menu` for the kebab; pass the existing `AssignTeammatePicker` and `ReschedulePicker` components (from 03.5-03) as MenuItem children — those components already wrap the API calls. Style the table with inline class names using existing design tokens — no new CSS file. Match the 28px-radius `.week-cal-outer` shell wrapper. Use JetBrains Mono via the existing `recgon-label` class for headers. Apply `.cal-day-load-text` style for the body cell mono numbers (priority chip, date text). Implement sort state via `useState<{column: ColumnKey; direction: 'asc'|'desc'}>` (no external lib). Use stable `useMemo` for the sorted task array. Render rows with `role="button"` + `aria-label="open task {task.title}"`. The row click handler should ignore clicks bubbled from the kebab button (use `event.target` check or `event.stopPropagation()` inside the kebab handler). Tests use the RTL pattern from `src/__tests__/inferredSkills.ui.test.tsx` — vi.mock `@/components/TeamProvider` and `@/components/Toast` if needed, mount the component, assert with `screen.getByRole` / `getByText`.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.tableBoardRender.test.tsx src/__tests__/ownerBoard.tableBoardSort.test.tsx</automated>
  </verify>
  <done>TableBoard.tsx exports a component that renders the 6-column table, sorts on header click, opens the kebab menu without firing row click, and invokes `onAssign` / `onReschedule` callbacks on menu selection. Both vitest files green (Render: 3+ assertions, Sort: 4+ assertions including a tie-breaker test).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: View toggle wiring on OwnerWorkloadBoard — workload ↔ table without losing state</name>
  <files>src/app/projects/OwnerWorkloadBoard.tsx, src/__tests__/ownerBoard.viewToggle.test.tsx</files>
  <read_first>
    - src/app/projects/OwnerWorkloadBoard.tsx — current state from 03.5-03 (DndContext at root, fetch hooks, optimistic overrides)
    - src/components/v2/calendar/WeekNav.tsx (lines 38-118) — view-toggle dropdown pattern with click-outside + Escape + active-option `•` glyph
    - src/components/v2/projects/owner/TableBoard.tsx (from Task 1) — props contract for the table view branch
    - src/components/v2/projects/owner/OwnerWorkloadGrid.tsx (from 03.5-03) — current default view (`data-testid='owner-workload-grid'` should already be present; if not, add it for testing)
    - .planning/phases/03.5-owner-task-board/03.5-UI-SPEC.md §4 "View toggle (D-08)" (lines 237-243) — Radix DropdownMenu, min-width 132px, `cal-view-trigger` class, active = `•` signature pink
    - src/components/v2/projects/owner/AssignTeammatePicker.tsx + ReschedulePicker.tsx — to pass into TableBoard.onAssign / onReschedule
  </read_first>
  <behavior>
    - `viewMode` state defaults to `'workload'`. Toggle dropdown sits in the nav row next to the existing week-paging buttons (mirror WeekNav layout). Active option marked with `•` glyph in `var(--signature)`.
    - When `viewMode === 'workload'`: renders `<OwnerWorkloadGrid>` (current behavior, unchanged). When `viewMode === 'table'`: renders `<TableBoard>` instead. `<TriageDock>` and the CapacityBars wrapper (if rendered above the grid in the current OwnerWorkloadBoard layout) remain mounted in both modes. The DndContext wraps both view branches at root.
    - View toggle does NOT trigger a re-fetch (tasks already loaded). View toggle does NOT clear optimistic overrides (the optimistic state from 03.5-03 survives the toggle so a chip dragged then quickly toggled still reflects the optimistic position).
    - Wiring: `<TableBoard tasks={visibleTasks} teammates={teammates} projects={projects} onRowClick={(t) => openDetailPanel(t.id)} onAssign={(t) => openAssignPicker(t)} onReschedule={(t) => openReschedulePicker(t)} />`. The `openDetailPanel` / `openAssignPicker` / `openReschedulePicker` handlers already exist from 03.5-03 — reuse them.
    - Test (`ownerBoard.viewToggle.test.tsx`): mount `<OwnerWorkloadBoard teamId='t1' />` with mocked fetch returning fixtures. Step 1: assert `queryByTestId('owner-workload-grid')` truthy, `queryByTestId('owner-table-board')` null. Step 2: `fireEvent.click(screen.getByRole('button', {name: /view/i}))` to open dropdown; `fireEvent.click(screen.getByText(/^table$/i))`. Step 3: assert `queryByTestId('owner-table-board')` truthy, `queryByTestId('owner-workload-grid')` null. Step 4: assert `queryByTestId('triage-dock')` truthy across both steps.
  </behavior>
  <action>
    Update `src/app/projects/OwnerWorkloadBoard.tsx`: add `const [viewMode, setViewMode] = useState<'workload'|'table'>('workload')`. Import the WeekNav view-toggle pattern (or replicate inline — lines 38-118 of WeekNav.tsx) — a Radix `DropdownMenu` triggered by a button with `cal-view-trigger` class, two MenuItem children (`workload` / `table`) each rendering text + a leading `•` glyph styled with `color: var(--signature)` when active. Place the toggle in the existing OwnerWorkloadBoard nav row alongside the week-paging buttons. Conditionally render `<OwnerWorkloadGrid />` (when `viewMode === 'workload'`) or `<TableBoard tasks={visibleTasks} teammates={teammates} projects={projects} onRowClick={...} onAssign={...} onReschedule={...} />` (when `viewMode === 'table'`). The TriageDock + (if currently rendered) CapacityBar containers remain outside the conditional branch so they stay mounted across mode changes. Ensure `data-testid='owner-workload-grid'` is on the OwnerWorkloadGrid root (add inside OwnerWorkloadGrid.tsx if missing — one-line addition) and `data-testid='owner-table-board'` is on the TableBoard root (add in Task 1). DO NOT trigger a re-fetch on `setViewMode`. DO NOT clear `optimisticOverrides` state. The test mocks fetch with the existing pattern from `src/__tests__/inferredSkills.api.test.ts` (vi.mock layout). Use `fireEvent.click` via React Testing Library to drive the toggle.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/ownerBoard.viewToggle.test.tsx</automated>
  </verify>
  <done>OwnerWorkloadBoard.tsx renders the view-toggle dropdown; default mode = workload; clicking 'table' swaps to TableBoard and preserves TriageDock + optimistic state. `ownerBoard.viewToggle.test.tsx` green with the 4 step-assertions above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /tasks personal header strip — role-conditional /projects link</name>
  <files>src/app/tasks/page.tsx, src/__tests__/tasks.personalHeaderStrip.test.tsx</files>
  <read_first>
    - src/app/tasks/page.tsx (lines 1-300) — current `V2TasksInner` layout; identify the spot where the column header row starts; the strip must be inserted ABOVE that row and below any existing page eyebrow
    - src/components/TeamProvider.tsx — `useTeam()` hook (already imported in tasks/page.tsx — currentTeam.role is available)
    - The exact /tasks header-strip copy is defined inline in this task's `<action>` block — there is no dedicated UI-SPEC section; treat the action block as source of truth
    - .planning/phases/03.5-owner-task-board/03.5-CONTEXT.md `<scope_boundary>` — DO NOT redesign /tasks; only header strip is in-scope this phase
    - src/__tests__/inferredSkills.ui.test.tsx — RTL render harness with conditional `useTeam` mock returning different role values
  </read_first>
  <behavior>
    - When mounted with `useTeam()` returning `{ currentTeam: { role: 'owner', ... } }`: strip is visible at top of page; contains mono eyebrow `PERSONAL TASKS`, sentence `Your assigned + accepted + in-review tasks. Owners can see the full team board at `, signature-pink `/projects` link (next/link).
    - When mounted with `role: 'member'` or `role: 'viewer'`: strip is visible; same eyebrow + sentence, but the trailing sentence stops at `team board.` (period, no link). No /projects link rendered for non-owners.
    - When `currentTeam` is null (no team selected yet — `loading` state): strip is NOT rendered (avoid flashing).
    - Strip uses existing tokens — `recgon-label` for the eyebrow, default sentence stack for the body, signature-pink for the link (`text-[var(--signature)]` or inline `style={{color: 'var(--signature)'}}`).
    - The existing kanban column header row + drag-drop + proof-drop + reschedule-request flows render UNCHANGED below the strip. Regression guard: the test asserts the three kanban column labels still render.
    - Test (`tasks.personalHeaderStrip.test.tsx`): mount `<V2TasksInner />` twice (or once with rerender flipping the mocked useTeam return value). Case 1: role='owner' → assert `getByText('PERSONAL TASKS')` AND `getByRole('link', {name: /\/projects/})` both truthy; assert kanban columns `assigned` / `in progress` / `in review` all still render. Case 2: role='member' → assert eyebrow still present; assert `queryByRole('link', {name: /\/projects/})` is null; assert kanban columns still render.
  </behavior>
  <action>
    Open `src/app/tasks/page.tsx`. Locate the top of the `V2TasksInner` JSX return — directly inside the outermost wrapper, before the existing column header row (the row containing `assigned` / `in progress` / `in review`). Insert a new fragment containing: (a) a `<div className="recgon-label">PERSONAL TASKS</div>` eyebrow, (b) a `<p>` containing the sentence `Your assigned + accepted + in-review tasks.` followed by a conditional fragment — `{currentTeam?.role === 'owner' && <> Owners can see the full team board at <Link href="/projects" style={{color: 'var(--signature)'}}>/projects</Link>.</>}` and `{currentTeam?.role !== 'owner' && currentTeam && <> Owners can see the full team board.</>}`. Wrap the strip in a parent `<section aria-label="personal tasks header" className="glass-card">` (or whichever wrapper matches the existing /tasks chrome — DO NOT introduce a second nested glass-card). Confirm the existing `Link` import from `next/link` is already in scope (it is — line 4 of the existing file). Render NOTHING when `!currentTeam` (loading). The test uses `vi.mock('@/components/TeamProvider', ...)` returning different role values across cases — mirror the pattern from `src/__tests__/inferredSkills.ui.test.tsx`. Use `rerender` from `@testing-library/react` to flip the mock between the two cases if cheaper than two `render` calls.
  </action>
  <verify>
    <automated>npm run test -- --run src/__tests__/tasks.personalHeaderStrip.test.tsx</automated>
  </verify>
  <done>tasks/page.tsx renders the new header strip at the top of `V2TasksInner` with role-conditional /projects link. `tasks.personalHeaderStrip.test.tsx` green covering both owner + non-owner cases. Existing kanban + drag-drop behavior unchanged (test asserts the three column labels still render).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /projects (TableBoard view) | Tasks travel through same owner-board bulk path as workload view (already privacy-filtered per 03.5-02) |
| Browser → /tasks header strip | Pure client-side render — role check via useTeam(); no server-side trust crossing introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3.5-01 | Authorization | OwnerWorkloadBoard view toggle | mitigate | Already gated upstream — non-owner sees NonOwnerProjectsView per 03.5-01 role branch and never reaches OwnerWorkloadBoard. View toggle is only reachable by owners. No new auth surface this plan. |
| T-3.5-02 | Authorization | TableBoard kebab actions | mitigate | Kebab actions delegate to AssignTeammatePicker + ReschedulePicker (03.5-03), which fire POST /api/recgon/tasks/[id]/assign and POST /api/teams/[id]/tasks/[taskId]/schedule. Both endpoints already enforce owner-only via verifyTeamAccess per 03.5-03 threat model. No new endpoint, no new auth gap. |
| T-3.5-03 | Privacy | TableBoard row data | mitigate | TableBoard consumes the same task list as OwnerWorkloadGrid — sourced from the bulk owner-board GET which already strips assignment_reasoning per 03.5-02 (Pitfall 3 hedge). Row click opens TaskDetailPanel via the existing per-chip /api/recgon/tasks/[id] route which applies the privacy filter on whyYouSentence. No bulk path acquires reasoning JSONB. |
| T-3.5-04 | Concurrency | OwnerWorkloadBoard viewMode + optimistic state | accept | Multi-owner drag conflict = last-write-wins (D-deferred per CONTEXT). View toggle does not introduce new concurrency risk — switching mode mid-drag is impossible (DragOverlay locks pointer state). Optimistic overrides from 03.5-03 are mode-agnostic and survive the toggle. Document inline. |
</threat_model>

<verification>
## Phase-level integration checks (manual UAT after Plan 04 ships)

| Behavior | Manual Steps | Expected |
|----------|--------------|----------|
| View toggle preserves state | Open /projects as owner. Drag a chip (optimistic). Immediately toggle to table view. Toggle back to workload. | Chip is in the dragged position (optimistic state survived toggle). |
| Sort by priority works | Switch to table view. Click PRIORITY header. | Rows sort 0 → 1 → 2 → 3. Click again → reverse. |
| Kebab menu actions on table | Click a row's `⋯` → select `assign manually`. | AssignTeammatePicker opens; same flow as workload-view dock action. |
| /tasks header strip — owner view | Log in as team owner. Visit /tasks. | Strip visible with `/projects` signature-pink link. Click link → routes to /projects (owner board). |
| /tasks header strip — non-owner view | Log in as team member. Visit /tasks. | Strip visible WITHOUT `/projects` link (sentence truncated). |
| /tasks kanban regression | Drag a task from `assigned` → `in progress` on /tasks. | Same behavior as pre-Phase-3.5 — accept action fires, chip moves, toast confirms. |
</verification>

<success_criteria>
- All three vitest files green (TableBoard render + sort, viewToggle wiring, /tasks header strip).
- The OwnerWorkloadBoard view toggle works end-to-end (manual UAT signs off on workload ↔ table swap, sort, kebab actions, and optimistic state preservation).
- The /tasks header strip renders the /projects link only for owners and is invisible during the loading / no-team state.
- Zero regression to /tasks kanban (assigned → in-progress → in-review drag flow unchanged).
- Phase 3.5 vertical-slice acceptance — an owner can: see the empty board (01) → see real tasks + capacity (02) → act on the board with manual-assign / reschedule / dismiss / drag-drop (03) → toggle to table view and see /tasks clarified for the team (04).
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-owner-task-board/03.5-04-SUMMARY.md` per the GSD summary template — document what shipped, what was deferred (still-deferred items: /tasks redesign, mobile redesign, multi-owner conflict resolution per CONTEXT scope_boundary), and any gotchas (e.g., if the view-toggle dropdown had click-outside edge cases that needed escape-hatch handling).
</output>
