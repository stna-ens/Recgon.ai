---
phase: quick-260616-2au
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/recgon/manualAssign.ts
  - src/app/api/recgon/tasks/[id]/assign/route.ts
  - src/app/api/teams/[id]/tasks/route.ts
  - src/components/v2/tasks/CreateTaskModal.tsx
  - src/components/v2/CreateTaskHost.tsx
  - src/components/v2/TopNavV2.tsx
  - src/components/v2/CommandPaletteHost.tsx
  - src/components/WorkspaceShell.tsx
  - src/app/command/page.tsx
  - messages/en/tasks.json
  - messages/tr/tasks.json
  - messages/en/shared.json
  - messages/tr/shared.json
autonomous: true
requirements: [MANUAL-TASK-CREATE]
must_haves:
  truths:
    - "Owner sees a global '+ New Task' button on every page; member/viewer do not"
    - "Owner can create a task with Auto assignee + Auto schedule (current dispatcher behavior)"
    - "Owner can create a task assigned to a specific teammate (owner-gated branch)"
    - "Owner can set a specific schedule date that overrides Recgon's computed date"
    - "A task can be created with no project (projectId = null) and still appears in /command"
    - "The MANUAL_ASSIGN_SENTENCE + reasoning live in one shared helper used by both routes"
    - "npm run lint and npm run test pass"
  artifacts:
    - path: "src/lib/recgon/manualAssign.ts"
      provides: "Shared MANUAL_ASSIGN_SENTENCE + buildManualAssignReasoning() helper"
      contains: "MANUAL_ASSIGN_SENTENCE"
    - path: "src/components/v2/tasks/CreateTaskModal.tsx"
      provides: "Owner create-task modal (title, desc, project, kind, assignee, priority, schedule, deadline)"
      min_lines: 80
    - path: "src/components/v2/CreateTaskHost.tsx"
      provides: "Global event host listening for v2:open-create-task"
      contains: "v2:open-create-task"
  key_links:
    - from: "src/app/api/teams/[id]/tasks/route.ts"
      to: "src/lib/recgon/manualAssign.ts"
      via: "import buildManualAssignReasoning"
      pattern: "manualAssign"
    - from: "src/components/v2/TopNavV2.tsx"
      to: "src/components/v2/CreateTaskHost.tsx"
      via: "window CustomEvent('v2:open-create-task')"
      pattern: "v2:open-create-task"
    - from: "src/components/WorkspaceShell.tsx"
      to: "src/components/v2/CreateTaskHost.tsx"
      via: "JSX mount next to CommandPaletteHost"
      pattern: "CreateTaskHost"
---

<objective>
Let the team owner create a task by hand, with per-task choice of (a) a specific teammate OR Recgon-auto-assign, and (b) a specific schedule date OR Recgon-auto-schedule. Reuse the existing owner command center (/command) as the all-tasks management surface and add a Linear-style global "+ New Task" button on every page.

Purpose: Today tasks are minted+dispatched fully automatically with no manual entry point. The owner needs a way to inject and direct work.
Output: Extended POST /api/teams/[id]/tasks (assignee + schedule branches), a shared manual-assign reasoning helper, a CreateTaskModal, a global event host + top-bar button + command-palette action, a command-center header button, and bilingual EN/TR strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Confirmed signatures — use directly, no exploration needed. -->

src/lib/recgon/storage.ts:
- listTeammates(teamId: string): Promise<Teammate[]>           // Teammate has .id, .displayName, .userId
- listTeammatesWithStats(teamId): Promise<TeammateWithStats[]> // used by GET /api/teams/[id]/teammates -> { teammates }
- getTask(taskId): Promise<AgentTask | null>
- assignTask(taskId, teammateId, assignedBy: 'recgon'|string, jobId?, schedule?: {scheduledDate?, scheduledUntilDate?, deadline?, scheduleNote?}, reasoning?: AssignmentReasoning): Promise<void>
- setTaskSchedule(taskId, schedule: {scheduledDate?, scheduledUntilDate?, deadline?, scheduleNote?}): Promise<void>
- clearTriageNote(taskId): Promise<void>

src/lib/recgon/scheduler.ts:
- type SchedulePlan = { scheduledDate: string /*YYYY-MM-DD*/; deadline: string /*ISO*/; scheduleNote: string }
- planTaskSchedule(input: { teammate: Pick<Teammate,'capacityHours'|'workingHours'>; task: Pick<AgentTask,'estimatedHours'|'deadline'|'priority'|'title'>; loadByDate?; now?; horizonDays? }): SchedulePlan | null

Existing assign route (src/app/api/recgon/tasks/[id]/assign/route.ts) builds:
  reasoning: AssignmentReasoning = { kind:'math_only', mathScore:0, mathBreakdown:{skillOverlap:0,fitForKind:0,availabilityNow:0,loadHeadroom:0,interestNudge:0}, whyYouSentence: MANUAL_ASSIGN_SENTENCE }
  then assignTask(...), enqueueReframeJob(taskId, assigneeId, teamId).catch(...), clearTriageNote(taskId).

POST /api/teams/[id]/tasks currently: verifyTeamWriteAccess -> mintUserTask(...) -> dispatchTask(teamId, task.id) (try/catch swallow) -> getTask -> sanitizeTaskForClient.

UI primitives: import { Modal, Button, FormField } from '@/components/ui'; inputs use className="ui-input"; useToast() from '@/components/Toast' (addToast(msg, 'success'|'error')); useTeam() from '@/components/TeamProvider' gives { currentTeam } where currentTeam.role is 'owner'|'member'|'viewer'.

Command page (src/app/command/page.tsx): useSWR<CommandResponse>(`/api/teams/${teamId}/command`) with { data, mutate }; data.role==='owner' (isOwner), data.teammates (have displayName), data.projects; hero header div className="v2-mc-hero-right".

Global host data sources: teammates via GET /api/teams/[id]/teammates -> { teammates }; projects via GET /api/projects?teamId= -> array of { id, name }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared manual-assign helper + extended POST route</name>
  <files>src/lib/recgon/manualAssign.ts, src/app/api/recgon/tasks/[id]/assign/route.ts, src/app/api/teams/[id]/tasks/route.ts</files>
  <action>
Create src/lib/recgon/manualAssign.ts exporting `MANUAL_ASSIGN_SENTENCE` (the exact existing string 'Owner manually assigned — no automatic fit found.') and `buildManualAssignReasoning(): AssignmentReasoning` returning the math_only zero-math envelope currently inlined in the assign route (lines 43, 108-119). Import AssignmentReasoning type from '@/lib/recgon/types'.

Refactor src/app/api/recgon/tasks/[id]/assign/route.ts to delete its local `MANUAL_ASSIGN_SENTENCE` const and inline reasoning object, importing both from '@/lib/recgon/manualAssign' instead. Behavior must be byte-identical — pure extraction, no logic change.

Extend POST in src/app/api/teams/[id]/tasks/route.ts. Add two optional body fields: `assigneeId?: string | null` and `scheduledDate?: string | null` (YYYY-MM-DD). Treat absent / null / the literal string "auto" as "let Recgon decide". After `mintUserTask(...)` (keep existing mint call unchanged), branch:

- AUTO assignee + AUTO schedule: keep current `dispatchTask(teamId, task.id)` in try/catch (unchanged).
- AUTO assignee + explicit date: run `dispatchTask`; then re-fetch with `getTask`; if it became assigned, call `setTaskSchedule(task.id, { scheduledDate, deadline: new Date(scheduledDate + 'T23:59:59Z').toISOString(), scheduleNote: 'Schedule set by owner.' })` to override the day.
- PERSON assignee (owner-only branch — mirror /api/recgon/tasks/[id]/assign): require `role === 'owner'` from verifyTeamAccess (NOT just verifyTeamWriteAccess) and return 403 if not owner; validate assigneeId is in `listTeammates(teamId)` (400 if not); build reasoning via `buildManualAssignReasoning()`. If explicit date: construct a schedule `{ scheduledDate, deadline: <end-of-day ISO>, scheduleNote: 'Manually scheduled by owner.' }`. If AUTO date: call `planTaskSchedule({ teammate, task: getTask result })` for the chosen teammate; if it returns a plan use its fields, else pass null schedule. Then call `assignTask(task.id, assigneeId, session.user.id, null, schedule, reasoning)`, fire-and-forget `enqueueReframeJob(task.id, assigneeId, teamId).catch(...)`, then `clearTriageNote(task.id)`. Do NOT call dispatchTask in this branch.

In all branches finish with the existing `getTask` + `sanitizeTaskForClient` return. Import the new helpers (assignTask, listTeammates, clearTriageNote, planTaskSchedule, setTaskSchedule, enqueueReframeJob, verifyTeamAccess) at top — verifyTeamAccess returns the role string. Keep the existing title-required 400 and kind validation.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 && grep -q "MANUAL_ASSIGN_SENTENCE" src/lib/recgon/manualAssign.ts && grep -q "from '@/lib/recgon/manualAssign'" "src/app/api/recgon/tasks/[id]/assign/route.ts" && grep -q "manualAssign" "src/app/api/teams/[id]/tasks/route.ts"</automated>
  </verify>
  <done>Helper exists and is imported by both routes; POST handles all four assignee×schedule combinations; person branch is owner-gated; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: CreateTaskModal + global host + top-bar button + palette action + shell mount</name>
  <files>src/components/v2/tasks/CreateTaskModal.tsx, src/components/v2/CreateTaskHost.tsx, src/components/v2/TopNavV2.tsx, src/components/v2/CommandPaletteHost.tsx, src/components/WorkspaceShell.tsx</files>
  <action>
Create src/components/v2/tasks/CreateTaskModal.tsx, modeled on EditTaskModal.tsx (same Modal/Button/FormField/ui-input shell, same submit/error/useToast flow, 'use client'). Props: `{ open, onOpenChange, teamId, defaultProjectId?, teammates?, projects?, onCreated }`. Types: teammates as `{ id: string; displayName: string }[]`, projects as `{ id: string; name: string }[]`. When teammates/projects props are undefined (global button path), fetch on open: teammates from `/api/teams/${teamId}/teammates` (read `.teammates`, map to {id, displayName}) and projects from `/api/projects?teamId=${teamId}` (map to {id, name}). Use `useTranslations('tasks')` + `useTranslations('common')`.

Fields in this order: (1) Title required, autofocus; (2) Description textarea; (3) two-column row: Project select with a first "No project" option (value "") seeded from defaultProjectId, and Kind select (next_step, dev_prompt, marketing, analytics, research, custom); (4) two-column row: Assignee select whose first option is value "auto" labeled t('createTask.autoAssignee'), then teammates by displayName, and Priority select (low=1, normal=2, high=3); (5) two-column row: Schedule date input (empty = auto) and optional Deadline date input.

Submit -> POST `/api/teams/${teamId}/tasks` with JSON body: title, description, kind, projectId (null when ""), priority (Number), deadline (null when empty). Include assigneeId ONLY when assignee !== 'auto'; include scheduledDate ONLY when the schedule date input is non-empty. On !res.ok throw j.error. On success: toast — if assignee was a teammate use t('createTask.assignedTo', {name}); else t('createTask.queued'); call onCreated(); onOpenChange(false). Mirror EditTaskModal's submitting/error state handling exactly.

Create src/components/v2/CreateTaskHost.tsx mirroring CommandPaletteHost's event pattern ('use client'): useState open; useTeam() for currentTeam; usePathname() to infer defaultProjectId when path matches /projects/[id] (regex `^/projects/([^/]+)`); useEffect adds window listener for 'v2:open-create-task' -> setOpen(true). Render `<CreateTaskModal open teamId={currentTeam.id} defaultProjectId={...} onCreated={() => window.dispatchEvent(new CustomEvent('v2:task-created'))} onOpenChange={setOpen} />` (no teammates/projects props -> modal self-fetches). Return null when no currentTeam.

In src/components/v2/TopNavV2.tsx: add a "+ New Task" button inside `.v2-topnav-right` (before the cmdk trigger), rendered only when `currentTeam?.role === 'owner'` (import/use useTeam()). onClick dispatch `window.dispatchEvent(new CustomEvent('v2:open-create-task'))`. Label from ts('nav.newTask') — reuse the existing shared-namespace translator already in this file. Give it a class like `v2-newtask-trigger` and add minimal CSS matching the existing trigger button style (do not introduce alien aesthetics; reuse glass-card/signature-pink tokens already used in this file's <style>).

In src/components/v2/CommandPaletteHost.tsx: add one entry to `actionCommands` (id 'act-create-task', group actionsGroup, keywords ['new','task','create','add','görev']) whose run does `setOpen(false); window.dispatchEvent(new CustomEvent('v2:open-create-task'));`. Label from t('commandPalette.createTask') in the shared namespace.

In src/components/WorkspaceShell.tsx: import CreateTaskHost and mount `<CreateTaskHost />` right next to the existing `<CommandPaletteHost />`.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 && grep -q "v2:open-create-task" src/components/v2/TopNavV2.tsx && grep -q "v2:open-create-task" src/components/v2/CreateTaskHost.tsx && grep -q "CreateTaskHost" src/components/WorkspaceShell.tsx && grep -q "act-create-task" src/components/v2/CommandPaletteHost.tsx</automated>
  </verify>
  <done>Owner sees "+ New Task" in the top bar; clicking (or the palette action) opens CreateTaskModal which self-fetches teammates/projects; modal POSTs and toasts; host is mounted in WorkspaceShell.</done>
</task>

<task type="auto">
  <name>Task 3: Command-center header button + bilingual i18n (EN/TR)</name>
  <files>src/app/command/page.tsx, messages/en/tasks.json, messages/tr/tasks.json, messages/en/shared.json, messages/tr/shared.json</files>
  <action>
In src/app/command/page.tsx: import CreateTaskModal and add local state `const [createOpen, setCreateOpen] = useState(false)`. In the hero header `.v2-mc-hero-right` add an owner-only ("+ New Task", shown when isOwner) Button that calls setCreateOpen(true). Render a `<CreateTaskModal open={createOpen} onOpenChange={setCreateOpen} teamId={teamId} teammates={data?.teammates ?? []} projects={data?.projects ?? []} onCreated={() => { void mutate(); }} />` — pass the already-loaded teammates/projects so it does NOT re-fetch. Map data.teammates to {id, displayName} if their shape differs. Use the page's existing translator for the button label (t('createTask.newButton') or shared nav.newTask — pick whichever namespace the page already imports; if none, add useTranslations('tasks')).

Add to BOTH messages/en/tasks.json and messages/tr/tasks.json a `createTask` block with keys: title, newButton, success/error, autoAssignee ("Auto — let Recgon decide" / Turkish equivalent), autoSchedule, queued ("Queued for Recgon"), assignedTo ("Assigned to {name}"), failed, fields.{title,description,project,noProject,kind,assignee,priority,schedule,deadline}, kindOptions.{next_step,dev_prompt,marketing,analytics,research,custom}, priorityOptions.{low,normal,high}. Provide natural Turkish translations in the tr file (never copy English).

Add `nav.newTask` to BOTH messages/en/shared.json and messages/tr/shared.json, and `commandPalette.createTask` to BOTH shared files. Verify JSON stays valid (no trailing commas). Keys must match exactly what Tasks 2 and 3 reference; reconcile any naming the executor chose in earlier tasks so no t() call resolves to a missing key.
  </action>
  <verify>
    <automated>node -e "['messages/en/tasks.json','messages/tr/tasks.json','messages/en/shared.json','messages/tr/shared.json'].forEach(f=>JSON.parse(require('fs').readFileSync(f)));console.log('json ok')" && grep -q "createTask" messages/en/tasks.json && grep -q "createTask" messages/tr/tasks.json && grep -q "newTask" messages/en/shared.json && npm run lint 2>&1 | tail -3 && npm run test 2>&1 | tail -8</automated>
  </verify>
  <done>Command center shows owner-only "+ New Task" opening a pre-populated modal that refetches the table on create; createTask + nav.newTask + commandPalette.createTask exist in EN and TR; all message JSON parses; lint and test pass.</done>
</task>

</tasks>

<verification>
1. `npm run lint` passes (no unused imports, no missing-key warnings surfaced by next-intl in dev).
2. `npm run test` passes (existing assign-route + scheduler tests unaffected by the helper extraction).
3. All four message files parse as valid JSON and contain the new keys in both locales.
4. Manual (owner login): top bar shows "+ New Task" for owner, absent for member/viewer; create with Auto+Auto -> appears in /command; create with specific teammate+Auto -> assigned + scheduled; create with teammate+date -> assigned on that exact date; create with No project -> projectId null, still in /command.
</verification>

<success_criteria>
- POST /api/teams/[id]/tasks handles assignee×schedule (auto/auto, auto/date, person/auto, person/date); person branch is owner-gated and validates the teammate.
- MANUAL_ASSIGN_SENTENCE + reasoning live only in src/lib/recgon/manualAssign.ts, imported by both routes (no duplicated constant).
- Global "+ New Task" button (owner-only) + command-palette action + command-center header button all open the same CreateTaskModal via the v2:open-create-task event.
- All new UI strings exist in both EN and TR.
- npm run lint and npm run test pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260616-2au-manual-task-creation-owner-with-optional/260616-2au-SUMMARY.md`
</output>
